{
  "version": "FN-1.4",
  "note": "재무 백필(A3)의 수집 파라미터와 인수 조건. price.v1.json과 같은 층(dataPolicies)이며 score()도 A6도 읽지 않는다. 여기가 정의하는 것은 '어느 시점에 무엇을 알 수 있었는가'다 — A3의 본질은 재무 수치가 아니라 시점(PIT) 축이다. 운영 수집기(scripts/fetch-fundamentals-kr.js)는 '현재 값'을 쓰고 이 정책은 '그 시점에 알 수 있었던 값'을 쓴다.",
  "market": "KR",

  "source": {
    "provider": "DART OpenAPI",
    "endpoint": "fnlttSinglAcnt.json",
    "endpointNote": "주요계정. FN-1.0은 fnlttSinglAcntAll(전체 재무제표)을 골랐고 정찰이 그 선택을 뒤집었다(2026-08-05, scripts/probe-fundamentals-a3.py). 이유는 두 가지이며 첫 번째가 결정적이다. (1) 전체 재무제표 응답에는 thstrm_dt가 없다 — 표본 240건 전건에서 회계기간말을 읽지 못했다(주요계정은 240/240 성공). 회계기간말이 없으면 계약 1(availableFrom > periodEnd)을 잴 수단 자체가 없고, 수집기는 전건을 PERIOD_END_UNPARSED로 버린다. (2) 선택의 근거였던 '이름 매칭은 취약하다'가 실측에서 성립하지 않았다 — 7계정 전부 매칭이 주요계정 96.25% 대 전체 재무제표 96.67%로, 240건 중 1건 차이였다. 부채총계·자본총계는 nameExact 240/240으로 미스가 0이고, 미스가 몰린 유동자산·유동부채(3.3%)는 금융업 양식이라 애초에 커버리지 분자 밖이다.",
    "endpointCallNote": "호출은 (corp, 사업연도)당 정확히 1회다. 주요계정은 fs_div가 파라미터가 아니라 응답 행의 필드라서 연결·별도가 한 번에 온다 — 전체 재무제표는 fs_div가 필수라 실측 1.43회였다. 그 결과 총 호출이 45,612 대 65,092로 30% 싸다. FN-1.0은 '호출량 이점이 시점(PIT)을 사면 안 된다'고 적었는데, 실측에서는 호출량과 PIT가 같은 쪽이었다. 경계는 옳았고 어느 쪽이 그 대가를 치르는지에 대한 추정이 틀렸다.",
    "thstrmOnlyNote": "당기(thstrm) 열만 쓴다. 전기(frmtrm)·전전기(bfefrmtrm)를 쓰면 안 된다 — 그 값들의 availableFrom은 '그 보고서의 접수일'이라 과거 연도를 실제보다 늦게 안 것으로 기록하게 된다(교훈47). 엔드포인트가 3개 연도를 한 번에 준다는 사실은 호출량을 줄일 유혹이지 시점을 바꿀 근거가 아니다. 이 규칙이 무너지면 엔드포인트를 바꾼 이득이 그대로 사고가 된다.",
    "reprtCode": "11011",
    "reprtCodeNote": "사업보고서(연간)만 쓴다. 분기·반기를 섞으면 같은 fiscalYear에 서로 다른 확정도의 숫자가 섞이고, 어느 것이 '그 시점의 최선'인지 판정 규칙이 하나 더 필요해진다. 연간으로 고정하면 availableFrom 하나로 순서가 정해진다.",
    "fsDivPreference": ["CFS", "OFS"],
    "fsDivNote": "연결 우선, 없으면 별도. 주요계정에서 fs_div는 요청 파라미터가 아니라 응답 행의 필드이므로 이것은 '재시도 순서'가 아니라 '행 선별 순서'다 — 한 번의 응답에서 CFS 행을 먼저 찾고 없으면 OFS 행을 쓴다. 정찰 실측 분포는 CFS 220 · OFS 20이었다.",
    "companyEndpoint": "company.json",
    "companyEndpointNote": "표준산업분류코드(induty_code)만 받는다. 법인당 1회다(연도별 아님). sectorType으로의 변환은 A3가 하지 않는다 — 그 규칙은 lib/sectorResolver.js와 config/sector-map.json에 이미 있고, 파이썬으로 다시 구현하면 같은 계약이 두 벌이 되어 둘이 같이 틀린다(교훈44). A3는 sicCode라는 사실만 남기고 해석은 JS 쪽 단일 창구가 한다.",
    "sectorPitCaveat": "sicCode는 PIT가 아니다. DART는 업종의 시계열을 제공하지 않으므로 '현재의 업종'이 전 사업연도에 붙는다. 업종 변경이 드물어 실무상 영향이 작다고 보고 그대로 쓰되, 그것은 가정이지 사실이 아니므로 진단의 sectorNotPointInTime 플래그로 하류에 들고 간다 — 가정을 숨기면 나중에 사실로 굳는다."
  },

  "fiscalYearFrom": 2015,
  "fiscalYearFromNote": "fnlttSinglAcntAll은 2015 사업연도부터 제공된다. FY2015 사업보고서는 2016-03경 접수되므로 2016-01~03 스냅샷은 연간 재무가 아예 없다 — 커버리지 손실이지 look-ahead가 아니다. 이 구간을 채우려고 FY2015 보고서의 전기·전전기 열을 끌어오면 2013·2014 수치에 2016-03이라는 availableFrom이 붙어 '늦게 알았다'가 되고, 2016-01 스냅샷에는 여전히 쓸 수 없다. 즉 이 공백은 메울 수 없고 A6의 coverage decile이 드러낼 사실이다.",

  "fiscalYearTo": 2025,
  "fiscalYearToRule": "현재월 >= 4월이면 (현재연도 - 1), 아니면 (현재연도 - 2)",
  "fiscalYearToNote": "규칙으로 계산하지 않고 값을 박는다 — 실행 시각에 따라 수집 범위가 달라지면 같은 정책으로 돌린 두 실행의 산출물이 갈리고 manifest 해시가 재현성을 잃는다. 대신 스크립트가 규칙과 대조해 어긋나면 WARN을 낸다. 해가 바뀌면 이 값을 올리는 것이 절차의 일부다.",

  "shards": 8,
  "shardsNote": "실행 축이다. 저장 축은 fiscalYear별 파일이라 샤드 수를 바꿔도 산출물 바이트가 바뀌지 않는다(A2a와 동일한 분리). 법인은 corp 정렬 후 라운드로빈으로 분배한다 — 연속 구간으로 자르면 폐지 법인이 특정 샤드에 몰려 소요와 실패율이 크게 갈린다.",

  "collectionContract": {
    "fields": [
      "source.endpoint",
      "source.reprtCode",
      "source.fsDivPreference",
      "source.companyEndpoint",
      "fiscalYearFrom",
      "fiscalYearTo",
      "accounts.matchOrder",
      "accounts.spec",
      "stopAfterConsecutiveEmptyYears"
    ],
    "note": "resume 호환 판정의 단일 출처다. FN-1.2까지는 '정책 version 문자열이 같은가'로 판정했는데, 그것은 재개 가능성의 blunt한 대리 지표였다 — 수집이 여러 날에 걸치는 단계에서 임계 하나만 고쳐도 version이 올라가고, 그 순간 8샤드의 상태가 전부 폐기되어 이미 쓴 DART 호출이 사라진다. FN-1.3 실측: 그 경로가 발동했다면 1,381법인·16,050호출·하루가 조용히 버려졌을 것이다. '버전 승격이 하루치 수집을 말없이 버린다'는 done.add 결함과 같은 모양의 resume 무결성 결함이다.",
    "principleNote": "여기 나열하는 것은 **수집 결과를 결정하는 규칙**이다. 이 값들이 그대로면 어제의 수집은 오늘의 규칙으로도 같은 수집이므로 이어받아도 된다. 반대로 acceptance 임계·quota·probeCorps·retryAttempts·output·note는 여기 없다 — 그것들은 finalize의 판정이거나 같은 결과에 이르는 경로의 차이일 뿐, 무엇이 수집되고 무엇이 공백이 되는지를 바꾸지 않는다. 판단 기준은 하나다: '이 값이 달랐다면 어제와 다른 수집 결과가 나왔는가?'",
    "failureClassificationNote": "**failureClassification은 여기 없다.** 한 번 넣었다가 뺐고, 그 왕복이 이 판정의 기준을 가장 정확하게 설명한다.\n\n넣었던 근거는 '어떤 실패를 재시도 불가로 볼 것인가는 그 법인이 계속 재시도될지 승인 대상 공백이 될지를 가른다'였다. 그럴듯하지만 이 구현에서는 사실이 아니다. 코드에서 retryable은 세 곳에만 나타난다 — hardSkipped 항목의 라벨, hardSkippedByRetryable 진단 집계, 중단 메시지의 힌트. todo 계산에도 shard_status에도 완료 게이트에도 들어가지 않는다. todo는 'done에 없는 전부'이므로 **재시도 불가로 분류된 법인도 매 실행 똑같이 재시도되고**, 공백을 닫는 것은 오직 사람의 승인이다. 즉 이 표는 수집 결과를 바꾸지 않고, 사람이 승인을 판단할 때 보는 자문 라벨을 바꾼다. 게다가 그 라벨은 다음 재시도에서 현재 표로 다시 계산되므로 낡음이 자가 치유된다.\n\n그래서 넣으면 비용만 남는다: 표를 고치는 순간 진행 중인 수집이 폐기된다. 오류 원인 분해 과정에서 새 status를 발견해 표에 넣는 것은 정상적인 개선인데, 그때마다 며칠치 수집을 잃게 된다.\n\n**판정 기준은 '결과에 영향을 주는가'가 아니라 '이미 모은 것을 다시 쓸 수 있는가'다.** 두 질문이 대개 같은 답을 주지만 여기서 갈렸다. 만약 나중에 retryable이 실제로 수집 경로를 가르도록 바뀐다면(예: 재시도 불가는 todo에서 제외), 그때는 이 표가 이 목록에 들어와야 한다 — 그 변경과 이 목록은 함께 움직인다.",
    "hashNote": "해시 대상은 위 경로들의 값과 **경로 목록 자체**다. 목록을 바꾸는 것은 '무엇이 계약인가'를 바꾸는 일이라 그때는 이어받지 않는 쪽이 안전하다 — 보수적 방향(재수집)으로 틀리게 만든다. shards는 여기 없고 별도 필드로 남는다: 그것은 레코드 내용이 아니라 담당 법인 배분을 바꾸므로 판정과 사유 메시지를 따로 두는 편이 읽힌다.",
    "provenanceNote": "이 완화로 하나의 산출물이 여러 정책 버전에 걸쳐 수집될 수 있게 됐다. 그래서 상태가 policyVersions를 누적하고 finalize가 collectionPolicyVersions로 진단에 남긴다 — 옛 판정이 주던 '이 레코드는 한 버전에서 나왔다'는 정보를 값으로 대체한 것이다. 버리는 것은 제약이지 기록이 아니다."
  },

  "requestSleepSeconds": 0.12,
  "retryAttempts": 4,
  "retryBackoffBase": 2,
  "circuitBreakerConsecutiveFailures": 30,
  "circuitBreakerNote": "'조회된 데이터 없음'(013)은 실패가 아니다 — 그 법인이 그 해에 보고서를 안 냈다는 정상 사실이므로 연속 실패 카운터에 넣지 않는다. 카운터가 세는 것은 네트워크 오류·비2xx·파싱 실패·정의되지 않은 오류(900)다. 013을 실패로 세면 폐지 법인 구간에서 서킷이 즉시 열린다.",

  "failureClassification": {
    "nonRetryableStatuses": ["100", "101"],
    "defaultRetryable": true,
    "retryableNote": "하드 실패를 두 갈래로만 나눈다. DART status가 nonRetryableStatuses에 있으면 retryable=false, **그 밖의 모든 것은 true**다 — HTTP 5xx·타임아웃·연결 오류·JSON 파싱 실패·800(시스템 점검)·900(정의되지 않은 오류), 그리고 이 표에 없는 새 status 전부.",
    "defaultNote": "기본값이 true인 것이 이 표의 핵심 계약이다. 반대로 두면 DART가 새 오류 코드를 내놓는 날마다 그 법인들이 조용히 '수집 불가'로 승격되고, 복구 가능한 데이터가 영구히 버려진다 — done.add가 만든 결함과 같은 모양이 다른 자리에서 재발한다. 모르는 실패는 '아직 모른다'이지 '못 한다'가 아니다.",
    "nonRetryableNote": "100(부적절한 값)·101(부적절한 접근)만 false다. 둘 다 파라미터 계약 위반이라 같은 요청을 다시 보내면 같은 답이 온다 — 재시도가 정보를 더 주지 않는 유일한 부류다.",
    "notClassifiedHere": "020(한도 초과)과 013(데이터 없음)은 여기 없다. 전자는 루프를 break해 다음 실행이 이어받고, 후자는 '그 해에 보고서를 안 냈다'는 정상 사실이라 애초에 실패가 아니다. 이 표가 분류하는 것은 '보고서가 있어야 하는데 못 받은 경우'뿐이다.",
    "approvalNote": "retryable=true는 아무리 오래 실패해도 WARN으로 승격하지 않는다 — 미완료로 남아 다음 실행이 계속 재시도한다. 일시 장애가 N번 반복됐다는 것은 그 법인의 데이터가 없다는 뜻이 아니다. retryable=false만 사람의 승인(config/backfill/declared-gaps-a3.json) 대상이며, 그 채널은 커밋 2에서 만든다. 그때까지 retryable=false가 하나라도 나오면 finalize가 막힌다 — 조용히 통과하는 것보다 낫다.",
    "policyNotCodeNote": "이 표가 코드가 아니라 정책에 있는 이유는 임계와 성질이 같아서다. '어떤 실패를 수집 불가로 볼 것인가'는 코드에 두면 조용히 넓어지고, 넓어진 사실이 파일 해시에 남지 않는다."
  },

  "probeCorps": ["00126380", "00164779"],
  "probeYear": 2023,
  "probeNote": "대량 루프 진입 전 2회 정찰(교훈32). 삼성전자·SK하이닉스로 경로가 열려 있는지만 본다. 둘 다 실패하면 3,801법인을 돌리지 않고 즉시 중단한다.",

  "quota": {
    "dailyCallLimit": 20000,
    "safetyMarginCalls": 4000,
    "quotaExceededStatus": "020",
    "shardBudgetMode": "activeShards",
    "note": "DART 일 한도. 초과하면 다음날까지 대기이므로 resume이 선택이 아니라 계약이다(BF-1.1 §7 A3 계약 5). 한도는 API 키 단위라 샤드 전체가 하나의 예산을 나눠 쓴다.",
    "shardBudgetModeNote": "FN-1.4에서 equalSplit → activeShards. 허용값은 둘뿐이고 모르는 값은 FAIL이다(느슨해지는 방향의 우회를 막는다).\n\n  equalSplit    (dailyCallLimit − safetyMarginCalls) / shards        — 옛 동작\n  activeShards  내가 오늘 쓴 양 + (dailyCallLimit − safetyMarginCalls − 오늘 전체 사용) / 활성 샤드 수\n\n바꾼 이유는 마지막 샤드가 항상 뒤처지는데 8등분이라 그때 예산의 87.5%가 놀기 때문이다. 2026-08-07 실측: 샤드 6에 1,478건이 필요했고 A3 몫의 잔여는 3,465건이었는데 나눗셈이 막아 하루를 더 썼다. 안전여유를 0으로 해도 풀리지 않는다(샤드당 2,500인데 샤드 6이 이미 2,005를 썼다). A3b를 신설하면 같은 일이 또 생긴다.\n\n'오늘 전체 사용'과 '활성 샤드 수'는 체크아웃 시점에 커밋돼 있는 _state-*.json에서 읽는다. 샤드는 병렬 matrix라 실행 중 서로를 못 보지만 같은 스냅샷을 보므로 배분의 합이 절대 한도를 넘지 않는다 — Σ(내가 쓴 양) + 활성수 × (풀 / 활성수) ≤ dailyCallLimit − safetyMarginCalls. 다른 샤드의 callsUsedToday는 그 상태의 lastRunDate가 오늘(KST)일 때만 센다. 상태 파일이 없는 샤드는 '활성이며 0 사용'으로 본다 — 활성 수가 커지는 방향이라 과다 배분이 되지 않는다.\n\nquota는 collectionContract.fields에 없으므로 이 변경은 resume을 깨지 않는다.",
    "safetyMarginNote": "일 한도의 20%다. 감이 아니라 같은 키를 쓰는 다른 소비자의 실측 합이다(2026-08-05): disclosures.yml이 평일 매일 KR 100종목 × list.json으로 100~200건, quarterly-fundamentals.yml이 연 4회 100종목 × 4회로 약 400건, add-ticker.yml이 같은 수집기를 dispatch로 돌려 약 400건(시점 예측 불가), dart-corpcode-a07이 월 1건. 최악의 겹침이 하루 약 1,000건이므로 4,000은 그 4배다. 첫 설계값 600은 이 겹침보다 작아서 A3가 운영 워크플로의 한도를 먹는 구간이 있었다. 여유분을 늘려서 치르는 비용은 수집 일수 2.5일 → 3.1일 하루뿐이다 — 백필은 한 번 도는 일이고 운영은 매일 도므로, 둘이 부딪히면 양보하는 쪽은 백필이다.",
    "overshootNote": "예산 검사는 법인 단위로 한다. 한 법인이 최대 사업연도 11개 × fs_div 2회 + 업종 1회를 쓰므로 샤드당 최대 25건 정도 초과할 수 있고, 8샤드 합쳐 약 200건이다. 안전 여유분이 이것도 함께 흡수한다. 검사를 호출 단위로 내리지 않는 이유는 법인 중간에 끊으면 그 법인의 부분 레코드가 남고, 재개 시 같은 키가 두 벌이 되기 때문이다.",
    "shardBudgetNote": "샤드가 자기 예산을 다 쓰면 실패가 아니라 '오늘 몫을 마쳤다'로 끝낸다. 진행 상태를 커밋하고 exit 0으로 끝내며, 다음 실행이 이어받는다. 여기서 exit(1)을 내면 정상적인 분할 실행이 매일 빨간 불로 보인다."
  },

  "collectionWindow": {
    "recordDays": true,
    "immutabilityNote": "확정된 과거 사업연도의 사업보고서는 불변이므로 수집이 며칠에 걸쳐도 된다. 예외는 정정공시 하나뿐이다 — 수집 중간에 정정본이 올라오면 산출물이 혼합 시점 스냅샷이 된다(A법인은 1일차 응답, B법인은 3일차 응답).",
    "pitNote": "혼합 시점은 PIT를 깨지 않는다. 각 레코드가 자기가 받은 rcept_no에서 나온 availableFrom을 들고 있고, 정정 전 원본도 '그때 실제로 존재했던 보고서'라 유효한 PIT 레코드이기 때문이다. 깨지는 것은 재현성이다 — 재수집하면 바이트가 달라지는데 그 차이가 버그인지 정정공시인지 구분할 근거가 없다. 그래서 수집 창(첫 실행일 ~ 마지막 실행일)을 진단과 manifest에 남긴다. 해시가 바뀌었을 때 후보 원인이 문서로 남아 있어야 한다.",
    "perRecordNote": "레코드마다 collectedAt을 넣지 않는다. 해시 대상 산출물에 비결정적 필드가 들어가면 generatedAt을 해시에서 제외한 이유가 무의미해지고, 재수집할 때마다 내용이 같아도 해시가 갈린다. 수집 시점은 집계(수집 창)로만 남긴다."
  },

  "stopAfterConsecutiveEmptyYears": 3,
  "stopAfterNote": "한 법인이 보고서를 낸 뒤 연속 3개 사업연도가 비면 그 법인의 스캔을 멈춘다. 폐지 법인은 마지막 보고서 이후 영원히 013을 돌려주므로, 이 조기 종료가 1,222개 폐지 법인의 호출을 크게 줄인다. 3인 이유는 1~2년 결산 공백(감사의견 거절 후 지연 공시 등)이 실재하기 때문이다 — 1로 두면 공백 뒤에 재개된 보고서를 통째로 놓친다. 보고서를 한 번도 못 찾은 법인은 이 규칙을 적용하지 않고 전 연도를 시도한다(시작점을 모르기 때문이다).",

  "accounts": {
    "matchOrder": ["id", "nameExact", "nameContains"],
    "matchOrderNote": "IFRS 태그를 1순위로 두되 주요계정 응답에는 account_id가 없으므로 실질은 이름 매칭이다. 태그 경로를 남겨두는 이유는 두 가지다 — 전체 재무제표로 되돌아갈 때 같은 코드가 그대로 동작하고, 태그 비교가 접두사를 떼고 꼬리만 보므로('ifrs_Equity'와 'ifrs-full_Equity'를 같게 본다) 2019년 전후 taxonomy 변경에 이미 대비돼 있다. 어느 수단으로 잡았는지는 레코드의 accountSource에 남는다: 계약 2(연도별 매칭률)의 하락이 소스 변화인지 이름 변주인지 사후에 가를 수 있어야 한다. 정찰 표본은 32법인이라 긴 꼬리의 이름 변주를 다 보지 못한다 — 그것을 잡는 것은 accountSourceDistribution과 연도별 커버리지 WARN이다.",
    "spec": {
      "currentAssets": { "sj": ["BS"], "idTails": ["CurrentAssets"], "exact": ["유동자산"], "contains": [] },
      "currentLiab":   { "sj": ["BS"], "idTails": ["CurrentLiabilities"], "exact": ["유동부채"], "contains": [] },
      "liabilities":   { "sj": ["BS"], "idTails": ["Liabilities"], "exact": ["부채총계"], "contains": [] },
      "equity":        { "sj": ["BS"], "idTails": ["Equity"], "exact": ["자본총계"], "contains": [] },
      "revenue":       { "sj": ["IS", "CIS"], "idTails": ["Revenue"], "exact": ["매출액", "수익(매출액)", "영업수익"], "contains": ["영업수익", "매출"] },
      "opProfit":      { "sj": ["IS", "CIS"], "idTails": ["OperatingIncomeLoss"], "exact": ["영업이익"], "contains": ["영업이익"] },
      "netIncome":     { "sj": ["IS", "CIS"], "idTails": ["ProfitLoss"], "exact": ["당기순이익"], "contains": ["당기순이익"] }
    },
    "specNote": "유동자산·유동부채가 둘 다 없는 것은 결측이 아니라 금융업 재무제표 양식이다(fetch-fundamentals-kr.js의 v1 휴리스틱). 따라서 이 두 계정의 미매칭률은 게이트로 쓰지 않고 sectorType과 교차해서만 읽는다.",
    "requiredForCoverage": ["equity", "netIncome", "revenue", "opProfit", "liabilities"],
    "requiredForCoverageNote": "커버리지 계산의 분자다. 유동자산·유동부채를 뺀 이유는 위 specNote와 같다 — 금융업이 구조적으로 결측이므로 넣으면 업종 구성이 커버리지로 위장한다."
  },

  "pointInTime": {
    "availableFromSource": "rcept_no",
    "availableFromRule": "rcept_no 앞 8자리(YYYYMMDD)를 접수일로 읽는다",
    "contract": "availableFrom > periodEnd",
    "contractNote": "BF-1.1 §7 A3 계약 1. 회계기간말이 아니라 공시가 실제로 나온 날부터 그 숫자를 알 수 있다. 이걸 뒤집으면 백테스트에 look-ahead가 들어간다. 위반은 FAIL이다 — 구조적으로 발생할 수 없고, 발생했다면 로직 반전이거나 periodEnd 파싱 오류다.",
    "periodEndSource": "thstrm_dt",
    "periodEndSourceNote": "회계기간말은 보고서가 스스로 말하는 값을 쓴다. A1a의 fiscalMonth로 계산하면 안 된다 — 그것은 '현재의 결산월'이라 결산월을 변경한 회사의 과거 보고서와 어긋나고, 애초에 A1b 폐지 법인에는 그 필드가 없다(실측: A1a 2,579종목 중 12월 결산이 아닌 곳이 52종목).",
    "periodEndAvailabilityNote": "이 필드가 엔드포인트 선택을 뒤집었다. 정찰 실측(2026-08-05): 주요계정은 thstrm_dt를 240/240 제공하고 전체 재무제표는 0/240이다. 두 형태('2016.01.01 ~ 2016.12.31'과 '2016.12.31 현재')로 오며 회계기간말은 어느 쪽이든 마지막 날짜다. 회계기간말이 없으면 계약 1을 잴 수단이 없고, 잴 수 없는 계약은 계약이 아니다.",
    "amendmentCaveat": "DART가 돌려주는 rcept_no는 정정공시가 있으면 정정본의 것이다. 따라서 availableFrom은 원본 접수일보다 늦을 수 있고, 그 보고서의 수치는 실제보다 늦게 알려진 것으로 기록된다. 방향이 보수적이라 그대로 둔다 — look-ahead가 아니라 커버리지 손실로 나타나기 때문이다. 반대 방향(원본 접수일을 쓰되 정정된 수치를 붙이는 것)이 진짜 사고다: 그 시점에 존재하지 않던 숫자를 그 시점에 알았다고 기록하게 된다.",
    "restatementNote": "같은 (corp, fiscalYear)에 서로 다른 availableFrom이 여럿 나올 수 있다(정정공시). 그것은 중복이 아니라 사실이므로 병합하지 않는다. 유일성 키는 (corp, fiscalYear, availableFrom)이고, A5는 스냅샷일 d에 대해 availableFrom <= d인 것 중 fiscalYear가 가장 큰 것, 그 안에서 availableFrom이 가장 큰 것을 고른다."
  },

  "derived": {
    "roe": "netIncome / equity",
    "debtRatioRaw": "liabilities / equity",
    "note": "A3는 사실 층이다(BF-1.1 §5). 파생값은 인수 조건이 요구하는 두 개만 진단용으로 계산하고 산출물에는 원계정을 그대로 남긴다. 특히 금융업 debtRatio를 null로 비우는 규칙(fetch-fundamentals-kr.js의 RAW_DEBT_SECTORS)은 A3가 적용하지 않는다 — 그것은 채점 정책이고, 정책 층에서 적용해야 정책 버전이 바뀔 때 재수집이 아니라 재계산으로 끝난다."
  },

  "acceptance": {
    "availableFromMissing": 0,
    "availableFromNotAfterPeriodEnd": 0,
    "periodEndMissing": 0,
    "dateContractViolations": 0,
    "corpContractViolations": 0,
    "duplicateKeys": 0,
    "yearsWithNoData": 0,
    "minCorpsWithDataWarn": 2200,
    "coverageRateMinWarn": 0.60,
    "coverageRateMin": 0.86,
    "currentListedCoverageMin": 0.96,
    "yearCoverageDropWarn": 0.15,
    "roeAbsOutlierThreshold": 2.0,
    "roeAbsOutlierRateWarn": 0.02,
    "negativeEquityRateWarn": 0.05,
    "periodEndParsedRateMin": 0.99
  },
  "periodEndParsedRateNote": "확보한 보고서 중 회계기간말을 읽어낸 비율이다. 실측 전인데도 FAIL인 유일한 임계이며, 이유는 이 실패가 등급이 아니라 이분적이기 때문이다 — 정찰 실측이 주요계정 240/240 대 전체 재무제표 0/240으로 갈렸다. 응답 형식이 바뀌거나 파서가 깨지면 비율이 서서히 나빠지는 것이 아니라 통째로 무너진다. 0.99는 '전부 아니면 전무' 사이의 빈 구간에 놓은 선이므로, 이 값이 걸리면 그것은 품질 저하가 아니라 소스·파서의 구조 변화다.\n\n이 지표가 필요한 이유는 산출물만으로는 보이지 않기 때문이다. periodEnd를 못 읽은 보고서는 build_record가 레코드로 만들지 않으므로 산출물의 periodEndMissing은 언제나 0이다. 즉 확보한 보고서의 절반을 잃어도 인수 조건은 전건 통과한다 — 분모가 산출물에 남지 않는 손실이다. 그래서 샤드 상태에 누적 카운터(reportsFound·periodEndParsed)를 두고 finalize가 그것으로 비율을 낸다. 진단만으로 세면 안 된다: 샤드 진단은 실행마다 덮이는데 수집은 며칠에 걸친다.",

  "probeAcceptance": {
    "periodEndParsedRateMin": 0.99,
    "note": "정찰도 같은 임계로 판정한다. 정찰의 목적은 발견이므로 어떤 결과든 산출물을 남기지만, 정책이 고른 엔드포인트가 PIT 앵커를 주지 못한다는 것은 발견이 아니라 계약 위반이다 — 그 상태로 수집을 시작하면 3일 뒤 0레코드를 얻는다. 그래서 정찰은 판정을 verdict 블록에 쓰고, FAIL이면 산출물을 남긴 뒤 비정상 종료한다(build 스크립트의 _abort와 같은 형태). 2026-08-05 첫 정찰에 이 판정이 있었다면 fnlttSinglAcntAll의 0/240에서 즉시 붉은 불이 켜졌을 것이다."
  },
  "acceptanceNote": "FAIL은 구조적으로 위반이 불가능한 것만이다 — availableFrom 부재·계약 1 위반·periodEnd 부재·날짜 형식·corp 형식·(corp,fiscalYear,availableFrom) 중복·데이터가 0인 사업연도. 나머지는 전부 WARN으로 시작한다: 이 프로젝트의 규칙은 '실측하지 않은 임계는 WARN으로 시작하고 실측 1회 후 FAIL로 승격'이고(PR-1.0 → PR-1.3이 그 경로였다), FN-1.0 시점에는 커버리지·ROE 이상치·연도별 매칭률의 실측이 없다. 정찰(scripts/probe-fundamentals-a3.py)은 표본 30여 법인이라 게이트의 기준선이 될 수 없다 — 표본으로 잰 값을 전수의 임계로 쓰면 임계가 실측처럼 보이는 추정이 된다.",

  "measuredAt": "2026-08-08",
  "measured": {
    "stageVersion": "A3.0",
    "manifestHash": "sha256:d37769470101be68",
    "inputDigest": "sha256:9df10969e0e62742",
    "sampleComplete": true,
    "corpsTargeted": 3801,
    "corpsWithData": 3003,
    "corpsWithDataByGroup": { "current": 2564, "delisted": 439 },
    "corpsTargetedByGroup": { "current": 2579, "delisted": 1222 },
    "currentListedCoverage": 0.9942,
    "delistedCoverage": 0.3592,
    "coverageRate": 0.9604,
    "yearCoverageMedian": 0.9644,
    "yearCoverageMin": 0.9307,
    "yearCoverageMax": 0.9829,
    "yearCoverageMaxAdjacentDrop": 0.0271,
    "periodEndParsedRate": 1.0,
    "restatedFiscalYears": 0,
    "roeComputable": 24436,
    "roeAbsOutlierCount": 331,
    "roeAbsOutlierRate": 0.01355,
    "negativeEquityCount": 161,
    "negativeEquityRate": 0.00651,
    "reportsFound": 24750,
    "recordCount": 24750
  },
  "measuredNote": "A3 첫 전수 수집(2026-08-05~08-08)의 실측이자 기준선이다. probed(표본 32법인)와 이름을 다르게 두는 이유는 test-policies.js가 measured의 존재로 WARN→FAIL 승격을 가르기 때문이다. 임계를 다시 손댈 때는 이 값이 비교 기준이다.",

  "promotion": {
    "version": "FN-1.4",
    "promotedAt": "2026-08-10",
    "procedure": "docs/FN-1.4-measured승격절차.md",
    "promoted": {
      "coverageRateMin": {
        "value": 0.86,
        "derivation": "§2.2 등급형 · floor(measured 0.9604 × (1 − 허용낙폭 0.10), 2)",
        "allowedDropBasis": "연도별 관측 최저가 2023년 0.9307이고 그것이 정상 변동의 하단이다. 0.86은 그보다 7%p 아래라 '어느 해보다도 나쁘다'를 넘어선다(§2.2 세 번째 근거).",
        "catches": "필수 5계정 중 하나가 일부 회사·연도에서 매칭에 실패하기 시작하는 점진적 열화. 전면 실패는 어떤 선이든 잡지만 점진적 열화는 선의 위치가 정한다."
      },
      "currentListedCoverageMin": {
        "value": 0.96,
        "derivation": "§2.2 등급형 · floor(measured 0.9942 × (1 − 허용낙폭 0.03), 2)",
        "allowedDropBasis": "현재 상장 법인은 사업보고서 제출 의무가 있어 결측이 구조적으로 없다. 미확보 15건(0.58%)이 관측치이고 0.96은 그 약 5배 아래다.",
        "catches": "현재 상장 법인의 수집 실패. 폐지 그룹의 구성 변화에 가려지지 않는다 — coverageRate 하나만으로는 폐지 비율이 흔들릴 때 현재 상장의 열화가 묻힌다."
      }
    },
    "notPromoted": {
      "minCorpsWithDataWarn": "절대 개수라 분모가 회사 구성이다(§2.3). A1b가 2026-08-10에 1,222→1,223으로 움직였다. WARN으로 남기고 비율 지표(currentListedCoverageMin)가 그 자리를 대신한다.",
      "delistedCoverage": "게이트로 만들지 않는다(§2.3). probed.delistedCoverageNote가 예고한 대로 분모에 SPAC과 2015 이전 폐지가 섞여 있다. measured에 기록만 한다.",
      "yearCoverageDropWarn": "§2.2의 도출식이 '높을수록 좋은' 지표만 상정한다. 이 셋은 낮을수록 좋아 식이 없고, 거울식을 지금 만들면 데이터를 본 뒤에 긋는 것이 되어 §0을 어긴다. WARN 유지.",
      "roeAbsOutlierRateWarn": "위와 같다. 추가로 measured 0.01355를 소수 2자리로 올리면 임계와 관측값이 붙는다.",
      "negativeEquityRateWarn": "위와 같다. measured 0.00651."
    },
    "unchanged": {
      "periodEndParsedRateMin": "§2.1 이분적 지표. measured 1.0이지만 0.99를 올리지 않는다 — 값이 이미 최대인데 선을 밑에 바짝 붙이면 잡는 것은 늘지 않고 정상 변동에만 걸린다.",
      "coverageRateMinWarn": "0.60은 measured에서 유도된 값이 아니라 절대 규칙 1의 '유보' 경계에서 온 하류 한계다. §2.2가 '하류 계약이 이미 그 수를 쓰고 있으면 건드리지 않는다'고 정했다."
    }
  },

  "probed": {
    "probedAt": "2026-08-05",
    "script": "scripts/probe-fundamentals-a3.py",
    "sample": { "corps": 32, "current": 24, "delisted": 8, "years": [2014, 2025] },
    "fnlttSinglAcnt": {
      "callsPerCorpYear": 1,
      "reportsFound": 240,
      "rceptNoIsDate": 240,
      "periodEndParsed": 240,
      "availableFromAfterPeriodEnd": 240,
      "availableFromNotAfterPeriodEnd": 0,
      "fullHitRate": 0.9625,
      "accountMissRate": { "currentAssets": 0.0333, "currentLiab": 0.0333, "liabilities": 0.0, "equity": 0.0, "revenue": 0.0208, "opProfit": 0.0042, "netIncome": 0.0042 },
      "fsDiv": { "CFS": 220, "OFS": 20 },
      "disclosureLagDays": { "min": 66, "p50": 89, "p95": 484, "max": 1958 },
      "estimatedCalls": 45612
    },
    "fnlttSinglAcntAll": {
      "callsPerCorpYear": 1.43,
      "reportsFound": 240,
      "periodEndParsed": 0,
      "fullHitRate": 0.9667,
      "estimatedCalls": 65092
    },
    "fiscalYear2014": "표본 32법인 전건 0보고서 — fiscalYearFrom 2015가 실측으로 확인됐다",
    "delistedCoverage": { "sampled": 8, "withAnyReport": 5, "rate": 0.625,
      "zeroReport": ["037630", "054440", "128910"] },
    "delistedCoverageNote": "표본 8건이라 게이트의 기준선이 될 수 없다. 다만 0건 중 128910이 '동부티에스블랙펄기업인수목적'(SPAC)이라는 사실은 기록해 둔다 — A1b는 A0.7 − A1a 차집합이라 A1a가 이름으로 제외한 SPAC이 그대로 들어온다. 즉 A3의 폐지 그룹 분모에는 애초에 채점 대상이 아닌 법인이 섞여 있고, corpsWithDataRateByGroup.delisted가 낮게 나오는 것을 곧바로 '수집 실패'로 읽으면 안 된다. 본 수집에서 이 값이 낮으면 분모부터 다시 정의한다(A2b에서 51.6%를 커버리지로 읽지 않기로 한 것과 같은 판단).",
    "note": "이 블록은 measured가 아니다. 표본 32법인의 정찰이며 게이트 임계의 기준선으로 쓰지 않는다 — 표본으로 잰 값을 전수의 임계로 쓰면 임계가 실측처럼 보이는 추정이 된다. test-policies.js가 measured의 존재 여부로 WARN→FAIL 승격을 가르므로 이름을 일부러 다르게 둔다. measured는 첫 전수 수집이 채운다.",
    "estimatedDays": "총 호출 약 45,600건 ÷ 일 예산 16,000건 ≈ 3일 (사업연도 2015~2025 11개 기준이며 정찰의 12개 연도 추정치보다 약간 작다. 폐지 법인의 조기 종료가 더 줄인다)"
  },

  "coverageNote": "coverageRate는 requiredForCoverage 5계정이 모두 잡힌 레코드의 비율이다. CLAUDE.md 절대 규칙 1의 '커버리지 60% 미만이면 등급 유보'와 같은 숫자를 쓰되 층이 다르다 — 저쪽은 종목별 지표 결측률이고 이쪽은 수집 단계의 계정 확보율이다. 같은 값으로 둔 것은 우연이 아니라, 수집 커버리지가 그 아래로 떨어지면 하류에서 어차피 전건 유보가 되므로 그 지점을 수집 단계에서 먼저 보이게 하려는 것이다.",

  "yearCoverageDropNote": "BF-1.1 §7 A3 계약 2('특정 연도만 급락하면 실패')의 구현이다. 전체 평균이 아니라 인접 연도 대비 낙폭을 본다 — 평균만 보면 한 해가 무너져도 나머지 열 해가 가린다. FN-1.0에서는 WARN이고, 첫 수집 실측 후 FAIL로 승격한다.",

  "output": {
    "dir": "data/backfill/fundamentals/a3",
    "partition": "fiscalYear",
    "format": "jsonl.gz",
    "gzipMtime": 0,
    "gzipCompressLevel": 9,
    "sortKey": ["fiscalYear", "corp", "availableFrom"],
    "fields": ["corp", "ticker", "fiscalYear", "availableFrom", "rceptNo", "fsDiv",
               "periodEnd", "currency", "sicCode",
               "currentAssets", "currentLiab", "liabilities", "equity",
               "revenue", "opProfit", "netIncome", "accountSource"],
    "outputNote": "gzip mtime을 0으로 고정한다(A2a와 동일한 이유 — 기본값은 현재 시각이라 내용이 같아도 매 실행 바이트가 달라지고 manifest가 재수집 판정 기능을 잃는다). 정렬 키에 availableFrom을 넣는 이유는 정정공시로 같은 (corp, fiscalYear)가 여러 번 나올 수 있어서다. ticker는 조인 키가 아니라 사람이 읽는 보조 필드다 — 조인은 반드시 corp로 한다(계약 4).",
    "stateDir": "data/backfill/fundamentals/_shards",
    "stateDirNote": "resume 상태와 중간 산출물. A2a의 샤드는 한 워크플로 실행 안에서 artifact로 넘겼지만 A3는 일 한도 때문에 실행이 여러 날에 걸치므로 artifact로는 이어받을 수 없다. 그래서 이 디렉터리는 Actions가 커밋한다. finalize가 성공하면 삭제한다 — 남겨두면 전체 데이터의 사본이 저장소에 영구히 쌓인다."
  }
}
