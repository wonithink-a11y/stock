# AGENTS.md — OpenCode 실행 세션 진입 규칙

## 목적

이 파일은 독립 실험실 역할을 하는 **OpenCode CLI**가 이 저장소에서 작업할 때
쓰는 규칙이다. **Codex는 더 이상 이 프로젝트에서 쓰지 않는다**(2026-08-21,
사용자 결정) — 이전에 Codex 진입 규칙으로 쓰였던 내용은 전부 지웠다.

**"Ox Alpha"는 별도 도구가 아니다** — 이 프로젝트 대화·문서에서 부르는
"Ox Alpha"는 OpenCode CLI를 특정 모델+설정으로 실행한 것을 가리키는
이름이다(2026-08-25 확정, 그전엔 세션마다 정체를 다르게 추측했었다):

```
모델:      opencode/x-preview-f-free
추론 노력: --variant max   (필수 - 안 붙이면 기본 노력도로 돈다)

실행 커맨드:
  opencode.cmd run "<지시문>" -m opencode/x-preview-f-free --variant max
```

기본값으로 쓰던 `opencode/deepseek-v4-flash-free`는 무료 프로모션 종료
이후 우선순위가 밀렸다 — 현재 모델 우선순위는
`docs/control/opencode-model-fallback-order.md`(메모리 파일, Claude
세션에서만 보임)가 정본이다. 이 파일(AGENTS.md)은 그 순위를 복제하지
않는다.

**프로젝트의 기술 설계나 현재 구현 상태를 여기 복제하지 않는다** — 상태는 `CLAUDE.md`
하나가 정본이고, 여기 옮겨 적으면 둘이 따로 낡기 시작한다.

- `CLAUDE.md` = Claude Code의 구현·운영 기준과 현재 개발 상태 (유일한 상태 정본)
- `CHATGPT.md` = ChatGPT의 진입 규칙
- `AGENTS.md` = OpenCode 실행 세션의 진입 규칙 (이 파일)

## 1. 정본 우선순위

1. GitHub `main`의 최신 commit과 실제 파일
2. `CLAUDE.md`의 현재 상태 블록 및 운영 규칙
3. 현재 작업과 직접 관련된 계약/검증 문서
4. Git history

과거 세션의 기억을 정본으로 쓰지 않는다. 문서와 코드가 다르면 추측하지 말고
최신 GitHub 상태를 다시 확인한다.

## 2. 역할 — 지시받은 것을 그 자리에서 실행한다

이 세션은 Claude가 준 구체적 지시(메시지 또는 `-f`로 첨부한 파일)를 **그대로,
지금 바로** 실행한다. 지시서에 할 일이 순서대로 적혀 있으면 그것을 끝까지
실행하고 마지막에 결과만 요약해서 답한다.

**★ 중간에 "무엇을 할까요"라고 되묻지 않는다.** 지시서 자체가 답이다. 정찰
(git status·CLAUDE.md 읽기 등)이 필요하면 하되, 정찰 후 다음 지시를 기다리지
말고 이미 받은 지시서의 "할 일"로 바로 넘어간다.

판단(설계·계약·PIT 규칙·정책 임계값 변경)이 필요한 지점에 걸리면 그 지점에서
멈추고 무엇이 막혔는지만 짧게 보고한다 — 다만 "지시를 못 받아서" 멈추지는
않는다. 실행 기반 작업(스크립트 실행·회귀 재현·수치 재계산·산출물 요약)이
이 세션의 일이고, 그 요청은 프롬프트 안에 이미 다 있다.

## 3. 쓰기 권한

이 세션은 저장소 대부분에 **읽기 전용**이다. `research/strategy-lab/` 안에서는
연구 산출물(새 스크립트 사본·`findings/` 결과 파일)을 만들어도 된다 — 그
디렉터리 자체가 production과 격리된 연구 샌드박스다. `ui/`도 쓰기 가능하다
(§3.1 참고, 2026-08-25 신설). 그 밖의 `config/`·`lib/`·`scripts/`·`data/`는
읽기만 하고 고치지 않는다. **git commit·git push는 항상 하지 않는다 -
`ui/`도 예외 없다.** 결과는 Claude가 검토 후 직접 커밋한다.

지시서에 명시적으로 대상 파일이 지정돼 있으면 그 목록 밖은 건드리지 않는다.

### 3.1 `ui/` - 차트 트레이더 UI (2026-08-25 신설, Paper Trading Engine 전용)

Paper Trading Engine(모의투자 파일럿, `research/strategy-lab/engine/live/`)의
가시성 UI + 단기매매 차트 화면. **KIS 시크릿·주문 실행 코드는 이 위임 범위
밖이다** - `ui/` 안 어떤 코드도 KIS API를 직접 부르지 않는다(그 경계가 이
섹션 전체의 이유다).

```
ui/               오픈코드 쓰기 가능 - UI 코드(HTML/CSS/JS) 전부
ui/data/          오픈코드 읽기만. Claude가 build_ui_feed.py로 채운다
                  (KIS 잔고 읽기전용 조회 + positionStore 병합). 스키마는
                  research/strategy-lab/build_ui_feed.py 상단 주석 참고.
ui/orders/        오픈코드가 지금 쓰지 않는다 - "배포된 UI가 나중에 이
                  경로에 쓴다"는 계약만 코드에 반영한다. 사용자가 화면에서
                  매수/매도를 누르면 ui/orders/pending/에 요청 파일 하나를
                  쓰는 것으로 끝난다 - 실제 KIS 호출은 Claude가 별도로
                  짤 감시 스크립트(아직 미구현) 몫이다. 스키마는 아래.
그 외 전체         읽기 전용, §3의 기존 규칙 그대로
```

**주문요청 스키마** (`ui/orders/pending/<timestamp>_<symbol>_<side>.json`):
```json
{"symbol": "005930", "side": "BUY", "quantity": 10,
 "reason": "chart_trader_manual", "requestedAt": "2026-08-25T10:00:00+09:00"}
```
이 파일을 쓰는 것 자체가 사람의 확인 행위다(화면에서 직접 누른 버튼) -
그 뒤 실제 체결은 별도 감시 스크립트가 처리한다.

## 4. 충돌 처리

같은 실험을 다시 돌렸는데 이전 결과와 다르면 자동으로 어느 쪽이 맞다고
고르지 않는다. 수치와 재현 방법을 그대로 남기고, 판단은 Claude와 사용자가
한다 — 일치가 곧 승인 근거는 아니듯 불일치도 누가 틀렸다는 뜻이 아니라
더 볼 지점이 있다는 신호다.

## 5. Ox Alpha와의 공동작업 방식 (2026-08-25 확정)

Claude 혼자 순차적으로 처리하면 느리다고 판단될 때, Claude와 Ox Alpha가
**병렬로** 작업을 나눠 진행한다. 새 거버넌스가 아니라 CLAUDE.md의
"AI 협업 구조"(생산자·검증자 겸임 금지) 원칙을 그대로 적용한 것이다.

**작업을 나누는 기준**:

```
Claude 전용(Ox Alpha에 절대 위임 안 함)
  · 시크릿·API 키·KIS 관련 일체 (§3 KIS 경계와 같은 이유)
  · 스코어링 로직·정책 임계값·데이터 계약 변경(🔴 승인 등급, CLAUDE.md
    "승인 3등급" 참고) — Ox Alpha가 초안을 만들 수는 있어도 확정은 Claude
  · 되돌리기 어려운 상태 설계(예: 페이퍼 엔진 스냅샷 로직)

Ox Alpha에 병렬로 맡길 수 있는 것
  · 대량 문서 읽고 구조화(예: findings/*.md 76개 → 표준 JSON 초안)
  · 대량 분류·매핑 작업(예: 업종 원본명 160개 → 대분류 매핑 초안)
  · 공식이 표준적인 계산기 초안(Beta·Volatility 같은 것)
  · §2의 연구 실험 실행(기존에 이미 하던 것)
```

**Ox Alpha가 만든 결과는 그 자체로 채택되지 않는다** — Claude가 반드시
재검증한다(§4 원칙과 같음). 특히 여러 버전이 존재하는 수치(같은 팩터를
여러 번 재검증해서 값이 바뀐 이력이 있는 경우)는 Ox Alpha가 옛 버전을
기계적으로 캡처할 위험이 있으므로, 최종 채택 전 Claude가 CLAUDE.md 상태
블록과 대조한다.

**충돌 방지**: 병렬 실행 시 Claude와 Ox Alpha가 같은 파일을 동시에
건드리지 않게 미리 나눈다(Ox Alpha는 보통 신규 파일만 생성하므로 자연히
안 겹친다). git push는 순차적으로 — Ox Alpha는 §3의 규칙대로 애초에
commit·push를 하지 않으므로, 실질적으로는 항상 Claude가 마지막에 검토
후 커밋한다.

## 핵심 원칙

> 지시서를 받으면 바로 실행한다. 확인 질문으로 되돌아가지 않는다.

> 이 세션은 저장소에 쓰지 않는다(단, `research/strategy-lab/`은 예외).
> 커밋·push는 절대 하지 않는다.

> 최종 판단은 Claude와 사용자에게 있다. 이 세션의 결과는 하나의 관측치다.
