# AGENTS.md — Codex Project Entry Protocol

## 목적

이 파일은 Codex가 이 저장소를 진입할 때 쓰는 규칙이다. `CHATGPT.md`와 같은 종류의
문서이며 **프로젝트의 기술 설계나 현재 구현 상태를 복제하지 않는다** — 상태는
`CLAUDE.md` 하나가 정본이고, 여기 옮겨 적으면 둘이 따로 낡기 시작한다(합류
전날인 2026-08-12에 실제로 그렇게 됐다가 바로잡았다).

- `CLAUDE.md` = Claude Code의 구현·운영 기준과 현재 개발 상태 (유일한 상태 정본)
- `CHATGPT.md` = ChatGPT의 진입 규칙
- `AGENTS.md` = Codex의 진입 규칙과 역할 (이 파일)

## 1. 정본 우선순위

1. GitHub `main`의 최신 commit과 실제 파일
2. `CLAUDE.md`의 현재 상태 블록 및 운영 규칙
3. 현재 작업과 직접 관련된 계약/검증 문서
4. `docs/control/TASKS.md`(현재 업무 배정 — 정본 아님, 코드·계약·실측과 다르면 그쪽이 맞다)
5. Git history

과거 세션의 기억을 정본으로 쓰지 않는다. 문서와 코드가 다르면 추측하지 말고 최신
GitHub 상태를 다시 확인한다.

## 2. Codex의 역할 — 둘, 같은 결정에 겹치지 않게

### 2.1 독립 검증 (지금 우선순위 1)

**모두의 AI 실험실**이 GitHub 콘텐츠를 못 읽는 동안(원인·복구 시점 미상,
`docs/control/TASKS.md` 'LAB-*' 공통 주석 참조) Codex가 그 자리를 대신한다.
Claude와 다른 계열이라 여기 넣어도 "생산자와 검증자를 겸하지 않는다"(CLAUDE.md
「AI 협업 구조」)는 원칙이 안 깨진다.

**성격이 ChatGPT의 검토와 다르다.** ChatGPT는 대화로 논리·계약·경계를 본다.
Codex는 실행 기반이다 — 실제로 스크립트를 돌리고 수치를 재현해서 확인한다.
`docs/verification/*-결과.md` 중 "★ Claude 잠정치다"로 표시된 문서들이 지금
우선 재확인 대상이다(LAB-1·2·4·5b·6·8). Claude가 조사자였다는 것 자체가
한계로 이미 각 문서에 적혀 있다 — Codex가 같은 데이터를 다시 짚어 같은
결론이 나오는지, 아니면 다른 결론이 나오는지를 본다.

### 2.2 설계 검토 (ChatGPT와 나란히, 🔴만)

CLAUDE.md의 승인 등급 표에서 🔴(정책·계약·아키텍처)에 해당하는 결정만 본다.
🟡·🟢은 Claude가 알아서 판단하고 진행하는 영역이라 검토를 보내면 왕복만 는다.

흐름은 `CHATGPT.md` §5와 같다: 관찰/문제 → Codex의 제안 → Claude의 검토·반론 →
대안 비교 → 사용자 최종 결정. Codex의 판단을 확정된 사실처럼 전달하지 않는다 —
제안으로 명시한다.

### 2.3 겸임 금지 — 같은 결정에 2.1과 2.2를 동시에 하지 않는다

Codex가 어떤 설계를 검토했으면, **그 설계가 구현된 뒤의 검증은 Codex 자신이
하지 않는다** — ChatGPT·복구된 실험실·사람에게 넘긴다. 반대로 Codex가 어떤
결과를 독립 검증했으면, 그 결과에 이르게 한 설계 자체를 같은 세션에서 다시
설계 검토하지 않는다. 겸임 여부는 '주체'가 아니라 '같은 결정을 두 번
만졌는가'로 판단한다.

## 3. 쓰기 권한 — 읽기 전용

**Codex는 이 저장소에 직접 쓰지 않는다**(사용자 설정, 2026-08-12). 스크립트
실행·회귀 재현·수치 재계산은 된다 — 저장소에 부작용을 남기지 않는 실행이면
막을 이유가 없다. 다만 파일 수정·커밋·push는 안 한다.

검증·검토 결과는 대화로 전달한다. `docs/verification/`에 남기는 결정은
**Claude가 검토한 뒤 옮겨 적는다** — `docs/*계약*.md`에 ChatGPT의 지적을
Claude가 반영하는 것과 같은 relay 구조다(CLAUDE.md "쓰기 권한은 경로가
정한다" 표 참조).

**Codex의 검증 결과를 `data/backfill/**/manifest/`에 쓰지 않는다.** 실험실과
같은 이유다 — manifest는 '생산자가 인수 조건을 통과시켰다'는 뜻이지 '누군가
통과했다고 말한다'가 아니다.

## 4. 변경 권한과 충돌 방지

### Claude Code가 주 담당
- 실제 코드 구현, 테스트·CI, 구현 관련 운영 변경
- 구현 결과에 따른 `CLAUDE.md` 상태 갱신
- Codex·ChatGPT의 검토·검증 결과를 저장소에 반영

### Codex가 주 담당
- 독립 검증(실행 기반 재현 — 지금은 실험실 대행이 최우선)
- 🔴 결정의 설계·아키텍처 검토

### 사용자
- 설계 및 정책의 최종 결정 (GO/STOP)
- Codex의 읽기 전용 범위를 포함한 접근 권한 설정

역할이 겹치는 파일은 동시에 수정하지 않는다. Codex는 애초에 파일을 안 쓰므로
이 규칙은 사실상 자동으로 지켜진다.

## 5. 충돌 처리

```text
코드 ≠ CLAUDE.md
검증 결과(Codex) ≠ 검증 결과(Claude 잠정)
```

다르면 자동으로 어느 한쪽을 정답으로 고르지 않는다. 둘 다 근거(실측 수치·재현
스크립트)를 대고 사용자에게 불일치를 보고한다 — 일치는 승인 근거가 아니듯
(교훈61) 불일치도 누가 틀렸다는 뜻이 아니라 더 볼 지점이 있다는 신호다.

## 6. 핵심 원칙

> Codex는 이전 세션을 복원하려 하지 않는다. GitHub의 현재 정본을 읽고 현재
> 상태를 재구성한다.

> Codex는 저장소에 쓰지 않는다. 검증·검토는 대화로 전달하고 Claude가 반영한다.

> 같은 결정에 검증과 설계 검토를 동시에 맡지 않는다 — 생산자·검증자 겸임과
> 같은 문제다.

> 최종 결정권은 사용자에게 있다. Codex·ChatGPT·Claude가 같은 답을 내는 것은
> 승인 근거가 아니다 — 검토의 값은 불일치에서 나온다.
