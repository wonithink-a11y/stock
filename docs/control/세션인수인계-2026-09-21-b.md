# 세션 인수인계 — 2026-09-21 (b) · A3e 완주와 회계 축 탐색 종결

이전: `세션인수인계-2026-09-21.md`. 이 세션의 후속. 주문·정책·config·lib 변경 없음(VM 유닛 pull 명령만 수정·사용자가 설치).

## 0. 결론
| 항목 | 결과 |
|---|---|
| A3e 수집·매핑 | 24,749행 완료, 매핑 a3e-map-1.1 동결(감사 docs/control/A3e-계정매핑감사-2026-09-21.md) |
| EV 가치 3셀 × PBR 잔차화 | 전부 REJECT (11번) |
| 고부채 쏠림 통제 4셀 | 전부 REJECT · 기전 NOT SUPPORTED(쏠림을 없앴는데 하락장 격차 그대로) (12번) |
| 품질 3축(GP/A·발생액·자산성장) | 1단계 전부 REJECT, 2단계 미실행 (13번) |
| VM 유닛 | 09-21 09:05 RV20·크립토가 `git pull --ff-only` 실패로 미실행 → 5개 유닛을 `pull --ff-only origin main` 으로 수정, 사용자가 VM 설치(b61f5882) |
| 동시호가 시험 | 09-21 매수 PASS(체결가=공식 종가, 0.0bp) |

**A3e 로 연 회계 축은 PBR 위에 더해지는 정보가 없다 — 이 라인은 종결.** 하지 않는다: 방향 뒤집기·임계·가중치·분위수 변경·축 추가(각 사전등록 §6).

## 1. 내일(09-22) 확인 — 시한순
1. 07:31 이후: 미국 스냅샷 첫 자동 실행 로그·`_status.json` (`ssh stock-new`).
2. 08:45 매도 → 09:05 대조: 동시호가 시험 통과 여부(`~/collector-venv/logs/auction-probe.log`). 통과해야 O2b·O3u 모의 실행으로 간다, 실패하면 중단.
3. **09:05 이후: RV20·크립토 유닛이 정상 실행됐는지**(`journalctl -u rv20-futures-paper-order.service`, `systemctl --failed`). 크립토 슬리브는 09-21 청산 판정이 빠져 AVAX·INJ 보유가 ~48시간이 된다 — 사후 해석 때 편차로 기록. 09-18 RV20 도 KIS 모의 서버 ReadTimeout 으로 실패했었다(별건).

## 2. 새로 알게 된 것
- **접두어**: A3e 표준코드는 2015~2018 `ifrs_`, 이후 `ifrs-full_`. 손익계산서는 `CIS`/`IS` 로 갈린다. 차입금은 `dart_` 확장 코드라 이름 매칭(리스 제외).
- **`총액`·`유동성 대체 부분` 차입금 행은 부호가 일정하지 않다**(대체 행 697개 중 97개 음수) → `debt_ambiguous` 로 주 분석 제외(4.4%).
- **감가상각 커버리지 19%**(2019 이후 14%) → EV/EBITDA 전면 불가.
- **12번 부수 관찰(미검증, 새 가설)**: 고부채 1/3 을 빼면 PBR 단독의 TRAIN Sharpe 가 절반(0.35→0.17) — PBR 성과 일부가 고부채 종목에서 왔을 수 있다.
- 13번: 저발생액·저자산성장 종목은 ROE 가 낮은 쪽으로 쏠린다(상관 −0.33·−0.43). GP/A 는 ROE·op_margin 과 0.5.
- 이 세션 코드 재사용 구조: `11_ev_value_family_oos.py`(프레임·평가·게이트·판정·가족 난수 바닥선) → `12_`·`13_` 이 importlib 으로 불러 쓴다(모듈명이 숫자로 시작해 일반 import 불가).

## 3. 다음 세션 착수 (인수인계 §6 그대로, 우선순위 순)
① **KIS 목표가 수집 설계**(대량 수집이라 착수 전 확인) ② ~~유상증자 회피 필터 사전등록~~ **종결** — 사전 노출 점검 결과 PBR 슬리브 슬롯 3,402개 중 4개(0.1%)만 걸려 시험 대상이 없다(findings/overissue-filter-exposure-check-2026-09.md, 건수만 셌고 수익률 미확인). 걸림이 있는 곳은 lowmom60_v1 4.6%(검정 불가, 위생 규칙 🔴 판단 사안). E1/E2 권리락 수정주가 처리(V3)는 여전히 미확정 ③ **애널리스트 추정치(EPS·영업이익 리비전) 소스 조사**. 회계가 아닌 정보 쪽이 남은 방향이다.

## 4. 커밋 (전부 push)
b61f5882 유닛 pull 수정 · af4d1f0c A3e 매핑 감사 · 6b9c2b52/40990a48/105d38b5/4c95e89c EV 가치(사전등록·정오표·구현·결과) · 4ec0c469/00cde550 고부채 통제(사전등록·구현) · 1dbf541a/5bd2c2c7 품질(사전등록·구현) + 각 결과 문서와 CLAUDE.md 갱신.
