# 세션 인수인계 — 2026-09-27 · 스캘핑·초단타 후보 6개 대조와 실험 (전부 REJECT)

사용자 지시: "실험실에 없으면 실험 진행" → 이어서 "자는 동안 권고대로 다양하게 테스트하고 인수인계 작성".
정책·config/policies·criteria·워크플로·VM 변경 없음. **실주문·수집 없음**(보유 데이터만). 커밋은 전부 push.
사용자 로컬 변경 `scripts/decode-gemini.py`(M)는 건드리지 않았다.

## 0. 다음 세션이 먼저 할 것

1. 이 라인은 **닫혔다** — 새 스캘핑 후보가 오면 아래 §1 표부터 대조한다(대부분 이미 쟀다).
2. 기존 일정 그대로: 09-28(월) 히트맵 '장중' 첫 실물 · 국내 모의 1주 시험 · 10-01~05 월간 점검(CLAUDE.md 절차).
3. 호가 데이터(⑥) 수집은 **권고: 안 함**(§3). 사용자가 원하면 그때 🔴급 확인 후 사전등록부터.

## 1. 외부 제안 6개 — 대조 결과

| 제안 | 결과 | 근거 |
|---|---|---|
| ① 크립토 VWAP ±2σ 이탈 후 복귀 | **새로 실행 → REJECT** | 하단 회귀 롱 16셀 전부 \|t\| ≤ 1.07 — 신호 없음 |
| ② 국내 ORB 돌파 | **새로 실행(즉시 매수) → REJECT** | 롱 gross TRAIN −4~−17 · VALID −15~−87 · TEST −32~−128bp |
| ③ 거래량 급증 모멘텀 | 국내: 기존 3B(고유동성 엣지 0) · ORB 결합(K8) REJECT · **크립토 1분봉 새로 실행 → REJECT** | TRAIN t 5.30 → BTC OOS +0.01bp |
| ④ VWAP 과매도 반등(KR) | 기존 `vwap-deviation-event-study` REJECT(최대 14bp < 33bp) — 재실행 안 함 | 크립토 쪽은 ①의 R(재진입 확인) 셀로 같이 쟀다 |
| ⑤ 돌파 후 눌림목 | 기존 K6 ORB 재테스트 REJECT(net −61bp) · K2f INFORMATION(net 음) · Stine B REJECT | 이번 K7(즉시)과 비교: **둘 다 롱 손실** |
| ⑥ 호가 잔량 불균형 | 국내 데이터 0 · 크립토 테이커 흐름 FAIL(Step 35) — 수집 가능성만 조사(§3) | |

EMA 5분 스캘핑(참고자료)은 EMA 9/21 교차 FAIL(`github-strategy-reproduction-2026-08`)·단일 지표 폐기 원칙으로 열지 않았다.

## 2. 이번에 실행한 것 (사전등록 → 결과 전 커밋 → 실행)

| 커밋 | 내용 |
|---|---|
| 70d64249 | 사전등록 `scalping-candidates-preregistration-2026-09.md` + `futures/scalping_candidates.py`(selftest 9/9) |
| 852180bd | 사전등록 `scalping-volsurge-preregistration-2026-09.md` + `futures/scalping_volsurge.py`(selftest 4/4) |
| (이 커밋) | 결과 두 건(md·json) · CLAUDE.md '안 한다' 한 항목 · 이 문서 |

### 2-1. V — 크립토 1분봉 VWAP 밴드 (32셀, BTC TRAIN 2018~21 → BTC 2022~·ETH)
- UTC 자정(=KST 09:00) 앵커 VWAP·거래량가중 σ, k 2·3, 진입 T(이탈)/R(재진입), 청산 15/60/240분/VWAP 복귀, 롱·숏.
- 바닥선 3.16(신호 순환 이동 200회). 최대 셀 = 상단 2σ 이탈 → 15분 **추세**(t −2.96, 미달). BTC OOS +0.25bp · ETH +3.9bp(t 7.1) < 비용 10bp.
- **핵심: 제안된 하단 회귀는 BTC 1분봉에 없다**(KR 은 1~2bp 회귀라도 있었다).

### 2-2. K7/K8 — 국내 ORB 돌파 즉시 매수 (4셀, structure-patterns K 인프라)
- OR = 첫 3봉, 11:00 까지 종가 > ORH×1.003 첫 봉 → 다음 봉 진입, 30분/15:20 청산, 거래량 3배 조건 유무.
- 바닥선 2.86(자체 2.49 보다 엄한 K 가족값). TRAIN 부호 음, OOS 에서 더 음. JSON 의 숏 방향 net 양수(+56·+75bp)는
  **공매도 불가·바닥선 미달·사후 가설**이라 기록 전용 — 채택·재론하지 않는다.

### 2-3. 크립토 1분봉 거래량 급증 (12셀)
- 급증 = 직전 60분 중앙값의 5·10배, 양봉 롱/음봉 숏, 5·15·60분.
- TRAIN 10배 양봉 5분 t 5.30(바닥선 2.81 통과) → **BTC 2022~ +0.01bp, 이동 대조 이하** → REJECT. ETH +1.3bp.

## 3. 호가 데이터(⑥) 확보 가능성 — 조사만 (다운로드 없음, 목록 조회)

`data.binance.vision`(공개, 키 불필요):
- 선물 `bookDepth` BTCUSDT **2023-01-01 ~ 2026-09-25**, 하루 ~0.56MB(전체 ~0.8GB). 내용은 가격 대비 ±1~5% 구간 묶음 깊이 스냅샷 —
  **최우선 호가·대기열이 아니라** hftbacktest 식 체결 모델은 못 만든다. 불균형 지표는 만들 수 있다.
- 선물 `bookTicker`(최우선 호가) **2023-05-16 ~ 2024-03-30 만** — 10개월, 이후 중단.
- 체결 틱: 현물 `trades` 2017-08~, 선물 `aggTrades` 2019-12~(하루 ~15MB).
- **권고: 수집 안 함.** 이유 — (1) 테이커 매수비율 불균형이 이미 FAIL, (2) 크립토 1분봉 신호는 이번까지 세 번(워뇨띠 R1·V·급증) 2018~21 → 2022~ 에서
  소멸, (3) gross 가 비용 10bp 의 1/3 이하라 메이커 체결 모델 없이는 경제성 판정이 안 되는데 그 모델에 필요한 대기열 데이터가 없다.
  국내 호가는 KIS 실시간 수집이 필요(새 VM 유닛 = 🔴급 확인).

## 4. 함정·교훈

- **크립토 1분봉 2018~21 신호는 OOS 에서 반복적으로 사라진다**(R1 t 2.9 → −0.5bp · 급증 t 5.3 → 0.0bp). 다음 크립토 분봉 가설은
  TRAIN 을 2022~ 로 두는 설계를 먼저 검토(단 이번 가설들의 기간 재절단 재시험은 금지 — 새 가설에만).
- ORB 류의 '초반 강세 추종'은 국내에서 일관되게 반대(intraday-timeofday IC −0.059 · K6 · K7 · K8). 숏으로 뒤집고 싶어지는 자리지만
  공매도 불가 + 사후 가설.
- `scalping_candidates.py` 는 `structure_phase4` 의 KR 로더(5분 격자 ~600MB)를 import 한다 — 전체 실행 2.5분, 메모리 여유 있는 PC 에서.
