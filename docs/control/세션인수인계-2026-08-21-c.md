# 세션 인수인계 — 2026-08-21-c — 투자대가 방법론 타당성 조사 → turnover20 tercile 테스트베드 결함 확정 → 오염된 2건 재검토로 PBR·LOWMOM60+수급 판정 뒤집힘

이 문서는 정본이 아니다 — 정본은 GitHub `main`의 실제 파일이다. `세션인수인계-
2026-08-21-b.md`("다음 방향은 A5-3 밸류에이션(peg/pbr)을 Strategy Lab에
결합" — §3)의 다음 세션이다. GPT가 제안한 "검증된 투자대가 방법론(버핏·린치·
그린블라트·오닐)을 한국시장 데이터로 정량화 → precheck → 후보 선별" 방향을
그대로 착수했으나, 세 번째 팩터(ROE/Buffett Quality)까지 돌린 뒤 **팩터
자체가 아니라 이 백테스트 인프라의 유동성 tercile 버킷이 수익을 만들고
있었다는 걸 플라시보 테스트로 확정**했다. 사용자 질문("왜 이렇게까지 유효한
검증이 없었나")을 계기로 한 걸음 더 파서, **`turnover20` rolling tercile을
"유동성 통제변수"로 쓰는 이 프로젝트의 관행 자체가 중립적이지 않고 그 자체로
강한 예측변수였음을 확정**했다(§"추가 확인" 참고). 이어서 사용자 질문("실험실
테스트를 다 다시해야 하나")에 정확한 영향 범위를 코드로 확인한 뒤, 그 오염된
방식을 실제로 쓴 2건(ChatGPT 저모멘텀+수급 A/B/C 기각, PBR 대형주 반전)을
중립성 검증된 절대임계값으로 재검토했더니 **둘 다 판정이 뒤집혔다** — 팩터
알파가 진짜 있었는데 오염된 통제변수 때문에 죽어 보였다(§"추가 확인 (같은
세션 재후속)"). **결과적으로 이 세션은 두 개의 새로운 유력 후보(PBR/A5-3
밸류에이션, LOWMOM60+기관수급)를 다시 열어놓은 채로 끝난다.**

## 추가 확인 (같은 세션 후속) — 테스트베드 설계 결함 확정, 이 라인 완전 종료

§4 메타패턴 확정 이후 사용자가 "A안(T3 정밀 재검토)"을 승인해 decile/IC
분석을 실제 롱 전략(하위 ROE 분위 배제)으로 구성해 백테스트했다. **여기서
ROE 신호 자체는 진짜였다** — decile 분석(§4-1)의 IC(t=8.06)와 정확히
일치하게, 저ROE 분위를 배제할수록 CAGR이 단조적으로 개선됐다(T3 baseline
-5.77% → 상위 4분위만 -1.68%, 최대 +4.1%p 개선). 하지만 **모든 변형이
여전히 마이너스**였고, 이게 사용자의 결정적 질문("이렇게까지 유효한 검증이
없는 이유가 뭐일까")으로 이어졌다.

기존 리포트(`2026-08-15-trend-breakout-v1-benchmark-analysis/universe_ew_
benchmark.json`)에 이미 있던 **전체 유니버스 동일가중 매수-보유 벤치마크
(2016~2026 CAGR +4.78%)**와 T3 baseline(-5.77%)의 격차(10.5%p)가 ROE 효과
(최대 +4.1%p)보다 훨씬 커서, "테스트베드 설계 자체가 잘못됐을 가능성"을
`testbed_mechanics_diagnostic.py`로 직접 검증했다 — tercile 제한을 완전히
빼고 전체 유니버스를 월별 리밸런싱만 했을 때 벤치마크와 얼마나 다른지,
비용을 0bps/30bps 둘 다 재서 "리밸런싱 메커니즘"과 "비용 가정"을 분리했다.

**결과 — 결정적**:

| 시나리오 | CAGR |
|---|---|
| 전체 유니버스, 월별 리밸런싱, 비용 0bps | +6.53% |
| 전체 유니버스, 월별 리밸런싱, 비용 30bps | **+2.94%** |
| 매수-보유 벤치마크(리밸런싱 없음, 재계산 아님) | +4.78% |
| T3(고유동성 tercile), 월별 리밸런싱, 30bps | **-5.77%** |
| T1(저유동성 tercile), 월별 리밸런싱, 30bps | **+11.97%** |

전체 유니버스를 tercile로 안 자르면 월별 리밸런싱+30bps 비용을 반영해도
+2.94%로 정상 범위다(비용 드래그 30bps×12≈연 3.6%로 설명되는 격차, 매수-
보유보다 살짝 낮은 정도). **리밸런싱 메커니즘도 비용 가정도 원인이 아니다.**
문제는 정확히 **turnover20(최근 20일 거래대금) 기준으로 tercile을 나누는
것 그 자체**다.

### 결론 — "왜 이렇게까지 유효한 검증이 없었나"에 대한 답

이 프로젝트가 시도한 **5DC·TREND-BREAKOUT-v1·LOWMOM60·REV20·PBR·PEG·ROE
7개 전략 전부가 "유동성과 무관하게 살아남는가"를 검증할 때 정확히 같은
turnover20 rolling tercile 방식을 유동성 통제변수로 썼다**(대부분
`lowmom60_robustness.py` 계열 코드 재사용). 그런데 이 tercile은 중립적인
통제변수가 아니라 그 자체로 강력하고 방향성 있는 예측변수였다 — 고유동성
(T3)은 거래량 급증 직후를 쫓아가 사는 셈이라 되돌림에 물리고, 저유동성
(T1)은 반대로 강하게 우상향한다(원인은 미확정 — 급증 후 되돌림, 저유동성
종목의 가격 데이터 특성, 혹은 둘 다일 수 있다). 서로 다른 경제적 근거를
가진 7개 가설이 하나같이 "저유동성에서만 플러스"로 수렴한 건 팩터 각각의
문제가 아니라, **공유된 "통제변수"가 실은 팩터보다 훨씬 강한 숨은 신호였기
때문**이다. 지금까지의 모든 "팩터 vs 유동성tercile" 대결은 애초에 공정한
대결이 아니었다.

### 이게 의미하는 것 — 앞으로 무엇을 바꿔야 하는가

- **turnover20 rolling tercile을 "유동성 통제"로 쓰는 관행 자체를 재검토해야
  한다.** 상대적 tercile 대신 절대 거래대금 임계값, 또는 시가총액 기반
  분류가 더 중립적일 수 있다(단, 이것도 검증 필요 — 절대 임계값 자체가
  같은 종류의 숨은 신호를 가질 가능성을 배제 못 한다).
- **오늘 이전에 이 tercile 방식으로 "채택 불가"라고 결론 낸 모든 판정
  (LOWMOM60·REV20·5DC 정밀 스윕·TREND-BREAKOUT)이 재검토 대상일 수 있다** —
  팩터가 진짜 나빴는지, 이 오염된 통제변수 때문에 나빠 보였는지 아직
  모른다. 이번 세션은 이걸 재검토하지 않았다(범위 밖).
- 전체 유니버스·월별 리밸런싱·30bps 비용 자체는 건전하다(+2.94%, 매수-보유
  대비 합리적) — 문제는 스코어링/랭킹이 아니라 "무엇을 유니버스로 삼는가"
  단계에 있었다.

신규 스크립트: `t3_factor_decile_check.py` · `t3_roe_quality_strategy_backtest.py`
· `testbed_mechanics_diagnostic.py` (전부 로컬 미커밋, 관례대로).

## 추가 확인 (같은 세션 재후속) — 오염된 2건 재검토, 둘 다 판정 뒤집힘

사용자가 "지금까지 한 실험실 테스트를 다 다시해야하는가"를 물어 정확히
어떤 결론이 영향받는지 코드를 직접 확인했다 — **전부가 아니라 2건뿐**이었다
(`turnover20` 상대 tercile을 실제로 쓴 결론만): ①ChatGPT 저모멘텀+수급
후보 A/B/C 기각(`lowmom60_institutional_eligible_precheck.py`) ②PBR "대형주
반전"(`a5_valuation_factor_precheck.py`, 2026-08-21-b §추가확인). LOWMOM60·
REV20의 **원래** robustness 검증(`lowmom60_robustness.py`·
`rev20_combined_robustness.py`)은 절대 거래대금 임계값을 쓰지 상대 tercile이
아니라 무관함(코드 직접 확인). 5DC·TREND-BREAKOUT-v1은 turnover/tercile을
아예 안 써 완전히 무관.

사용자 승인("go") 후 이 2건을 재검토했다:

**1단계 — 대체 필터 중립성 검증**(`absolute_turnover_filter_validation.py`):
LOWMOM60/REV20 원래 robustness가 쓰던 **절대 거래대금 임계값**(turnover20
>= 1억원)이 오늘 확정한 상대 tercile과 같은 숨은 편향이 있는지 먼저 확인 —
전체 유니버스 무필터 baseline(+2.94%, testbed_mechanics_diagnostic.py) 대비
고유동성(≥1억원) 버킷 EW 무작위 갭이 -0.68%p에 그쳐 **중립으로 검증됨**
(참고: 저유동성(<1억원) 버킷은 절대임계값으로 나눠도 여전히 강한 프리미엄
(+13.05%, 무작위도 +11.27%)을 보인다 — 이건 tercile 특유의 문제가 아니라
이 데이터셋의 진짜 저유동성 종목 자체가 가진 별개의 미해결 현상이다).

**2단계 — 오염된 2건 재실행**(v2, 유동성 필터만 절대임계값으로 교체, 나머지
로직 완전 동일):

| 결론 | v1(오염된 tercile) | v2(절대임계값, 중립 검증됨) |
|---|---|---|
| LOWMOM60+수급, 대형주/고유동성(`lowmom60_institutional_eligible_precheck_v2_absolute.py`) | -11.8% (기각) | **+13.90%** |
| PBR, 대형주/고유동성(`a5_valuation_factor_precheck_v2_absolute.py`) | -1.48% (반전, 기각) | **+7.06%** |
| PBR, 저유동성 대조군 | — | +7.48%(고유동성과 거의 같음 — 유동성 무관 알파) |

**둘 다 판정이 뒤집혔다.** 상대 tercile 방식이 낸 "채택 불가"는 팩터가
진짜 죽은 게 아니라 오염된 통제변수 때문에 죽어 보인 오판이었다.

### 결론 — 다음 세션에서 재개할 두 후보

- **PBR/A5-3 밸류에이션**: 2026-08-21-b가 "3번째 유동성 함정"으로 완전히
  닫은 라인이지만, 절대임계값으로 다시 재면 유동성과 무관하게 +7~8%대로
  견고하다. **재개 가치 있음** — 단, top30·월별 리밸런싱이라는 좁은 설정
  에서만 봤고(오늘 §추가확인의 T3 decile 분석과 같은 한계), 실제 Strategy
  Lab 정책으로 만들려면 decile/IC 정밀검증과 out-of-sample 확인이 먼저다.
- **LOWMOM60+기관수급(ChatGPT A/B/C)**: 대형주 구간에서도 +13.9%로 강함.
  **재개 가치 있음** — A/B/C 각각의 구체 설계(수급 필터 조합 방식)는 아직
  구현 안 됨, 이제 막힌 이유가 사라졌으니 실제 구현 여부는 사용자 판단.

**이 라인은 여기서 멈춘다**(다음 세션이 실제 구현 착수 여부를 판단). 신규
스크립트(전부 로컬 미커밋): `absolute_turnover_filter_validation.py` ·
`lowmom60_institutional_eligible_precheck_v2_absolute.py` ·
`a5_valuation_factor_precheck_v2_absolute.py`.

---

## 새 세션은 여기부터

1. **결론: PBR·PEG·ROE 세 팩터(가치·성장·품질, 서로 다른 경제적 근거) 전부가
   "팩터 알파"가 아니라 "T1(저유동성 tercile) 버킷에 들어간 것 자체"였다.**
   무작위로 T1에서 30종목을 뽑아도(20회 평균) CAGR +10.26%가 나왔는데, 이건
   PEG로 고른 T1(+4.08%)이나 ROE로 고른 T1(+8.68%)보다 오히려 높다 — 팩터
   기반 선별이 무작위 선별보다 못했다(§3 참고).
2. **이 유동성 프리미엄은 이미 죽은 것으로 확인된 바 있다.** 2026-08-18
   REV20 검증(`research/strategy-lab/reports/2026-08-18-strategy-candidates/
   README.md` §11)이 비용·유동성·가격 필터를 동시에(현실적으로) 걸자 같은
   종류의 저유동성 알파가 완전히 손실로 뒤집힌다는 걸 이미 보였다. 오늘의
   메타패턴 확정은 이걸 "REV20만의 특이 현상"에서 "이 프로젝트의 모든 가격
   기반·팩터 기반 전략 탐색이 반복적으로 부딪히는 구조적 벽"으로 일반화했다.
3. **CAN SLIM(오닐, 4번째 방법론)은 시도하지 않았다** — 같은 벽에 부딪힐
   가능성이 매우 높다고 판단해 사용자 승인 하에 이 라인 전체를 종료했다
   (§2 타당성 조사에서 CAN SLIM은 C(분기 EPS 성장) 원천 데이터 자체가 없고
   L(상대강도) 코드도 없어 어차피 절반만 구현 가능하다는 게 이미 확인된
   상태였다).
4. **(A)를 실제로 진행해 테스트베드 설계 결함까지 확정했다**(§"추가 확인"
   참고) — T3 안에서 ROE 하위분위를 배제하면 CAGR이 단조적으로 개선되지만
   (최대 +4.1%p) 전부 여전히 마이너스였고, 그 이유가 "turnover20 rolling
   tercile을 유동성 통제변수로 쓰는 방식 자체가 중립적이지 않고 그 자체로
   강한 예측변수"였기 때문임을 `testbed_mechanics_diagnostic.py`로 확정
   했다. tercile 없이 전체 유니버스를 월별 리밸런싱만 하면 CAGR +2.94%로
   정상 범위(매수-보유 벤치마크 +4.78% 대비 합리적 비용 드래그) — 리밸런싱
   메커니즘도 비용 가정도 원인이 아니었다.
5. **사용자 질문("실험실 테스트 다 다시해야 하나")에 코드를 직접 확인해
   답했다** — turnover20 상대 tercile을 실제로 쓴 결론은 딱 2건뿐이었다
   (ChatGPT 저모멘텀+수급 A/B/C 기각, PBR 대형주 반전). LOWMOM60·REV20의
   원래 robustness는 절대 거래대금 임계값을 써서 무관, 5DC·TREND-BREAKOUT
   도 turnover/tercile 자체를 안 써서 무관(§"추가 확인 (같은 세션 재후속)"
   참고).
6. **그 2건을 절대임계값(turnover20>=1억원, 중립성 검증됨)으로 재검토하니
   둘 다 판정이 뒤집혔다** — LOWMOM60+수급 대형주 -11.8%→**+13.90%**,
   PBR 대형주 -1.48%→**+7.06%**(유동성 무관하게 견고). 상세는 §"추가 확인
   (같은 세션 재후속)". **다음 세션이 고를 것: 이 두 후보(PBR/A5-3 밸류에이션,
   LOWMOM60+기관수급)를 실제 Strategy Lab 정책으로 만들지 여부.** top30·
   월별 리밸런싱이라는 좁은 설정에서만 봤다는 한계는 여전히 있다 —
   decile/IC 정밀검증과 out-of-sample 확인이 실제 착수 전에 필요할 수
   있다. 과거 LOWMOM60·REV20 자체의 원래 판정(절대임계값 기반이라 이번
   결함과 무관)까지 재검토할지는 별개 판단, 이번 세션 범위 밖.
7. `git log --oneline -20`·`git status --short`로 이 문서 이후 변경이 있었는지
   먼저 확인한다.

---

## 1. GPT 제안 — 투자대가 방법론 타당성 조사부터 시작

사용자가 GPT의 제안("버핏/린치/그린블라트/오닐 방법론을 한국시장 데이터로
정량화")을 전달, "타당성 조사후 실행하자"로 착수 승인. 토큰 절감을 위해
데이터 필드 매핑 조사는 Explore 서브에이전트 2개(Quality/Value 계열,
Technical/모멘텀 계열)에 병렬 위임.

### 타당성 조사 결과 요약

| 방법론 | 상태 | 핵심 이유 |
|---|---|---|
| Lynch GARP/PEG | ✅ 즉시 가능 | peg·epsGrowthRate·debtRatio 전부 `available:true`(2026-08-21 A5-3 D4), 패널 생성 스크립트 재사용 가능 |
| Buffett Quality | ✅ 즉시 가능 (FCF 없는 축소판) | roe·roeConsistency(min 5y)·debtRatio·operatingMarginTrend 전부 available. FCF는 A3가 현금흐름표를 안 모아 미구현 |
| CAN SLIM(O'Neil) | 🟡 절반만 가능 | S(거래량급증)·I(기관수급)·M(시장국면 proxy)·N(52주신고가)은 기존 코드 재사용 가능. **C(분기 EPS 성장)는 DART가 연간만 수집해 구조적으로 불가능**, **L(상대강도 지수화)은 코드 자체가 없음** |
| Greenblatt Magic Formula | ❌ 사실상 불가 | ROIC·EV/EBITDA·Earnings Yield 전부 미구현 — A3 스키마에 현금흐름표·EV 계산 원재료가 없음 |

사용자 승인 순서: **Lynch → Buffett → CAN SLIM(축약판)**, Greenblatt는 보류.

---

## 2. Lynch GARP/PEG precheck

`scripts/build-a5-valuation-panel.js`(2026-08-21-b가 만든 pbr precheck 패널
빌더)를 확장 — `epsGrowthRate`·`debtRatio` 필드 추가(하위 호환, pbr precheck는
기존 필드만 읽음). mergerSpinoff 이력 종목 제외는 그대로 유지(D4 우회식이
같은 상류를 씀).

`research/strategy-lab/lynch_garp_factor_precheck.py` 신설 — peg =
per/epsGrowthRate(epsGrowthRate<=0이면 PEG 정의 불가로 제외, `lib/
scoringEngine.js:162-177` 운영 정의와 동일 조건). **구현 중 버그**: `df.set_
index([...])[["peg","debtRatio"]]`로 DataFrame을 만들면 `.get((ticker,date))`가
인덱스가 아니라 컬럼명을 찾아 전부 `None`이 되는 걸 발견(pbr precheck는
Series라 문제없었다) — `to_dict("index")`로 수정.

**결과** (2016-01~2026-08, 월별 리밸런싱, top 30, 30bps 왕복비용):

| 시나리오 | CAGR |
|---|---|
| 저PEG 매수, 필터 없음 | +2.77% |
| 고PEG 대조군 | -3.86% |
| 저PEG + 유동성 상위 tercile(대형주) | **-2.02%** |
| 저PEG + 유동성 하위 tercile(소형주) | +4.08% |
| 저PEG + 부채비율 중앙값 이하 품질필터 | +2.63%(필터 없음과 거의 동일) |
| 저PEG + 품질필터 + 대형주 | -0.04%(거의 0) |

PBR·REV20·LOWMOM60+수급에 이어 **네 번째로 같은 유동성 함정 재현**. 부채비율
품질 필터를 얹어도 대형주 구간에서 살아나지 않았다.

---

## 3. Buffett Quality precheck — 메타패턴 발견

가격(밸류에이션) 축을 아예 안 보는 순수 품질 가설이라 다른 팩터와 경제적
근거가 완전히 다르다는 점에서 별도로 의미 있는 테스트였다.

`scripts/build-a5-quality-panel.js` 신설 — A3(fundamentals)만으로 나오는 축
(roe·roeConsistency=min(roeHistory5y)·debtRatio·operatingMarginTrend)이라
A3c/corporateActions(D4 파이프라인)를 안 거친다 — mergerSpinoff 제외 불필요,
전체 유니버스(2578종목) 사용.

`research/strategy-lab/buffett_quality_factor_precheck.py` 신설.

**결과**:

| 시나리오 | CAGR |
|---|---|
| 고ROE 매수, 필터 없음 | -0.64% |
| 저ROE 대조군 | -1.45%(고ROE와 거의 구별 안 됨) |
| 고ROE + 대형주(T3) | **-7.66%** |
| 고ROE + 소형주(T1) | **+8.68%** |
| 고ROE + eligibility(5년무적자·부채비율≤200%) | -1.42%(개선 없음) |
| 고ROE + eligibility + 대형주 | -4.39% |

고ROE와 저ROE가 거의 구별력이 없다(둘 다 마이너스, 차이 0.8%p) — ROE 자체의
설명력이 PBR/PEG보다도 약하고, 오직 소형주 구간에서만 강한 플러스가 나왔다.
**세 번째로 같은 패턴**(PBR·PEG·ROE 전부 "소형주에서만 플러스").

---

## 4. 메타패턴 확정 — 플라시보 테스트

사용자 승인 후 "이게 팩터 알파인지, 유동성 버킷 자체의 프리미엄인지"를
가르는 테스트를 설계·실행.

`research/strategy-lab/meta_pattern_liquidity_check.py` 신설 — 팩터 순위를
아예 안 쓰고 같은 유니버스(2578종목)·기간·turnover20 tercile 정의·30bps
비용·월별 리밸런싱으로 (a) T1/T3 버킷을 통째로 동일가중 매수하거나 (b) 버킷
안에서 무작위로 30종목을 뽑는(20개 시드 반복) 플라시보 백테스트.

**결과**:

| 시나리오 | CAGR |
|---|---|
| T1 버킷 전체, 동일가중(팩터 없음) | +11.97% |
| T3 버킷 전체, 동일가중(팩터 없음) | -5.74% |
| T1에서 무작위 30종목(20회 평균) | **+10.26%** (범위 +4.69%~+16.18%) |
| T3에서 무작위 30종목(20회 평균) | -6.86% (범위 -12.88%~-1.90%) |

**무작위로 뽑은 T1 30종목(+10.26%)이 PEG로 고른 T1(+4.08%)이나 ROE로 고른
T1(+8.68%)보다 높다.** 즉 팩터 기반 선별이 순수 무작위보다 나은 게 없었다
— 세 팩터가 측정한 건 팩터 알파가 아니라 T1 버킷 소속 여부 그 자체였다.

이 T1 프리미엄이 실제로 거래 가능한지는 §1 4번(A)(B)에 이미 정리한 대로
2026-08-18 REV20 검증이 부정적 근거를 이미 갖고 있다 — 현실적 비용 하에서
소멸을 확인한 바 있다.

---

## 5. 안전 관련 메모

이번 세션은 전부 `research/strategy-lab/`·`scripts/`(신규 패널 빌더 2개,
precheck 스크립트 3개) 범위에서만 작업했다 — `config/policies`·`config/
criteria`·production 코드는 건드리지 않았다. 부작용 있는 실행: 로컬 Node/
Python 스크립트 실행 다수(전부 `data/backfill/`·manifest에 아무것도 안 쓰는
진단 전용, 교훈43), `git push` 없음(이 문서 커밋 전까지). 매 단계(방법론
순서, precheck 진행 여부, 메타패턴 조사, 라인 종료)마다 사용자 확인을 거쳤다.
