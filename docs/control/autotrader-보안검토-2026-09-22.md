# autotrader 웹·공개 엔드포인트 독립 보안 검토 (2026-09-22)

**검토자**: Claude 서브에이전트(opus, 읽기 전용, 이 세션 맥락 없음) — 생산자(같은 세션의 Claude)와 분리.
**범위**: `autotrader/web.py`·`web_auth.py`·`config.py`(웹 제어)·`cli.py`(run-due)·`notify.py`, systemd 유닛,
nginx(`deploy/nginx-*.conf`), 같은 도메인의 공개 API `scripts/kis-minute-history-api.py`·`scripts/kis-live-relay.py`.
**방법**: 코드 읽기만(네트워크·실행 없음). 저장소가 공개라 공격자도 같은 코드를 본다는 전제. 비용 약 17만 토큰.
**검토 결과는 관점 하나다** — 각 지적은 Claude 가 코드로 다시 확인한 뒤 고쳤다(아래 "확인" 열).

## 결과와 조치

치명 0 · 높음 2 · 중간 6 · 낮음 5. 인증 우회·경로 조작·열린 리디렉션·XSS 는 없음.

| ID | 등급 | 지적 | 확인 | 조치(커밋 62e0de30, 회귀가 핀) |
|---|---|---|---|---|
| H1 | 높음 | 공개 `/live-ws` 중계가 실계좌 보유 종목(`~/.kis-holdings.json`)으로 구독 → 누구나 종목 목록 수신 | 코드 확인. **단 당시 KIS 실계좌 보유 0종목이라 실제 누출 없음**(정적 목록으로 폴백 중) | 기본은 공개 목록만. `LIVE_RELAY_USE_HOLDINGS=1` 일 때만 보유 종목 |
| H2 | 높음 | 동시 요청이 잠금 검사를 한꺼번에 통과(PBKDF2 뒤에 실패 기록) → TOTP 무차별 대입·CPU 소진 | 코드 확인 | 인증이 걸린 POST 직렬화. 회귀: 동시 20건 → 검증 5·잠김 15, 잠금 제거 시 실패 |
| M1 | 중간 | 전역 잠금(시간당 20회 실패 → 30분)으로 익명 공격자가 주인을 로그인에서 잠글 수 있음 | 코드 확인 | **유지(수용)** — 무차별 대입 방어가 우선. 로그인된 세션은 영향 없음, 킬은 서버에서도 가능 |
| M2 | 중간 | 실계좌 킬 해제가 TOTP 만으로 → 킬을 켜도 피싱된 코드로 풀어 예약 주문 재개 | 코드 확인 | 실계좌 `resume` 도 패스키 전용 |
| M3 | 중간 | 패스키를 못 쓰면(설정·라이브러리 누락) 조용히 TOTP 로 강등 | 코드 확인 | 실계좌 주문성 조작은 TOTP 경로에서 **항상** 거부 |
| M4 | 중간 | 비밀번호 + TOTP 두 개를 실시간 피싱하면 공격자가 자기 패스키 등록 가능 | 코드 확인 | 등록은 서버 CLI `passkey-enroll` 의 1회용 코드(15분)로만 |
| M5 | 중간 | 웹 프로세스가 홈 전체를 읽음 | 확인 + **추가 발견**: `~/collector-venv/.env.bak-20260920-105511`(키 백업)·KIS 토큰 캐시·DuckDNS·`~/.ssh` 도 열려 있었다 | `ProtectHome=tmpfs` + 필요한 것만 바인드(허용목록). 임시 유닛으로 차단·동작 확인 후 적용 |
| M6 | 중간 | 공개 `/minute-history` 1건 = KIS 호출 최대 30건 → 분산 IP 로 모의 키 한도 소진 | 코드 확인 | KIS 동시 1건·지난 날짜 캐시·날짜 범위 검증 + nginx IP 별 요청 제한 |
| L1 | 낮음 | `web_auth.json` 저장 경합(스텝 역행·패스키 유실 가능) | 코드 확인 | 잠금 + 임시파일 교체, TOTP 스텝은 증가만 |
| L2 | 낮음 | HSTS 없음 | 확인 | nginx server 블록에 HSTS |
| L3 | 낮음 | 숫자 값 HTML 미이스케이프(출처는 증권사 API) | 확인 | 전부 `E(str(...))` |
| L4 | 낮음 | 공개 앱에 `/accounts` 경로가 남아 nginx 한 겹만 막음 | 확인 | 앱도 `X-Forwarded-For` 있는(=바깥) 요청 거부 |
| L5 | 낮음 | 오류 원문 응답 노출 | 확인 | 고정 문구, 원문은 로그에만 |

"문제없음"으로 본 영역: 경로·접두사 처리, 열린 리디렉션, `?p=` 선택, 안내 문구 허용목록, CSRF·쿠키, X-Forwarded-For 신뢰 범위(루프백만),
패스키 챌린지(1회용·세션·조작 결합·2분), `web_set_auto`/`effective_auto`.

## 같은 날 함께 한 것(검토 이전)

- `/accounts` 인터넷 차단(984d8306) — 업비트·빗썸·KIS 실계좌·RV20 잔고가 **인증 없이 공개**돼 있었다. 실계좌 요약은 웹 로그인 뒤로.
- 패스키(WebAuthn, `webauthn` 2.8) 도입(4fc80479). 사용자가 2026-09-22 01:46 폰으로 1개 등록.

## 남은 위험 · 다시 검토할 때

- 남은 위험: M1(가용성), 첫 로그인은 여전히 비밀번호 + TOTP(보기·모의 조작까지는 피싱 가능 — 실계좌는 패스키로 차단),
  DuckDNS 계정 탈취 시 도메인 자체를 잃는다(→ 계정 2단계 인증 권장), 직접 만든 HTTP 서버(외부 전문 검토 없음).
- **다시 돌릴 때**: 실계좌 프로필을 처음 연결하기 직전, 또는 인증·세션·웹 제어 코드를 크게 바꿨을 때.
  같은 지시문으로 서브에이전트를 다시 부르면 된다(범위·질문 목록은 이 문서 첫머리 그대로).
