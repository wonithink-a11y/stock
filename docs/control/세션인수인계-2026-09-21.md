# 세션 인수인계 — 2026-09-21 (미국 데이터·단기 규칙 정리·가격 밖 데이터 라인 개시)

이전: `세션인수인계-2026-09-20-d.md`(그림자 2종·단기 3계열·비용 정정). 이 세션은 CLAUDE.md 정리 → 미국 라인 → 단기 규칙 후속 → 가격 밖 데이터로 이어졌다.
주문·정책·config·lib 변경 없음(VM 유닛 1개 신설·cron 예약 1건은 사용자가 직접 설치). 전부 push 완료.

## 0. 결론 먼저

| 항목 | 결과 |
|---|---|
| CLAUDE.md | 84KB → 55KB. 09-06 이후 "완료" 항목과 종결된 "다음" 2건을 `완료-이력.md` 로 **원문 이월**(대조 검증). 이후 다시 소폭 증가 |
| 미국 ETF 30주선 기울기(사전등록 f87b550) | **REJECT** — TRAIN 은 근소 통과, VALID/TEST 부호 반전. 방식 A 종결 |
| 미국 개별주(방식 B) | 데이터 확보 조사: Sharadar(개인 라이선스, 로컬 전용) 1순위, MarketParquet $79(무료 샘플은 1년치뿐). **생존자만으로는 결론 못 냄** → S&P500 스냅샷 수집기 신설(앞으로의 공백 방지) |
| 단기 규칙 후속 | O5(갭하락 후 종가 회복) **REJECT**(신호 없음). R3 가 "롱 +57bp" 로 잘못 표시됐던 것을 **정정**(TRAIN 부호를 곱해 숏으로 뒤집힘, 실제 롱 −57bp) |
| 비용 축 | 국내 주식형 ETF 는 거래세·매매차익 과세 비대상(원문 확인). 지수형 수단은 gross 가 한 자릿수(2.5~7bp)라 **비용 축만으로 경제성 없음** |
| 새 규칙(CLAUDE.md) | 단기 결과 표에 gross(CI)·손익분기·net 표준 열, reason 첫머리 "신호/경제성" |
| 가격 밖 데이터 | 분기 가속 ACC2 **REJECT** · 기업행사 공시 3셀 **REJECT** · **A3e 확장 재무 수집 진행 중** |
| 모의 실행 | O2b·O3u 슬리브당 4천만원 설계 동결. **선행 시험 = 동시호가 1주**(cron 예약, 09-21 15:24 / 09-22 08:45) |

## 1. 진행 중 (다음 세션이 먼저 볼 것)

1. **A3e 수집** — `python research/strategy-lab/build_extended_financials_panel.py`(재개 가능). 진행은 `data/fundamentals-ext/_state.json` 의 `doneKeys` 수(목표 24,750).
   2026-09-21 00:48 기준 2,419 완료·호출 약 2,800. 예상 4~5시간. 끊기면 같은 명령으로 이어간다. 완료 후 **계정 매핑 감사**(차입금·감가상각·capex 정의를 결과 전에 고정) → EV/EBIT·EV/FCF·EV/매출총이익을 **PBR 잔차화 후 추가 정보**로 사전등록.
2. **모의 동시호가 1주 시험** — VM crontab 에 `# auction-probe-2026-09` 줄 4개. 09-21 15:24 매수 → 15:45 대조 → 09-22 08:45 매도 → 09:05 대조(예약 자동 제거).
   결과: `ssh stock-new "cat ~/collector-venv/logs/auction-probe.log"` 와 `~/collector/research/strategy-lab/data/paper/auction_test_vts.json`(`auction_order_probe_vts.py status`).
   **통과 기준**: 체결가 == 공식 종가/시가. 실패하면 O2b·O3u 모의 실행 중단(대체 체결 방식은 새 GO 필요).
3. **미국 스냅샷 타이머** `us-universe-snapshot.timer` — 화~토 07:30 KST, 첫 자동 실행 **09-22**. 데이터 `~/collector-venv/us-universe`(저장소 밖), 실패 시 exit 1 → 텔레그램 장애 방.
4. **ETF 횡단면** — 세금·분류 확인 완료(국내 주식형 152/263). **데이터 수집(pykrx 또는 yfinance) 승인 대기**, 시험 결과 뒤 결정.

## 2. 이 세션에서 새로 알게 된 사실

- **KIS 오픈API `invest-opinion`(FHKST663300C0)** 이 증권사별 투자의견·**목표가(hts_goal_prc)**·직전 의견을 날짜별로 준다. 2010년까지 조회되고(삼성전자 2010·2015·2017 확인), 호출당 100행 상한이라 월 단위 창이 필요하다.
  소형주는 커버리지가 거의 없다(동화약품 2026 1건). EPS·영업이익 **추정치** 자체는 아니다(그건 FnGuide 유료). CLAUDE.md 규칙 1(추정치는 점수에 안 넣음)과의 관계는 채택 시 🔴.
- **fnlttSinglAcntAll** 커버리지(FY2023, 101개): 매출·영업이익·현금·자산 100%, CFO 98%, 이자/금융비용 99%, 매출총이익 89%, 차입금 89%, capex 81%, **감가상각비 27%**(주석에만 있는 기업이 대부분). 연결 82 · 별도만 19.
- **yfinance 는 상장폐지 종목을 지운다**(LEH·SIVB·WCOM·FRC 빈 응답, BBBY 는 재사용 티커라 다른 회사 45행). 생존자 표본의 편향 방향은 불분명(OFF 구간 낙폭이 회복된 종목만 남는 효과 vs 승자만 남는 효과).
- **함정 하나(내가 냈다)**: 기록 전용 셀 gross 에 TRAIN 부호를 곱하면 음의 TRAIN 셀이 **숏으로 뒤집혀** 좋아 보인다(R3 사례). 이후 기록 전용 셀은 **롱 gross 로 고정**했다. 판정 셀은 영향 없음.
- 기업행사 결과: E1(무상증자) 20일 초과는 양(+)이나 60일은 세 구간 모두 음(−) — 표류가 뒤집힌다. **권리락 전후 A2a 수정주가 처리가 정확한지 확인하지 않았다.** E2·E3(유상증자)는 회피 신호(음) 방향이 세 구간에서 일관.

## 3. 커밋 (전부 push)

| 커밋 | 내용 |
|---|---|
| 8a87a05 | CLAUDE.md 84→55KB, 완료 항목 원문 이월 |
| f87b550 · f881c0e | 미국 ETF 30주선 사전등록 → 결과(REJECT) |
| 8999e6b · 112a07c | 미국 개별주 PIT 데이터 조사 문서 |
| 54076e0 · 118955d | S&P500 스냅샷 수집기 + VM 타이머 유닛 |
| 7bdaf5f | notify-failure 감시 대상 6개 추가(총 12) |
| 4fd4228 | 단기 결과 기록 규칙 2개 |
| fea2e4d | O2b·O3u 모의 실행 설계 동결 |
| 356b95c · 5eb61da · ade9e8a | O5 사전등록 → 결과 → R3 부호 정정 |
| e1ad657 · 9e1b7d2 | 비용 축 조사(ETF 세금 원문 확인) |
| c276474 · 3242ee8 | 동시호가 1주 시험 스크립트 + cron 예약 스크립트 |
| 3b0ef12 · 3f43bde | 분기 가속 ACC2 사전등록 → 결과 |
| 557cb78 | A3e 수집기 + 설계 문서 |
| 4fa3933 · 977760f | 기업행사 이벤트 스터디 사전등록 → 결과 |

## 4. 하지 않은 것·보류

- **표준 지표 묶음(RSI·MACD 등) 스크리닝** — 기대치가 낮아 가격 밖 데이터를 먼저 하기로 했다.
- **유튜브 '매매의정석' 지표** — 코드를 사용자가 확보해 붙여 넣어야 검증할 수 있다(리페인트·룩어헤드 점검 → 이식 → 사전등록).
- **워크플로 알림 연결 테스트**(`notify-failure` workflow_dispatch) — 아직 안 보냈다.
- 방식 B 과거 26년 PIT — Sharadar 또는 MarketParquet 결제·키 입력은 사용자 몫. 지수에서 신호가 없었으므로 서두르지 않는다.

## 5. 다음 할 일 (시한순)

| 시점 | 할 일 |
|---|---|
| 09-21 15:45 이후 | 동시호가 매수 대조 결과 확인 |
| 09-22 07:31 이후 | 미국 스냅샷 첫 자동 실행 로그·`_status.json` 확인 |
| 09-22 09:05 이후 | 매도 대조와 시험 통과 여부, 설계 문서에 결과 부기 |
| A3e 수집 완료 | 커버리지 점검 → 계정 매핑 감사 → 가치·품질 사전등록 |
| 시험 통과 시 | O2b·O3u 모의 실행 코드(15:20 스캔·동시호가 주문·원장), 스위치는 사용자 |
| 다음 세션(우선순위 순) | ① **KIS 목표가 수집 설계** ② **유상증자 회피 필터 사전등록** (아래 §6) |

## 6. 다음 세션 착수 항목 상세

### ① KIS 목표가·투자의견 수집 설계
- 소스: KIS `invest-opinion`(FHKST663300C0), 종목·기간 창으로 호출, **호출당 100행 상한** — 100행이 차면 창을 반으로 쪼개 재조회하는 적응형 분할이 필요하다.
- 확인된 것: 2010년부터 조회, 증권사별 의견·**목표가(hts_goal_prc)**·직전 의견(`rgbf_invt_opnn`)·날짜(`stck_bsop_date`). 소형주는 커버리지가 거의 없다.
- 설계에서 정할 것: (a) 대상 종목(유동 20억 이상 + 커버리지 있는 종목), (b) 호출량 추정(종목 수 × 창 수 — 연 단위 창으로 시작해 상한이 차는 곳만 분할), (c) KIS 모의 도메인 호출 한도·속도, (d) 저장 스키마(원문 행 그대로, PIT = 의견 날짜),
  (e) 파생 신호 후보(목표가 상향 빈도·평균 상향률·의견 상향 이벤트) — **목표가는 주가 추종(후행)** 이라 가격으로 정규화해야 한다.
- 규칙 1(추정치는 점수에 안 넣음)과의 관계: 연구 축까지는 자유, 점수 반영은 🔴. 연구 전용 패널(`data/…-ext/` gitignore) 선례를 따른다. **대량 수집이라 착수 전 확인.**

### ② 유상증자 회피 필터 사전등록
- 근거: 기업행사 이벤트 스터디(977760f)에서 E2 주주배정·E3 제3자배정이 세 구간에서 음(−) 방향 일관(OOS 군집 |t| 3.2~3.4) — 단 사전등록 규칙상 REJECT(TRAIN 이 바닥선 미달). **사후 관찰이므로 새 사전등록이 필요**하다.
- 설계 초안: PBR `pbr_value_v1_combined` 슬리브(백테스트 엔진 동일)에서 **리밸런싱 시점 직전 N일(사전 고정 1개 값) 안에 주주배정·제3자배정 유상증자 공시가 있는 종목을 후보에서 제외**하고, 제외 없는 기준선과 CAGR·MDD·Sharpe(MTM)를 비교. N 은 결과 전에 하나로 고정(스윕 금지).
- 판정: 제외가 TRAIN/VALID/TEST 에서 같은 방향으로 개선하고 난수 제외(같은 수의 무작위 종목 제외 1,000회) 바닥선을 넘는가. production 반영은 🔴 별도 GO.
- 함께 점검: E1(무상증자) 60일 음(−)의 **권리락 수정주가 처리** 정확성(가격 데이터 신뢰 문제라 필터 실험 전에 확인).
