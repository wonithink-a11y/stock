# 세션 인수인계 — 2026-09-20-d (그림자 관측 2종 · 단기 전략 3계열 · 비용 정정)

이 세션의 흐름: "실험실 데이터 중 더 파볼 것" → **부분 익절 그림자** → OpenCode 산출물 정리 → ChatGPT 단기 전략 조사 검증
(**구조형 35셀**) → 세금·수수료 정정 → **종가→익일 시가 14셀** → **O2b 그림자**.
연구·문서만 바꿨다. 실주문 없음, 정책·config·lib·VM 변경 없음. 전부 push 완료(`84688af`).

## 0. 결론 먼저

| 항목 | 결과 |
|---|---|
| 부분 익절 그림자 | **가동·동결**(7f845b7). 판정 = +20%/70% 하나, 12코호트·300건에서 1회. 첫 관측은 10월 초 |
| 구조형 단기 35셀 | **ECONOMIC 0** — Sweep·MSS·FVG·Spring·PDL·흡수·드라이브·재테스트 (657a0d2) |
| 종가→익일 시가 14셀 | **ECONOMIC 0, HOLD** — O2b·O3u 만 INFORMATION, net 손익분기·체제 의존 (e2a2dbf) |
| O2b 그림자 | **가동·동결**(6d2fd27). 250일에 1회 판정. 첫 17일 gross −38bp(표본 이름) |
| 비용 정정 | KIS 이벤트는 코인원 WTS 한정·9월 한시. 세금은 왕복 1회(이중 계상 아님) |
| OpenCode 산출물 | 5-4·6-1 커밋, F2 shadow 삭제, stray 폴더 삭제 |

## 1. 커밋 (전부 push됨, `0de4e19..84688af`)

| 커밋 | 내용 |
|---|---|
| 8d178ee · 7bce4f8 | 부분 익절 그림자 설계 초안 → +20/70 근거 강도 재확인·대안 A·B·C |
| 7f845b7 · 12a971c · e77e6be | 그림자 구현·동결(selftest 19건) · CLAUDE.md · 10월 1일 점검 연결 |
| 6631336 | 선물 Stage 5-4 연속 sizing(판정 B) — 독립 재현 12/12 |
| 26c4169 | 선물 Stage 6-1 — "OOS는 discovery 재게시" 정정 문구와 함께 |
| a812cb1 · 657a0d2 | 구조형 사전등록 + 스크립트 → 결과 |
| 9f8cecf · e2a2dbf | 종가→익일 시가 사전등록 + 비용 정정 → 결과 |
| 6d2fd27 · 84688af | O2b 그림자 동결 → 첫 관측·CLAUDE.md |

## 2. 부분 익절 그림자 (`docs/control/부분익절-그림자-설계-2026-09-20.md`)

- **+20%/70% 는 "방향"만 검증됐다.** 09-04 격자 12칸 중 Sharpe 최선일 뿐 평평한 능선(+15/70·+20/50·+30/70 과 구분 근거 없음), 매도비율이 격자 끝(70%)에서 단조라 100%(목표 청산, Tier 2 기각)로 외삽 불가.
  **+40%/50% 는 경쟁 후보가 아니다** — 09-17 세그먼트 스윕의 다른 질문이고 09-19 noise-test 가 세그먼트 차등을 잡음으로 닫았다.
- 구성: 판정 = PRIMARY 하나. **A** 기록 전용 4칸(+15/70·+30/70·+20/50·+40/50) · **B** 능선 비율 · **C** 트리거 이후 잔여 수익률(시장 대비). 실주문 불변, 반사실 계산.
- **C in-sample 점검**: 트리거 이후 잔여 경로는 시장과 구분 안 됨(+20%: +0.7% ± 1.8%, 중앙값 −3.1% — 우측 꼬리 때문). **알파가 아니라 분산 도구**로 읽는다. TEST 에서 잔여가 시장을 이겼는데 Sharpe 개선이 TEST 에 가장 컸다 → "트리거 이후 부진"이 원인이 아니다.
- 판정: 12코호트·300건, INCONCLUSIVE 면 24코호트까지 **1회만**. KEEP 후보여도 `portfolio.v1.json` 반영은 🔴. **현실적 기대 = INCONCLUSIVE**(무작위 sd 0.16~0.18 vs 개선 +0.17).
- 실행: `python research/strategy-lab/run_partial_exit_shadow.py` 월 1회(A2a 증분 뒤). 로그 `research/strategy-lab/reports/2026-09-partial-exit-shadow/observations.jsonl`(gitignore 예외, 커밋).
  첫 코호트(09-01, 진입 09-02)의 청산이 추석(09-24~26) 때문에 10월 초로 밀릴 수 있다 — "닫힌 거래 없음"은 오류가 아니다.

## 3. 구조형 35셀 (`findings/structure-patterns-{preregistration,results}-2026-09.md`)

KR 5분 20셀 + 크립토 1시간 15셀. **ECONOMIC 0.** K: REJECT 16·INFORMATION 4 / C: REJECT 11·**판정불가 4**(흡수 — 원 파라미터에서 이벤트 2건, 사전 선언한 완화 사다리도 문턱 미달 → 미판정으로 남김).

- **유일한 방향성**: 스윕→MSS→FVG 재진입 롱(K2f). 세 구간 모두 같은 방향이나 gross +7~25bp vs 비용 33bp → net −1~−26bp.
- ★ **접촉=체결 가정이 t 를 크게 부풀렸다**: t 13.9(접촉) → 4.5~5.8(관통 5·10bp). 지정가 셀은 처음부터 관통 기준으로 잰다(교훈).
- 방향이 반전인 것: K2m(스윕 뒤 MSS 롱 → 이후 하락), K5b(장초 상승 드라이브 되돌림 롱 → 큰 손실). 숏이면 net 양이지만 실전 불가.
- 이웃 파라미터 검증은 ECONOMIC 이 0개라 실행되지 않았다.

## 4. 비용 — 확인·정정 (가장 중요한 학습)

KIS 이벤트 페이지를 직접 열어 확인(2026-09-20): **코인원 WTS 주문 한정 · 2026-09-01~30 · 일 20억/누적 400억 한도**, 수수료·유관 0, **매도 거래농특세는 부과**. 정상: 수수료 0.0140527%·유관 0.003640% 편도.

| 오해 | 실제 |
|---|---|
| "세금 20bp 를 왕복에 붙이면 안 된다(40bp)" | 우리 모델은 **왕복 1회(매도만) 20bp** — `KR_FIXED_BP = 0.72 + 20.0`. 이중 계상 아님 |
| "유관기관 0.72bp 는 틀렸다" | 0.72 = 0.364 x 2. 맞다 |
| "수수료 무료" | 이벤트는 우리 KIS API 주문에 **적용되지 않는다**. 지속 기준은 정상 수수료 포함 |

- **연속 시장(5분봉 등) 왕복 ≈ 33.5bp**(세금 20.7 + 호가 스프레드 평균 12.6). 저가주가 많은 유니버스라 한 호가가 평균 12.6bp. 지속 기준 ≈ 36bp, 이벤트 ≈ 33bp.
- **종가·시가 단일가 체결**은 스프레드를 건너지 않는다 → 왕복 **23.54bp**(세금 20 + 수수료 2.81 + 유관 0.73).
- 세율 수치(코스피·코스닥 0.20%)는 2차 출처 다수로만 확인했다(공식 1차 미확인). **호가단위 표·유관기관 값은 기억/페이지 기반 — 종목 가격대별 호가 bp 는 근사.**
- 구조형 문서 두 개에 정정 주석을 넣었다. 이후 실험은 비용을 성분(세금·수수료·유관·스프레드)으로 분리해 기록한다.

## 5. 종가→익일 시가 14셀 (`findings/close-open-{preregistration,results}-2026-09.md`)

A2a 일봉 2016~2026, 종가·시가 단일가 체결 가정. **F(밤사이/장중 분해)는 이미 한 것**(`kr-intraday-overnight-asymmetry`: 밤사이 +14~18bp, 장중 −16~−24bp) — 새 것은 비용 재정의와 조건부 셀.

- **INFORMATION 롱**: O2b(종가>전일고가·거래량비≥1.5) gross +33/+19/+29bp, net **+2.1**(스트레스 −10.9, 이벤트 +5.7) · O3u(수익≥+5%) net −0.6. 나머지 롱은 net 음.
- **방향 사실**: 강한 종가·급등 → 밤사이 갭상승 후 **익일 장중 −35~−117bp**(갭상승 롱 open→close −117bp = 가장 확실한 "하지 말 것", 숏은 불가).
- **룩어헤드 아님**: 분봉 231일에서 신호를 15:20 로 옮겨 재계산 → gross 동일(O2b 45.9 vs 45.4bp). 신호 = 최종 종가라는 낙관 우려 해소.
- **체제 의존**: 연도별 net O2b 2022 −15 · 2018~19 +1~6 · 2020 +23 · 2026 +21. 최근 231일 +22bp 는 가장 강한 해.
- 매일 100% 회전 → 세금만 연 50%p 규모. net +2bp 면 실질 가치 거의 없다.
- ★ O0(기준선)의 INFORMATION 은 무의미다 — 정보 통계가 "유니버스 대비 초과"라 기준선은 정의상 0 이고 부호가 잡음이다.

## 6. O2b 그림자 (`docs/control/O2b-그림자-설계-2026-09-20.md`)

- 규칙 O2b 동결(유동 20억↑·종가>전일고가·거래량비≥1.5·수익<28%), T 종가 매수→T+1 시가 매도, 반사실. **신호일 ≥ 2026-08-22**. `futures/run_close_open_shadow.py`(selftest 10건).
- 판정 **250일 1회**: net_durable 평균>0 · t≥2 · 스트레스>0 = KEEP 후보 / 평균≤0 = REJECT / 그 외 500일 1회 연장. 일별 sd ≈139bp → t≥2 는 net ≈ **+18bp 이상**이어야 넘는다(10년 수준 +2 면 INCONCLUSIVE).
- **첫 관측 17일**: gross −38bp(시장 −16, 초과 −22, t −1.5). 신호일 시장 밤사이가 급락 다발(9/1 −206·9/10 −187bp, 종목 중앙값 −228bp 로 광범위) — 표본이 이르다. 이걸로 판단하지 않는다.
- 로그 `research/strategy-lab/reports/2026-09-o2b-shadow/observations.jsonl`(추적, 커밋). 실행 `python research/strategy-lab/futures/run_close_open_shadow.py`(같은 신호일 중복 기록 없음).

## 7. OpenCode 산출물 정리

- **커밋**: `stage5_4_continuous_sizing.*`(독립 재현 12/12, 판정 B) · `stage6_1_strict_oos.*` — 원 표기("STRICT HOLDOUT OOS")는 보존하되 **스크립트 상단에 "OOS 아님·discovery 재게시·증거로 인용 금지" 정정**, RV20 동결 메모의 "미커밋" 문구 갱신.
- **삭제(사용자 지시)**: `run_f2_shadow.py` · `eval_f2_shadow.py` · `data/paper/f2_static_v1_shadow.jsonl` — 크립토 funding carry 가 종결돼 쓸모가 없어졌다. 참조하는 타이머·워크플로 없음(과거 인수인계 문서에만 언급).
- **삭제**: `research/research/`(이중 경로 stray, 내용 `{}` — 09-20 오전 크립토 슬리브 테스트 부산물).

## 8. 다음 세션이 알아야 할 함정

1. **지정가 체결 가정을 "접촉"으로 두지 않는다.** 접촉만으로 체결되는 이벤트는 되돌아간 경우가 몰려 승자 선택 편향이 생긴다(t 13.9 → 4.5). 관통 기준으로 재는 진단을 같이 낸다.
2. **`.cache/a2a_parquet` 은 낡았다**(2026-08-03 까지). 커밋된 `data/backfill/price/a2a/*.jsonl.gz` 는 2026-09-16 까지다 — 그림자는 후자를 직접 읽는다. 과거 연구 스크립트가 캐시를 읽으면 최근 6주가 빠진다.
3. **5분 격자·패널은 2026-08-21 까지**(232일). 그 뒤 분봉 실험은 새 격자를 만들어야 한다(`intraday_grid5m.parquet` 은 로컬 캐시).
4. **pytest 는 selftest 를 자동 호출하지 않는다** — `tests/test_partial_exit_shadow.py` 처럼 감싸야 회귀에 잡힌다(부분 익절 그림자만 감쌈. `structure_phase4`·`close_open_phase5`·`run_close_open_shadow` selftest 는 수동 실행).
5. **셀 정의가 죽는 경우**(흡수, 이벤트 2건)는 결과 전에 사건 수로만 완화 사다리를 정해 두었다. 정의를 결과 보고 바꾸는 것과 구분한다.
6. `structure_phase4.py` 에 진단용 `thru` 옵션(기본 0 = 원 동작)이 있다. 원 셀 판정은 그대로다.
7. CRLF 경고가 커밋마다 뜬다(작업 트리 CRLF → LF 변환). 무해하나 파일 전체가 바뀐 diff 로 보이면 이 때문이다.
8. **작업 VM 접속은 `ssh stock-new`**(hostname `stock`). CLAUDE.md 의 "크립토 슬리브 타이머 설치가 남았다"와 09-21 점검표("내일 09:05 첫 청산")가 어긋난다 — 타이머가 이미 있는지 먼저 확인:
   `ssh stock-new "systemctl list-timers --no-pager | grep -E 'crypto-upshock|rv20|infinite-buying'"`.

## 9. 확인하지 못한 것

- 유관기관 수수료율·호가단위 표는 페이지·기억 기반, 세율의 공식 1차 출처는 미확인.
- 크립토 슬리브·RV20·무한매수 VM 상태는 이 세션에서 보지 않았다(09-21 아침 점검표가 정본).
- A2a "현재 상장분"은 생존편향이 있다 — forward 창(최근 몇 달)에서는 영향이 작다고 보지만 측정하지 않았다.

## 10. 다음 할 일 (시한순)

| 시점 | 할 일 |
|---|---|
| 09-21 아침 | 09-20-c 아침 점검표(복구한 유닛 3개·크립토 슬리브 09:05 첫 청산·RV20·무한매수 21:30). `--failed` 비었는지 |
| 10-01~05 | **A2a·refresh-selections 녹색 확인 뒤** 그림자 2개 실행·커밋: `run_partial_exit_shadow.py`(첫 코호트) · `run_close_open_shadow.py` |
| ~11-19 | 크립토 상승충격 슬리브 신호일 60일 판정(1회) |
| 2027-08~ | 부분 익절 그림자 12코호트·O2b 250일 판정 |

**하지 않는 것**: 새 단기 백테스트를 더 돌리지 않는다 — 그림자 3개(부분 익절·O2b·크립토 슬리브)에 표본이 쌓이는 쪽이 정보 대비 비용이 낫다. 조건 파라미터(O2b 0.8·1.5·5%, 부분 익절 20/70) 튜닝 금지, 손익분기 셀을 조정해 양으로 만드는 것은 다중검정이다.

## 11. 작업트리

`research/strategy-lab/data/market-regime/_manifest_fred_extended.json` · `ui/data/macro.json` — 자동 갱신분, 손대지 않았다. 미추적 파일 없음.
