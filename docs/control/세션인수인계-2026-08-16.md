# 세션 인수인계 — 2026-08-16

새 세션은 이 문서 하나로 오늘 검증된 것과 다음 결정 지점을 파악할 수 있다. 이 문서는
**정본이 아니다** — 정본은 CLAUDE.md·`docs/control/TASKS.md`·각 보고서 원본이고, 여기는
"오늘 뭘 했고 왜 그랬는지"와 "다음에 뭘 눌러야 하는지"만 모은다.

## 새 세션은 여기부터

1. `CLAUDE.md` 상태 블록을 읽는다 — T1·A3c 완료는 반영돼 있지만 **A2b 관련
   내용(KRX/Naver 트랙)은 이 세션 이후로 낡았다**. A2b는 이 문서(§4)가
   정본 대신이다 — KIS 전환(PR-1.5)에 이어 PR-1.6 임계 승격·전량 재실행
   finalize 통과까지 **2026-08-17에 끝났다.**
2. **A2b는 완료됐다 — 이 트랙에 열린 결정은 더 없다.** §4 하단 "PR-1.6
   승격 + 전량 재실행 완료" 소절이 최종 결과다. 다음 세션이 이어서 볼 것은
   A2b 자체가 아니라 그 후속(Strategy Lab PRIMARY 전환 등, §4 끝 참고)이고
   이것도 별도의 새 🔴 결정이라 여기서 미리 답을 내지 않는다.
3. Strategy Lab(SL-2)·DeepSeek same-bar 리뷰는 전부 종결됐다 — 참고만
   하면 되고 추가 결정 불필요.

---

## 1. T1 재현성 정찰 — 완료 (REPRODUCIBILITY FAIL)

Day1~Day7 전체 완주 후 최종 판정. **KIS 데이터 자체의 문제가 아니라 "재조회하면
완전히 같은 값이 나온다"는 동치 가설의 FAIL**이다. KIS는 계속 쓴다. 수집 시점
snapshot이 백테스트 기준 원본이고, 재조회 결과는 별도 버전으로만 취급한다
(`pendingT1.versionRetention: keep-all` 유지, 변경 없음).

```
상세  docs/operations/T1-Day1to7-최종판정-2026-08-16.md
      docs/operations/T1-후속검토-판정명칭및운영권고-2026-08-16.md
요약  CLAUDE.md 완료 블록 · docs/control/TASKS.md T1-1 행
```

**VM 조치**: `minute-t1-probe` cron 제거 완료(로그·원본 8개 파일은 보존, 삭제 안 함).
`minute-collect`·`alive-monitor`는 손대지 않았다.

**미해결로 남은 것**(그대로 유지, 다음 T1 창에서만 고칠 수 있음): `crossRun`이
sha만 남기고 필드 단위 diff를 안 남기는 구조적 한계, anchor가 `immediateRepeat`
대상에서 빠지는 문제 — `T1-후속검토-...md` §10 참고.

## 2. A3c(발행주식총수) — 완료

finalize 성공, 커밋 `a997f9a`. 격자 134,112셀 전수·`istcTotqyRowFoundRate`
95.257%·레코드 98,684. manifest `data/backfill/manifest/A3c.json`.

peg를 `lib/a5/resolver.js`에 연결하는 건 **별도 승인 필요, 아직 미착수**
(TASKS.md A5-3 참고).

## 3. calendar.json / KRX 인증 — 완료, 재현성 있는 원인 규명까지 끝남

**증상**: `data/backfill/calendar.json`을 재생성할 때마다 1순위(KOSPI 인덱스) 경로가
매번 실패해 폴백(개별종목 union)으로 넘어갔고, 그 폴백이 "오늘부터 최근
3,000거래일"만 주는 롤링 윈도우라 `from` 경계가 재생성마다 조용히 전진했다(A2a
빌드 시점엔 2014-05-13, 오늘 재생성 직후엔 2014-05-26).

**원인**: pykrx 1.2.8이 KRX 로그인 세션(`KRX_ID`/`KRX_PW`)을 요구하는데
`calendar.yml`이 이 secrets를 안 넘기고 있었다(GitHub Secrets엔 이미 존재,
워크플로 YAML에서 `env:`로 안 끌어왔을 뿐). 원본 HTTP 요청을 직접 재현해
`HTTP 400 / body: "LOGOUT"`까지 확인했다.

**수정**: `calendar.yml`의 Build calendar 스텝에 2줄 추가(커밋 `824c87e`) →
재실행 → `source: pykrx:index_ohlcv:1001`로 정상 전환, `from: 2014-01-02`로
완전 복원 확인(커밋 `5e5fff1`). `build-calendar.py`는 무수정.

```
관련 커밋   824c87e(ci 수정) → 5e5fff1(재생성 결과)
현재값      from 2014-01-02 · to 2026-08-14 · tradingDayCount 3097
```

## 4. A2b — 완료

### 지금까지 확인된 것

1차 실행(calendar 수정 전) → 4건 실패. calendar 인증 수정 후 재실행 →
**"캘린더 밖 날짜"·"exitAt 비거래일" 2건은 해소**, 나머지 2건은 그대로:

```
FAIL  데이터 확보 종목 509 >= 600 (정찰 실측 631)
FAIL  분석구간 내 폐지 461 >= 550 (정찰 실측 572)
```

**원인 규명 완료**: `build-price-a2b.py`가 쓰는 `stock.get_market_ohlcv_by_date(...,
adjusted=True)`는 **KRX가 아니라 Naver Finance**(`fchart.stock.naver.com/sise.nhn`)로
간다 — calendar.yml에 붙인 KRX 인증과 완전히 무관한 별도 서버다. 직접 재현해
Naver 쪽도 요청 범위와 무관하게 **정확히 3,000캔들, "오늘 기준 최근"만** 준다는
것도 확인했다(`count=999999` 요청 → 실제 반환 3,000개, 2014-05-26~2026-08-14).
정찰(2026-08-05)과 실제 수집(2026-08-16) 사이 11일 동안 이 롤링 윈도우가 전진해서
631→509·572→461로 줄어든 것으로 강하게 추정된다(LIKELY — 어느 종목이 정확히
넘어갔는지는 미확인, 원 정찰 산출물이 커밋 안 돼 있어 재정찰 없인 특정 불가).

`A2a`도 **완전히 같은 호출**을 쓰고 있어 이미 같은 문제를 안고 있다
(`frontTruncatedTickerDays: 2942`가 그 증거) — A2b만의 문제가 아니라 이 프로젝트가
"수정주가"를 얻는 방식 전체의 구조적 한계다.

### 검토한 우회 방법 4가지 (오늘, 코드 미변경)

```
① Naver 기간 분할        불가능(CONFIRMED) — count가 "오늘부터"로 고정 변환되는
                         구조라 과거 구간만 떼어 불러도 빈 응답만 온다
② 저장소의 다른 경로     없음 — A2a도 동일 호출·동일 문제
③ KRX 저수준 함수 전환   pykrx.website.krx.market.wrap.get_market_ohlcv_by_date()가
                         adjStkPrc=2(수정종가)를 KRX 자체에서 직접 준다(재구성 불필요).
                         인덱스와 같은 KrxWebIo 인증 계열 — 미인증 시 인덱스와 동일한
                         실패 신호(JSONDecodeError) 재현 확인. 인증 후 3,000 캡이
                         진짜 없는지는 로컬에 KRX_ID/PW가 없어 미검증(LIKELY)
④ KIS 데이터로 전환      scripts/probe-minute-kis.py가 이미 정의해 둔
                         inquire-daily-itemchartprice(FHKST03010100)에
                         FID_ORG_ADJ_PRC(수정/원주가 플래그) 존재 확인. KIS 인증
                         인프라는 이미 있음(분봉 수집과 공유). 실제 이력 깊이·
                         페이지네이션은 검증 기록 없음(UNCONFIRMED)
```

**다음 세션의 첫 작업(사용자 지시, 2026-08-16 확정)**: ③을 코드에 반영하기 전에
먼저 **소수 종목으로 단독 프로브**한다.

```
대상    pykrx.website.krx.market.wrap.get_market_ohlcv_by_date()
        (내부적으로 개별종목시세 / MDCSTAT01701 / adjStkPrc=2)
인증    기존 KRX_ID/KRX_PW로 (calendar.yml에 이미 연결한 것과 같은 자격증명)
범위    소수 종목(005930 등) 단독 프로브 — 대량 실행 아님
```

확인할 것 (전부 이번 세션에서 로컬 자격증명 없이는 못 밟은 것들):

```
1. 2014~현재 전체 기간을 실제로 반환하는가
2. 실제 반환 행수와 시작일/종료일
3. 반환값이 정말 수정주가인가(원가와 대조)
4. 3,000캔들 제한이 이 경로에도 있는가(없어야 ③이 성립)
5. 005930 등 분할 이력 종목에서 가격 연속성이 유지되는가
   (분할 전후 캔들이 이어지는지 — 수정주가라면 끊기지 않아야 한다)
```

**★ 프로브 결과를 확인하기 전까지 다음을 하지 않는다**: `config/policies/
price.v1.json`(a2b 블록·`collectFrom`·임계값 600/550) 수정, `build-price-a2b.py`
수정, `price-a2b.yml` 수정, A2b 전체 재실행. 프로브는 새 진단 스크립트
하나(예: `scripts/probe-price-a2b-krx-native.py`류) 정도로 충분하고, 그 결과를
갖고 다음 결정(③ 채택/④ 전환/임계값 재검토)을 사용자와 다시 논의한다.

### ③ 프로브 완료 — 결과: 3,000캔들 캡 없음, 그러나 분할 미조정으로 부적합

`scripts/probe-price-a2b-krx-native.py` + `probe-price-a2b-krx-native.yml`
(커밋 `3804d6a`) 실행 결과, `data/backfill/_probe-price-a2b-krx-native.json`
(커밋 `aabc808`):

```
005930·000660 둘 다  rows=3097 · first=2014-01-02 · last=2026-08-14
                     (calendar.json 전체 tradingDayCount와 정확히 일치)
```

**1·2·4 확인**: 3,000캔들 캡 **없음** — `adjStkPrc` 파라미터와 무관하게 요청한
전체 캘린더 구간(3,097거래일)을 그대로 돌려준다. Naver 우회 경로의 롤링 윈도우
문제는 이 경로에 없다.

**3·5 확인 — 부적합 판정**: `adjusted=True`(adjStkPrc=2)로 005930의 2018년
액면분할(50:1, 2018-05-04) 전후 종가를 직접 대조:

```
2018-05-03  2,650,000   (분할 전날)
2018-05-04     51,900   (분할 당일)
```

51.06배 단절 — **분할 조정이 전혀 안 되어 있다.** `adjusted=True`여도 그날짜의
KRX 정산가(액면분할 반영 전 원 단위)를 그대로 준다는 뜻이다. KRX의
`adjStkPrc=2`는 배당/유상증자 등 다른 조정을 의미할 뿐 액면분할 연속성과는
무관한 것으로 보인다(추정 — KRX 공식 정의 문서로 확인 안 됨).

**결론**: ③(KRX-native)은 3,000캔들 캡은 풀지만 분할 연속성이 없어 그대로
쓸 수 없다 — 분할 이력 종목마다 별도 재구성이 필요해지고, 이는 LAB-7이
피하려 했던 "재구성 비용"을 다시 불러온다. **다음 후보는 ④(KIS)** —
`FID_ORG_ADJ_PRC` 플래그의 분할 조정 여부와 이력 깊이·페이지네이션을 같은
방식(소수 종목 프로브)으로 확인해야 한다.

### ④ 프로브 완료 — 결과: 캡 있지만 페이지네이션 성공 + 분할 연속성 확인됨

`scripts/probe-price-a2b-kis.py`(커밋 다음 절 참고) 로컬 실행 — KIS
`inquire-daily-itemchartprice`(FHKST03010100)를 005930/000660으로 확인.
로컬 실행 산출물이라 규칙 4에 따라 `data/backfill/`에 커밋하지 않았고
실행 후 삭제했다. 수치는 이 문서에 옮겨 적는다.

**1·2 확인 — 캡 있음, 페이지네이션으로 완전히 우회됨**: 1콜당 정확히 100행
캡(`FID_INPUT_DATE_1/2` 범위와 무관, 확인된 하드 캡). `FID_INPUT_DATE_2`를
직전 응답의 최이른 날짜-1일로 갱신하는 커서 페이지네이션으로 32콜
(31×100행 + 마지막 97행) 만에 **정확히 3,097행**을 받았다 — `calendar.json`의
`tradingDayCount`와 완전히 일치, 첫날 2014-01-02·끝날 2026-08-14까지 빠짐없이.
005930·000660 둘 다 동일. **Naver·KRX-native와 달리 이 경로는 하드 3,000캔들
캡이 아니라 "콜당 100행" 캡이라 페이지네이션 비용만 감수하면 전체 이력을
받는다.**

**3·4 확인 — 분할 연속성 있음**: 005930 2018-05-04(액면분할 50:1) 전후를
`FID_ORG_ADJ_PRC=0`(수정주가)과 `=1`(원주가)로 나란히 대조:

```
             0503        0504        (원주가=1)      0503        0504
adjusted   53,000  →  51,900                     rawCloses   2,650,000  →  51,900
```

`adjusted`(=0)는 분할 전후로 **연속적**(53,000 → 51,900, 정상적인 하루
변동폭)이고 `raw`(=1)는 ③과 똑같은 51배 단절을 그대로 보인다. **KIS의
`FID_ORG_ADJ_PRC=0`은 실제로 액면분할을 조정한다** — ③의 KRX `adjStkPrc=2`와
다른 지점이다.

**결론 — ④가 인수인계 §4의 확인 항목을 전부 통과했다.** 전체기간 반환(캡은
있으나 페이지네이션으로 완전 우회) · 실제 수정주가 · 분할 연속성 유지.
A2b(및 구조적으로 같은 문제를 가진 A2a)의 대체 경로로 ④ 채택을
권고한다. **다음 결정은 사용자 GO**: `config/policies/price.v1.json`의
source를 KIS로 바꾸고 `build-price-a2b.py`를 페이지네이션 로직으로
재작성하는 것은 🔴 등급(정책·수집기 변경)이라 착수 전 승인이 필요하다.

### 1단계 정찰 완료 — 표본 40건으로 총 호출량 추정을 좁힘

전량(1,223건) 적용 전에 던진 질문: "데이터가 있는 종목(632건 추정)의 평균
콜 수를 몰라 총 호출량이 5,600~20,800으로 3.7배 벌어져 있다"(위 §7 KIS 호출량
추정 참고, 이 문서 아래 별도 절 없음 — 세션 내 대화 응답). 그 미지수를
`scripts/probe-price-a2b-kis-sample.py`(새 파일, A2b 코드·정책은 무수정)로
좁혔다.

**표본 설계**(재현 가능):
```
모집단   data/backfill/universe/a1b/delisted.jsonl 전수 1,223건 (corp_code 오름차순)
방법     random.Random(SEED).sample(range(1223), 40) — 비복원, 이후 인덱스 오름차순 정렬
seed     20260816
크기     40
인덱스   [19,41,71,74,91,127,136,224,264,295,301,325,364,402,430,454,569,587,595,
          636,641,671,707,716,734,767,776,817,818,823,841,951,954,960,993,1094,
          1178,1196,1198,1218]
티커     010050,005270,004660,025340,002270,016560,012090,009720,027440,032050,
          032930,054250,032390,065810,050050,035960,042670,057100,053870,064060,
          066480,138250,045890,077280,080030,083580,088020,108790,950010,096640,
          099350,207230,192240,183350,220110,329020,442900,451700,452980,473370
동시성   2 (ThreadPoolExecutor, 티커 단위 병렬 — VM 운영기준 시작값을 이 TR_ID에서 처음 검증)
콜 간격  0.15초 (워커별)
```

**실측 결과**(2026-08-16, 로컬 실행 — 산출물은 규칙 4에 따라 커밋하지 않고
이 문서에 수치만 옮긴다):

```
빈응답      19/40 (47.5%) — Naver 경로 옛 실측 48.4%(591/1,222)와 거의 일치.
            빈응답은 소스 특성이 아니라 "2014-05 이전 폐지"라는 시장 사실이라
            KIS로 바꿔도 비율이 유지된다는 이전 가정을 뒷받침한다.
유효 종목   21/40 (52.5%) · 평균 콜 13.24 · 범위 3~30 · 표준편차 약 7.64
            (분산이 커서 평균 하나로는 부족 — 최근 상장·최근 폐지(콜 6~8) 묶음과
            2014년 이전부터 오래 거래된(콜 20~30) 묶음이 섞여 있다)
표본 총 호출량   297회 (19×1 + 21×13.24)
EGW00201        0회 (297콜 전체)
실패(재시도 소진)  0건
소요시간          138.4초 (동시성 2) → 처리량 약 2.15콜/초
```

**모집단 재추정** (표본 비율을 1,223건에 그대로 적용):

```
예상 빈응답    1,223 × 0.475 ≈ 581건 (각 1콜)
예상 유효      1,223 × 0.525 ≈ 642건 (평균 13.24콜, ±1표준편차 5.6~20.9콜)
총 호출량 점추정   581 + 642×13.24 ≈ 9,080콜
총 호출량 범위(±1SD)  약 4,200 ~ 14,000콜
```

이전 추정(§ 대화 응답, 5,600~20,800콜 3-시나리오)보다 훨씬 좁혀졌고, 점추정
9,080콜은 그 중간 시나리오(10,703)에 가깝다.

**소요시간 재추정** (실측 처리량 2.15콜/초, 동시성 2 그대로 유지 가정):

```
점추정 9,080콜   ≈ 4,224초 ≈ 70분(1.2시간)
하한 4,200콜     ≈ 1,953초 ≈ 33분
상한 14,000콜    ≈ 6,512초 ≈ 109분(1.8시간)
```

**EGW00201 위험**: 표본 297콜(동시성 2·0.15초 간격) 전체에서 0회. 이전엔
"미검증"으로 남겨뒀던 이 TR_ID(FHKST03010100)에서 처음 실측한 값이고,
분봉 엔드포인트에서 검증된 동시성 2가 여기서도 안전하다는 첫 신호다.
다만 297콜은 예상 총량(약 9,000콜)의 약 3.3%에 불과해 표본 크기의 한계는
남는다 — 전량 실행에서 완전히 배제된 것은 아니다.

**결론**: 1단계 정찰로 총 호출량·시간 추정이 크게 좁혀졌고 EGW00201·실패
둘 다 0건이었다. ④(KIS) 채택 권고는 유지되고, 이제 규모 추정이 막연한
범위가 아니라 실측 기반 점추정(약 9,000콜·약 70분)이 됐다.

### 2단계 완료 — 정책 승격 + 수집기 재작성 (사용자 GO, 커밋 9611f49)

`price.v1.json` PR-1.4 → PR-1.5, `build-price-a2b.py` KIS 페이지네이션
재작성, `test-price-a2b.py` 스텁 교체, `price-a2b.yml` matrix(8) 제거·단일
job 전환. 전체 회귀 스위트 통과(A2b 36건 포함). 세부는 커밋 메시지 참고.

**설계 결정 하나 — matrix 샤딩 폐기**: 기존 A2a/A2b는 8-way GitHub Actions
matrix job으로 병렬 수집했다. KIS는 토큰 발급이 5분당 1회 제한이라(실측,
`probe-minute-kis.py`) 8개 job이 동시에 토큰을 요청하면 1개만 성공한다.
job을 나누는 대신 프로세스 하나 안에서 `ThreadPoolExecutor(max_workers=2)`로
동시성을 낸다 — 표본 프로브가 검증한 것과 정확히 같은 방식이다. `a2b.shards`를
1로 내렸고, 전량 예상 70분이 단일 job 시간제한(150분) 안에 넉넉히 들어가
sharding의 원래 목적("실패 시 재실행 범위 축소")이 굳이 필요 없어졌다.

**★ 실행 전 반드시 필요 — GitHub Secrets 누락**: `gh secret list`로 확인한
결과 `KIS_APP_KEY`·`KIS_APP_SECRET`이 저장소 시크릿에 없다(로컬 `.env`와
VM에만 있다). 워크플로를 그대로 트리거하면 토큰 발급 단계에서 즉시 실패한다.
**사용자가 GitHub 저장소 설정(Settings → Secrets and variables → Actions)에서
두 시크릿을 추가해야 한다** — Claude는 시크릿 값을 다루지 않는다(규칙 2).

**3단계(실제 전량 수집 실행)는 이 세션에서 하지 않았다** — 시크릿이 없어
어차피 못 돈다는 것과 별개로, 실행은 등급과 무관하게 항상 사전 확인
대상이다(부작용을 내는 Actions 수동 트리거). 시크릿 추가 후 사용자가
트리거를 요청하면 그때 `gh workflow run price-a2b.yml`을 돌린다.

### 3단계 실행 — collect 성공, finalize FAIL (원인 확정, 커밋 db9d7a2·614d63a)

사용자가 시크릿을 저장했으나 실제 이름이 `KIS_APPKEY`/`KIS_SECRET`이었다
(워크플로가 기대한 `KIS_APP_KEY`/`KIS_APP_SECRET`과 다름, `gh secret list`
실측으로 발견). 워크플로의 `secrets.` 참조만 실제 이름으로 매핑(커밋
`db9d7a2`), 로컬 `.env`·코드의 환경변수 이름은 그대로 뒀다. 같은 자리에서
Actions 실행 간 KIS 토큰 캐시 공유(`actions/cache`)도 추가했다(커밋
`614d63a`) — VM·로컬은 이미 캐시 파일 하나를 공유하는데 Actions만 매번
새로 발급하던 것을 보완.

**실행 결과**(run `31952773781`): collect 54분 성공 — 713,804행,
624종목 데이터 확보(2026-08-05 Naver 정찰 실측 631과 거의 일치, **KIS 전환의
목적인 롤링 윈도우 손실 해소는 확인됐다**). finalize는 인수 조건 2건에서
FAIL:

```
FAIL  데이터 확보 종목 504 >= 600 (후보 1223 − 품질 제외 120)
FAIL  분석 구간(2016-01-01) 내 폐지 456 >= 550
WARN  품질 제외율 19.23% <= 5% (120종목/624, TRANSIENT_PRICE_SPIKE 15 ·
      UNADJUSTED_CORPORATE_ACTION 105) — WARN 임계의 거의 4배
```

manifest 계약대로 산출물은 안 썼다(규칙 43 정상 동작) — 버그가 아니라
게이트가 정확히 잡은 것이다.

**원인 확정**(120건 중 31건 표본, KIS-adjusted·KIS-raw·Naver 3소스 교차검증
+ 플래그일 이후 60일 거래지속 여부):

```
KIS 데이터 자체 오류       0/31 — KIS-raw·KIS-adjusted·Naver 3소스 수치가
                          소수점까지 완전 일치(오류라면 Naver와 어긋났어야 함)
판정 로직 오탐            0/31 — ±50% 임계가 표본 전체에서 정확히 "진짜 큰
                          변동"만 잡음
진짜 가격변동(정리매매)    31/31 — 절대가 ₩2~2,000대 동전주 · 플래그일 이후
                          60일 내 거래일 0~6일(정상 종목이면 40여 일이어야
                          함) · 상장폐지 직전 정리매매(가격제한폭 없음)
                          구간의 극단적 변동으로 판단
```

전통적 "액면분할인데 조정 안 됨"(005930식) 사례는 표본에 없었다.
`UNADJUSTED_CORPORATE_ACTION`(105) vs `TRANSIENT_PRICE_SPIKE`(15) 라벨
쏠림은 `find_violations()`(build-price-a2a.py:252-261)의 "위반 후 3거래일
내 복귀 여부" 관측 창이 상장폐지로 데이터가 일찍 끊겨 구조적으로
UNADJUSTED 쪽에 쏠리는 부작용으로 보인다 — 제외 여부 자체의 오판은 아니고
세부 사유 라벨의 정밀도 문제다.

**다음 결정(🔴, GO 대기)**: `price.v1.json`의 `a2b.acceptance`
(`minTickersWithData` 600·`minTickersInAnalysisWindow` 550·
`qualityExcludedRateWarn` 5%)는 코드 없이 정찰 없이 잡은 값이라 이 실측
(19.23%)을 반영해 재검토가 필요하다. 원인이 정리매매라는 게 확정됐으니
"품질 문제라서 완화 못 한다"는 아니지만, 완화는 여전히 정책 버전 승격으로만
한다(규칙 6) — 임계를 얼마로 잡을지는 사용자 판단.

### PR-1.6 승격 + 전량 재실행 완료 (2026-08-17)

사용자가 처음 제안한 값(500/450/20%)은 방금 FAIL한 실측(504/456/19.23%)을
그대로 역산한 값이라 다음 실행이 조금만 나빠져도 다시 걸린다는 문제를
Claude가 지적했다 — 이 정책 파일 자체의 `acceptanceNote`가 "소스가 조금만
흔들려도 걸리도록" 좁은 여유를 의도적으로 둔다고 명시하는데, 실측을 그대로
승격하면 그 여유가 0에 가까워진다(교훈72와 같은 함정). 대안으로 PR-1.4가
정찰(631/572)에 적용한 것과 같은 비율(~5%·~4%)의 여유를 이번 실측(504/456)에
다시 적용해 **480/440**을 제안했고, 사용자가 이 안으로 진행하기로 확정했다.
`qualityExcludedRateWarn`은 WARN(비차단)이라 실측 그대로 20%로 승격.

```
정책      PR-1.5 → PR-1.6 (커밋 dc3e3fb)
회귀      test-price-a2b.py 규모 게이트 FAIL 픽스처(과거 500종목 하드코딩)를
          새 임계 아래로 재조정(400종목) — JS 8종·Python A2a/A2b 전체 통과
실행      price-a2b.yml run 31982876064, GO/STOP 확인 후 트리거
          collect 1h5m2s · finalize 44s · 전체 success
결과      데이터 확보 508 >= 480 (후보 1,223 − 품질 제외 122) [OK]
          분석구간(2016-01-01 이후) 460 >= 440 [OK]
          품질 제외율 19.37% <= 20% (TRANSIENT_PRICE_SPIKE 15 ·
          UNADJUSTED_CORPORATE_ACTION 107) [OK]
          실패·예외 0건 · EGW00201 0건 · EGW00316 0건
manifest  data/backfill/manifest/A2b.json (커밋 88d5756)
```

**A2b는 이걸로 완료다.** 480/440 baseline은 표본 31건 검증(전수 아님) 위에서
도출한 값이라는 한계는 `acceptanceNotePR16`에 그대로 남겨뒀다 — 다음 몇 회
실행 결과로 재확인이 필요하면 그때 다시 정책 버전을 올린다. Strategy Lab
PRIMARY 전환(A1A_A1B_MERGED 재실행, CLAUDE.md "착수 가능 ②")은 A2b 완료로
막힌 게 풀렸을 뿐 — 착수 여부는 별도의 새 🔴 결정이라 이 세션에서 답하지
않는다.

## 5. Strategy Lab — SL-2(1)(2)(3) 완료, 참고용

```
SL-2(1) MFE/MAE(2,154건 실제 체결)   research/strategy-lab/reports/2026-08-16-sl2-sequential/
                                      mfe_mae_2154_summary.json · mfe_mae_2154_trades.json
                                      STOP 1,575건·승률0%·전체손실98.92% 확인(Ultra 주장 검증됨)
SL-2(2) same-bar 수정 영향           .../sl2_2_samebar_impact.md
                                      TREND-BREAKOUT-v1: 유의미(거래+54%·CAGR/MDD 악화)
                                      5DC-v1A-P: 08-14 baseline이 수정 전 코드로 만들어짐 확정
                                      (재실행 필요, 미실행 — 별도 승인 대상)
SL-2(3) stop_distance 후보 비교      .../sl2_3_stop_distance_summary.json
                                      고정폭(median)이 상한(p75)보다 훨씬 효과적(손실 26%↓)
                                      — 전부 연구용 관찰, A2b 이전 데이터라 채택 근거 아님
```

중단됐던 equity/PnL 후보 정의 작업(`research/strategy-lab/reports/
2026-08-16-sl2-sequential/equity_candidates.json` 등)은 **보류 상태 그대로**다.
`trade_paths.pkl`(94,548건 전체 가격경로, 249MB)은 재사용 가능한 산출물로 남아있다.

### DeepSeek same-bar 원인분해 코드 리뷰 — 종결 (사용자 결정)

실험실(DeepSeek, OpenCode 경유)이 제출한
`research/strategy-lab/cause_decomposition_5dc.py`·`cause_decomposition_part2.py`
+ 검증 보고서(`research/strategy-lab/reports/2026-08-16-parallel-validation/
deepseek/5dc_v1a_p_cause_decomposition_final_verification.md`, 저장소 루트의
`reports/...`가 아니라 strategy-lab 상대경로다 — 혼동 주의)를 읽기 전용으로
검토했다.

```
same-bar/fusion 판정 로직   replay_scheduler_5dc.py의 schedule_post_fix가
                           engine/runner.py의 _schedule_portfolio(현재
                           production)와 줄 단위로 동일함을 직접 대조 확인
production 이식 필요 여부   없음 — 두 스크립트는 새 스케줄링 로직이 아니라
                           기존 production 로직(post_fix)의 복사 + 이미 고친
                           옛 버그(pre_fix, 비교용)의 재현일 뿐. 진짜 새 코드는
                           오프라인 사후 감사(분류·PnL분해)라 엔진에 안 들어감
최소 개선안(적용 안 함)     diag["sameBarTrades"]·diag["sameBarPnlSum"]을
                           run_smoke()의 diag에 5줄 정도로 추가하면 매 SMOKE
                           실행마다 same-bar 노출도를 자동 확인 가능 — 기록만,
                           미적용
검증 보고서 판정            동의 — 핵심 수치 3개(-59.11M/-33.74M/+20.96M)
                           산출물 간 교차 일치 확인, 이전 lightning 보고서의
                           "241건" 오인(다른 전략 TREND-BREAKOUT-v1의 산출물을
                           5DC baseline으로 착각)을 정확히 정정
```

**사용자 결정(2026-08-16, 이 세션에서 확정)**: DeepSeek 분석 코드는
production에 이식하지 않는다 · runner.py의 same-bar fix는 추가 수정하지
않는다(이미 DeepSeek의 post-fix와 동일 확인됨) · 5DC baseline·실험 결과는
수정하지 않는다 · same-bar 원인분해 수치(-59.11M, 92.54%)는 AI Lab 검증
결과로 참고만 한다 · `diag["sameBarTrades"]` 추가는 유용한 개선안으로
기록만 하고 지금은 적용하지 않는다. **코드·정책 무변경으로 종결.**

### same-bar 130 vs 231 — 종결 (2026-08-17)

2026-08-17 실험실 감사 둘(DeepSeek `postfix-baseline-audit`, Ultra
`postfix-integrity-audit`)이 5DC-v1A-P post-fix(1,592건) same-bar 건수를
다르게 보고했다(130건 STOP120/TARGET10 vs 231건 STOP212/TARGET19). Claude가
두 pkl을 직접 로드해 재계산 — `5dc_v1a_p_resolved.pkl`(strategyId=5dc_v1a_p,
closed=1,592)은 same-bar **130건**으로 DeepSeek·`engine/runner.py:99`
`_schedule_portfolio()`의 same-bar 정의와 정확히 일치. Ultra의 231건은
**다른 전략의 pkl 오사용**으로 판명 — `full_smoke_result.pkl`은 실제로
strategyId=`trend_breakout_v1`·closed=2,154이고 그 안의 same-bar는
241건(STOP212/TARGET19/TIME_EXIT10), Ultra 수치와 STOP/TARGET까지 정확히
일치한다(TIME_EXIT 10건만 누락). 판정식이 아니라 입력 데이터가 틀렸다.

**결론**: 5DC-v1A-P post-fix same-bar는 **130건**으로 확정. Ultra 보고서
(로컬 미커밋, `research/strategy-lab/reports/
2026-08-17-postfix-integrity-audit/`)는 원인이 확정된 것으로 충분하다고
판단해 이번 세션에서 정정하지 않는다. production 코드·§4의 A2b 트랙은
이 조사로 변경되지 않는다.

### Survivorship bias 검증 — 종결, PRIMARY 승격은 보류 (2026-08-17)

실험실(DeepSeek)이 A2b 완료 직후 `research/strategy-lab/reports/
2026-08-17-survivorship-bias-measurement/`에서 5DC-v1A-P를
`A1A_ONLY`(현재 baseline)와 `A1A_A1B_MERGED`(A1a+A1b 병합, A2b 가격 사용)로
각각 실행해 비교했다 — `engine/runner.py`는 그대로 두고 그 로직을 복제한
연구 드라이버(`run_5dc_v1a_p_merged.py`)로 실행, production 코드·정책
무수정(git status로 확인됨).

```
                A1A_ONLY        A1A_A1B_MERGED   Δ
CAGR            -9.8053%        -8.1847%         +1.62pp
MDD             -75.0002%       -74.2873%        +0.71pp
finalEquity     28,471,029      35,229,438       +6.76M
closed          1,592           1,585            -7
A1b 거래        -               119건 / -5.48M   -
```

**결론: survivorship bias는 -9.81%/-75% 성과를 설명하지 못한다.** 폐지
종목을 병합해도 나빠지지 않고 오히려 소폭 개선된다 — "표본이 생존자뿐이라
나빠 보인다"는 가설은 기각.

이 실험실 실행은 A2b가 아직 finalize FAIL이던 시점의 GitHub Actions
아티팩트(run 31952773781, 504종목)를 썼다. Claude가 이후 완료된 공식
A2b(§4, run 31982876064·manifest 88d5756·508종목)로 동일 비교를 재실행해
검증했다(`research/strategy-lab/reports/
2026-08-17-survivorship-bias-verify-508/`) — A1A_ONLY 재현은 완전 일치,
MERGED 결과도 504종목 버전과 소수점까지 동일(CAGR -8.1847%·MDD
-74.2873%·finalEquity 35,229,437.73·closed 1,585·A1b 119건/-5,475,308.18) —
추가된 4종목이 결과에 전혀 영향을 주지 않았다. 데이터 최신성 caveat는
해소됐고 결론은 확정이다.

**사용자 결정(2026-08-17)**: 이 SMOKE급 결과로 충분하다고 보고, PRIMARY
정식 승격(`config/policies/universe.v1.json` 병합 모드 확장·
`engine/runner.py`의 `A1A_ONLY` assert 완화, 둘 다 별도 🔴 결정)은 나중으로
미룬다. 코드·정책 무변경, 두 검증 산출물 모두 로컬 전용(미커밋)으로 남긴다.

## 6. 전략 검증 — 5DC-v1A-P 보류, 공유 리스크 계약 결함 발견 (2026-08-17)

A1a+A2b(508) 병합 데이터로 모의투자를 향해 가는 길에서, 5DC-v1A-P를 실제
전략으로 채택하기 전 검증 단계로 진행했다. 코드·정책 무변경, 전부 기존
산출물(pkl)과 A2a OHLC 대조로 확인했다.

### same-bar STOP 진단 — 원인은 신호가 아니라 리스크 계약의 타이밍

`5dc_v1a_p_resolved.pkl`(same-bar 130건 정본)에서 STOP 120건을 A2a와
대조 — **120/120 전부 진입가=당일 시가, 당일 저가가 스톱레벨에 실제
도달(갭 아님), stop폭이 그날 실현 고저폭보다 좁음**. 전체 1,592건은
변동폭/스톱폭 비율 중앙값 0.59(정상)인데 same-bar STOP 120건만 보면
1.86(약 3배) — 우연이 아니라 체계적 패턴.

원인: `strategies/5dc_v1a_p/policy.json`의 리스크 계약이 `stop = 2×ATR(14)[신호일
t]`, 진입은 `t+1` 시가다. **스톱 폭을 정하는 시점(t)과 그 스톱이 실제
시장에 노출되는 시점(t+1) 사이에 정확히 하루의 시차**가 있고, CCI가
오버솔드에서 막 회복하는 신호 특성상 바로 다음 날 변동성이 확대되는
경우가 잦아 신호일의 ATR이 이를 구조적으로 과소평가한다.

### TREND-BREAKOUT-v1 재현 — 같은 계약, 같은 패턴

`full_smoke_result.pkl`(2,154건 post-fix)로 동일 진단을 재현(재실행
없이 산출물+A2a만 사용):

```
same-bar STOP           212/2,154
진입가=t+1 시가          212/212 (100%)
stop=2×ATR(14)[t]       203/212(95.8%, 나머지는 장기 스무딩 누적오차 추정)
저가 실제 도달(갭 아님)   212/212 (100%)
종가 저가권 마감          156/212 (73.6%)
변동폭/스톱폭 비율        전체 중앙값 0.72 → same-bar STOP만 1.79 (약 2.5배)
```

`policy.json` 주석이 명시하듯 두 전략은 "RiskSpec/executor/portfolio/cost
contract verbatim" 공유 — **신호 로직(CCI 회복 vs Donchian 돌파)과 무관하게
공유 리스크 계약(`2×ATR[t]` 스톱 타이밍) 자체의 구조적 결함**임이 두
전략에서 독립적으로 확인됐다.

### A2b 품질제외 122종목 영향 — 분석적 추정, 효과 작아 중단

새 수집 없이 기존 `_diagnostics.json`의 위반 메타데이터(TRANSIENT_PRICE_SPIKE
15·UNADJUSTED_CORPORATE_ACTION 107, 위반일 평균 변동폭 103~126%)와 오늘
확인된 A1b(508) 코호트 실적(119건/-5.48M)을 비례 적용 — 추정 영향
약 -1.3M(병합 최종자산의 3.7%), CAGR/MDD 변화 0.5%p 미만. **원본
데이터를 그대로 넣으면 결함이 만든 가짜 변동성을 잴 뿐이라 의미 없다는
점도 확인** — 전수 복원 작업은 이 판단으로 중단.

### 사용자 결정 (2026-08-17)

```
5DC-v1A-P              보류 — 신호 로직이 나쁘다는 증거는 없음. 리스크 계약
                        (2×ATR[t] 스톱 타이밍) 결함이 손실의 상당 부분을 설명하므로,
                        다른 방안(계약 수정 등)을 마련해 재검증한 뒤 최종 판단
A2b 품질제외 122종목     보류 — 분석적 추정상 영향 작음, 추가 작업 안 함
Core 182종목 246일 백필  보류 — 저장소 어디에도 정의·근거 문서 없음(CLAUDE.md
                        한 줄뿐). 실제 의존성이 확인될 때만 재검토
exitReason 1,223종목 복원 보류 — Strategy Lab 백테스트·모의투자를 막지 않음
                        (BF-1.1의 GATE-EP-1/Primary 결론에만 필요, 별도 트랙).
                        DART list.json 표본 정찰(40건)로 복원 가능성 자체는
                        확인됐으나 dartModifyDate 앵커가 신뢰 불가(45%가 배치값)
                        라는 것도 함께 확인 — 착수 시 이 설계부터 다시 잡아야 함
PRIMARY 정식 승격        보류 유지(§5와 동일)
```

**다음 결정 지점**: 5DC-v1A-P·TREND-BREAKOUT-v1이 공유하는 리스크 계약의
스톱 타이밍을 어떻게 고칠지 설계 — 코드 변경(`policy.json`/`rule.py`)이
필요해 이번 세션 범위를 넘는다. 다음 세션이 이어받을 지점.

---

## 안전 관련 메모

- 오늘 트리거한 GitHub Actions: `calendar.yml`(성공, 2회) · `price-a2b.yml`(실패,
  2회 — 위 §4 참고, finalize가 인수 조건 실패로 산출물을 안 씀. manifest 계약대로
  정상 동작) · `fundamentals-a3c.yml`(finalize 성공).
- push 전 항상 `git fetch origin` → `git log HEAD..origin/main`로 겹침 확인 →
  겹치면 중지. 오늘 전부 겹침 없이 fast-forward만 있었다.
- 로컬 uncommitted 상태(`AGENTS.md` 수정, `research/strategy-lab/*.py` 다수,
  루트의 `compare_out.txt`·`mfe_check.txt`·`pkl_check.txt`)는 **이 세션이 만든 게
  아니다** — 세션 시작 전부터 있던 상태로 손대지 않았다.
