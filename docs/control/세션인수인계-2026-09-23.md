# 세션 인수인계 — 2026-09-23 (새벽) · Stine 조용한 재진입 REJECT · RV20 30분 지연 원인 수정

이전: `세션인수인계-2026-09-22-b.md`. 작성 2026-09-23 새벽(사용자 취침). 정책·config·criteria 변경 없음. 전부 push 완료.

## 0. 다음 세션이 먼저 할 것

1. **09-23 아침 확인**(09-22-b §0 그대로 + 하나 추가):
   - ★ **RV20 이 09:05 에 시작해 몇 분 안에 끝나는지**(`journalctl -u rv20-futures-paper-order.service --since today`) — 아래 §2 수정의 첫 실물 검증.
     여전히 30분이면 `/home/ubuntu/collector/research/strategy-lab/.cache/kospi200_daily/_state.json` 의 `empty` 를 본다.
   - 07:30 미국 스냅샷 · 09:05 크립토(5종 BTC·DOGE·PEPE·SHIB·SUI 청산) · `systemctl --failed`.
   - 장중 급등락 알림이 `[object Object]` 없이 오는지 — 수정(40117f9d, 09-22 20:48) 뒤 첫 실행은 오늘 장중이다(마지막 실행 18:23 은 수정 전).
   - 국내 장중 모의 1주 주문(`EXCG_ID_DVSN_CD=KRX`) · 미국 22:30 후 SOXL `AMEX` 주문 — 사용자 결정. 통과하면 `live.tr_ids_reviewed: true`.
2. 10-01 월간 점검(CLAUDE.md 절차) 그대로.

## 1. Stine 조용한 재진입 — REJECT·라인 종결

- Jesse Stine《Insider Buy Superstocks》의 진입 구조 하나("돌파를 쫓지 말고 돌파 뒤 조용해졌을 때 산다")만 떼어 KR 주식 전체(폐지 포함)에서 검증.
- 순서: 사건 수 탐침(수익률 없음) → 사전등록 30b5b240 → 계산 코드 fc94dae6(실행 전) → 버그 수정 9ddf12ec·9d367a60(pandas `.info` 이름 충돌, 결과 출력 전 예외) → 결과 9bef7c1d.
- 결과: 셀 A(돌파 추격)·B(2~6주 뒤 조용한 진입) **둘 다 REJECT**. B TRAIN 정보 +26bp·t 0.19(바닥선 2.26), OOS gross 음 — 비용이 아니라 신호 문제.
  A 는 OOS TEST 정보 −555bp·t −7.4. B−A OOS +375bp 는 "덜 나쁨"이라 기록 전용. 민감도 2변형도 동일. 청산은 99% 가 10주선 이탈, 평균 보유 5~7주.
- `findings/stine-quiet-entry-results-2026-09.md`. CLAUDE.md '안 한다'에 한 줄. 실적·내부자 필터를 그 위에 쌓지 않는다, 추격 음수를 공매도로 뒤집지 않는다.
- ★ **데이터 사실**: A2a·A2b 가격은 **수정주가, 거래량은 원값**(삼성전자 2018-05 분할에 거래량 23만→3,956만). 거래량 조건을 쓰는 연구는 기업행사 제외가 필요하다.
  A4 수급의 `buyAmount.전체`(실제 거래대금)로 원주가/수정주가 계수를 만들 수 있지만 **A4 에는 폐지 종목이 없다**(생존편향 — 필수 조건으로 쓰면 안 됨).
- 출처 메모: ChatGPT 가 인용한 책 요약 링크(fliphtml5·anyflip·studylib)는 무단 사본이다. 저장소에는 원문 규칙·사례를 옮기지 않았다.

## 2. RV20 — 매일 30분 지연 원인 수정 (5e3eddfa)

- 증상: 09-14 가동 첫날부터 매일 09:05 시작 → 09:35 종료, 주문이 설계보다 30분 늦게 나감.
- 원인: 주문 앞단 `collect_kospi200_daily_krx.py` 가 휴장일(빈 응답)을 완료로 기록하지 않아 매 실행마다 2010년 이후 휴장일 전부를 재조회하고,
  빈 응답 8건마다 60초 대기(로그의 "한도 추정 - 60초 대기"가 전부 과거 휴장일).
- 수정: 7일 넘게 지난 빈 응답만 `state["empty"]` 에 기록하고 재조회 안 함. 최근 7일은 늦은 게시 가능성 때문에 계속 재조회. 로컬 2회 실행 시험 통과.
- VM: `git pull` 후 수집기만 1회 수동 실행(주문 없음, 캐시만)해 휴장일 기록을 미리 채웠다 — 결과 §2-부록.
- 되돌리기: revert, 또는 VM `_state.json` 의 `empty` 삭제(그 날짜들을 다시 조회).

## 3. 실수 1건 — `git commit -a`

- 9ddf12ec 에 `-am` 을 써서 사용자 로컬 변경 `research/strategy-lab/data/market-regime/_manifest_fred_extended.json` 이 딸려 push됐다.
  revert 커밋 eaa1fd88 로 원격을 원상태로, 로컬 작업 트리 변경은 복원(`M` 그대로). 메모리에 "commit -a 금지" 기록.
- `git stash list` 의 autostash 3개(fred·macro.json)는 이전 세션 것 — 손대지 않았다.

### §2-부록 — VM 사전 실행 결과 (2026-09-23 새벽)

- 1회차(30분, 옛 동작과 같음): `done` 4,116일 · 확정 휴장 `empty` **245일**(연 9~19일 — 2024·2025 는 임시공휴일·연말 휴장 포함, 2026 은 6-03 지방선거·7-17 제헌절 포함).
- 2회차: **1.5초**, 새 조회 없음. → 오늘 09:05 RV20 은 몇 분 안에 끝나야 정상.
