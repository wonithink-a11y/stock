# 세션 인수인계 — 2026-09-11 (9/14 프로브 준비 + 구멍 감시 신설)

앞 문서(`세션인수인계-2026-09-10-c.md`)의 "남은 것" 중 **손이 닿는 두 건**을
했다. 오늘은 금요일 — **09-14 프로브 전 마지막 영업일**이다.

커밋 3개, 전부 push.

---

## 0. 다음 세션(= 09-14 월요일)이 먼저 할 것

### ★ 2026-09-11 추가 — 사실을 잡았다. 일이 두 줄로 줄었다

두 세션 동안 못 한 웹 확인이 됐다. **`WebSearch`·`WebFetch` 는 여전히 죽어 있고,
브라우저 패널(`Claude_Browser` navigate + get_page_text)이 살아 있다.** 앞으로
웹이 필요하면 그쪽으로 간다.

```
정규장        09:00 ~ 15:30    ★ 안 바뀐다
종가단일가    15:20 ~ 15:30    그대로
시간외종가    15:40 ~ 16:00    그대로
애프터마켓    16:00 ~ 20:00    신설 (시간외접속매매 · 즉시체결)
                               기존 시간외단일가(16:00~18:00) 폐지
ETF·ETN       제외
다음날 기준가  여전히 15:30 종가
프리마켓      2027년 말로 연기
```

출처는 뉴스 다중 일치(연합인포맥스·뉴시스·비즈워치·금강일보 등, 2026-09-09~10).
**뉴스로 🔴 을 바꾸지 않는다는 원칙은 그대로다** — 아래 프로브가 실측으로 확정한다.
다만 실측이 답해야 할 질문이 줄었다.

`세션인수인계-2026-09-10-b.md` §4 의 "답이 크게 갈리는 질문 넷" 중 셋이 닫힌다:

| 질문 | 답 |
|---|---|
| 정규장이 20:00 까지인가 | **아니다.** 애프터마켓 신설이다 |
| 종가단일가가 어디로 가나 | **안 움직인다** |
| 일봉 `close` 정의가 갈리나 | **안 갈린다**(기준가 15:30 종가 유지) → §6 '경계 기록' 도 닫힌다 |
| KIS 분봉이 연장분을 주나 | **여전히 모른다** ← 유일하게 남은 실측 |

그러므로 **`cursorSeed 153000` · `sessionMinutes 381` 은 그대로 맞고 🔴 정책
개정안은 아마 필요 없다.** 타이머도 전부 그대로다 — `minute-collect`(16:10·17:40) ·
`paper-trading-poll`(15:30 컷) · `promote-minute-manifest`(19:30) · `collector-pull`(06:30).
09-10 에 15:30 으로 좁힌 판단은 뒤집지 않는다.

### 남은 진짜 위험 하나 — `volume` (새로 생긴 항목)

`close` 는 안 바뀌지만 **일봉 거래량·거래대금이 16:00~20:00 체결을 합산하는지는
모른다.** 합산하면 16:40~18:20 에 도는 워크플로들이 **미완성 스냅샷**을 찍는다 —
오류도 갭도 없이(교훈75·81 과 같은 모양). 기존 시간외단일가(18:00)로도 이미
같은 구멍이 있었을 수 있고 연장이 그것을 2시간 키운다.

뉴스로는 못 푼다. **같은 일봉을 두 번 떠서 뺀다:**

```bash
python -c "from pykrx import stock; print(stock.get_market_ohlcv('20260914','20260914','KOSPI').loc[['005930','000020']])"
```

09-14 **16:40 과 20:30 에 한 번씩.** 같으면 아무것도 안 옮긴다. 다르면 옮길 대상은
**KRX 일봉을 읽는 것만** — `etf-etn-daily`(16:50) · `build-etf/etn-price-history`
(17:10·17:20) · `watchlist-daily-update`(17:30) · `stock-flows-update`(17:35) ·
`market-flows`(18:20) · `price-a2a`(19:00). `daily-analysis` · `macro` ·
`paper-trading-ui` · 분봉 계열은 무관하다.

---

```bash
python scripts/probe-minute-kis.py --session-bounds --date 2026-09-14
```

장 마감 후에 돌린다. 출력의 **판정** 한 줄이 🔴 정책 개정안의 입력이다.

| 판정 | 다음 행동 |
|---|---|
| `정책 유효` | 연장이 정규장에 안 왔다. 정책 손대지 않는다. 타이머도 그대로 |
| `정책 개정 필요` | `cursorSeed`·`sessionMinutes` 개정안 → 🔴 승인 → 타이머·크론(앞 문서 §4) |
| `판정보류` | seed 가 거부됐다. **끝시각을 못 본 것이지 없는 게 아니다**(교훈57). 판정 금지 |
| `측정실패` | rt_cd·dateHonored 를 보고 원인부터 가른다 |

순서는 앞 문서 그대로다: **프로브 → 정책 개정안(🔴) → 타이머·크론 → 경계 기록.**

---

## 1. 프로브가 자기 맹점을 갖고 있었다 (커밋 `45e1136`, 🟡)

`probe-minute-kis.py` 의 `[10] 하루 전량 수집` 은 커서를 **정책과 같은
`"153000"`** 에서 시작해 거꾸로 넘긴다. 마감이 20:00 으로 밀려도 밀린 구간을
**아예 요청하지 않으므로** 오류도 갭도 없이 "끝시각 153000" 으로 정상 보고한다.

즉 **09-14 에 이 프로브를 그대로 돌렸으면 연장 여부에 답을 못 냈다** — 수집기의
눈가리개를 그대로 쓴 도구로 그 눈가리개를 검사하는 꼴이다(교훈72·81).

`--session-bounds` 를 새로 냈다. seed 를 `235959` 로 두고 거꾸로 페이징한다.

### 오늘 실측 기준선 (2026-09-09 거래일)

```
005930   090000 ~ 153000   캔들 381   pages=5     ← 정책 sessionMinutes 381 과 일치
000020   090000 ~ 153000   캔들 254   pages=4
판정     정책 유효 — 실측 끝시각 153000 가 cursorSeed 153000 안에 있다
```

★ 중요한 건 숫자가 아니라 **seed `235959` 가 거부되지 않았다**는 것이다. 후퇴
없이 정상 응답했다 — 그러므로 09-14 에 15:30 이후 봉이 있으면 **보인다.**
거부됐다면 `판정보류` 로 떨어지고 그 사실이 출력에 남는다.

출력은 `data/backfill/_probe-session-bounds.json`. 로컬 실행이므로 **커밋하지
않는다**(규칙 4) — 오늘 것은 세션 스크래치패드로 옮겼다.

## 2. "저장소에 구멍이 생기면 누가 아는가" — 앞 문서 §7 미결을 닫았다 (커밋 `6a0ec9d`, 🟡)

앞 문서가 남긴 질문: *"VM 실패는 애초에 Actions 에 안 나타나므로 이 목록으로는
이번 사고를 못 잡았을 것이다 — 별도 채널이 필요한가? 미결."*

**별도 채널을 만들지 않았다. 감시 대상을 바꿨다.**

```
실패를 감시한다   →  그 실패 경로만 잡는다. VM 은 Actions 에 안 나타난다
결과를 감시한다   →  원인과 무관하게 잡는다
```

`promote-minute-manifest.py` 에 `repo_holes()` 를 넣었다 — 최근 거래일 중
저장소에 manifest 가 없는 날. VM 죽음·수집 FAIL·OCI 업로드 중단·승격 거부가
전부 같은 자리에서 붉어진다.

### ★ `승격할 것 없다`(exit 0) 경로에도 붙였다

이번 사고에서 **6일간 녹색이던 바로 그 경로다.** 승격 루프 뒤에만 붙였으면
같은 사고를 또 놓친다.

### 거짓 경보와 영원한 붉음을 둘 다 피하는 두 손잡이

```
grace = 1      가장 최근 거래일은 아직 안 올라왔을 수 있다 (VM 18:00 → 승격 19:30)
               → 하루 늦게 알리는 대신 거짓 경보를 안 낸다
lookback = 10  못 메우는 하루가 영원히 참인 경고가 되지 않는다
               (CLAUDE.md: 영원히 참인 경고는 모두가 무시하는 법을 배운다)
캘린더 없음     구멍 검사를 건너뛴다 — 모르는 것은 0이 아니다(교훈57)
```

캘린더가 최근을 못 덮으면 창이 그만큼 일찍 끝난다 — 거짓 경보 쪽이 아니라
침묵 쪽으로 틀린다. 의도한 비대칭이다.

### 검증

```
사고 재현    09-04·09-07 실종을 09-08 시점에 집어낸다   (실제 발견은 09-10, 수동 ls)
오늘 실측    저장소 구멍 0건
Actions 실물 run 34545549816  green · "승격할 것 없다" · 구멍 줄 없음
             ('[구멍검사 생략]' 도 안 찍혔다 = 캘린더를 읽고 실제로 쟀다)
회귀         test-promote-minute-manifest.py  19 → 26
```

### notify-failure 감시 목록 4 → 5

붉어져도 아무도 안 보면 같은 일이다. `promote-minute-manifest` 를 넣었다.
'계속 실패'가 억제 규칙에 걸리는 문제는 **구멍에는 해당 안 된다** — 구멍은
녹색 뒤에 처음 붉어지므로 전이가 있고, 첫날 한 통이 간다(그 뒤는 억제 = 의도).

`TELEGRAM_*` 는 2026-09-09 등록 완료다. 워크플로 주석이 "시크릿이 없다"로
낡아 있어 함께 고쳤다.

## 3. `--days 3` 룩백은 **일부러 안 고쳤다**

앞 문서 §7 이 미수정으로 남긴 항목이다. 넓히지 않기로 판단했다.

`run-minute-daily.py` 의 `todo` 는 **오래된 것부터** 돌고 `--budget-minutes`
가 있다. 창을 넓히면 **못 고치는 옛 하루가 매 실행의 예산을 먼저 먹어 오늘치를
굶긴다** — 지금보다 나쁜 고장이다. 창 크기를 얼마로 하든 그 날짜를 지나면
다시 조용해지는 것도 그대로다.

그래서 수집 창이 아니라 **결과(저장소)** 쪽에서 잡기로 했다(§2). 구멍이 하루
만에 텔레그램으로 오면 사람이 `--date` 로 지목한다 — 앞 문서에서 실제로 그렇게
복구했고, 없던 건 손이 아니라 **신호**였다.

되돌리려면: `repo_holes` 호출 두 군데를 지우거나 `lookback=0` 으로 둔다.

## 4. 웹 확인 — 2026-09-11 에 됐다 (§0 참고)

원문 기록: `WebSearch`·`WebFetch` 는 앞 세션과 같은 라우팅 오류로 여전히 죽어 있다.

```
There's an issue with the selected model (cc/claude-haiku-4-5-20251001)
```

**하지만 브라우저 패널은 돈다.** `mcp__Claude_Browser__navigate` 로 검색 URL 을 열고
`get_page_text` 로 읽으면 된다 — 이 경로로 09-14 제도 내용을 확정했다(§0).
"이 환경에서 웹이 안 된다"는 앞 두 문서의 서술은 **틀렸다. `WebSearch` 만 안 된다.**

## 5. 회귀

```bash
export PYTHONIOENCODING=utf-8
for f in scripts/test-*.py; do python "$f"; done
for f in scripts/test-*.js; do node "$f"; done
```

전체 통과. rebase 후 재검증까지 두 번 돌렸다(push 가 non-fast-forward 로
한 번 거절됐다 — 상대는 전부 Actions 생성 데이터 파일이라 겹침 없음, rebase).

## 6. 남은 것

- **9/14 프로브 + 일봉 volume diff** — §0. 시한 있음. 정책 개정안·타이머는
  **아마 불필요**(정규장 불변 확인). 프로브가 '정책 유효'면 그대로 닫는다
- ~~경계 기록(앞 문서 §4 계획 4번)~~ — **닫혔다.** 일봉 `close` 는 기준가가
  15:30 종가로 유지되므로 정의가 안 갈린다. `volume` 쪽은 위 diff 로 넘어갔다
- **2026-10-01 workflow_run 실물 검증** — 변동 없음
- **`_manifest_fred_extended.json`** — 작업트리에 그대로 둔다(판단 유지)
- 앞 문서 §7 이월 나머지: A2a 일봉 정의 경계 · LW3 4건 · `fully_filled`
  부분체결 구멍 · A2a 증분화(🔴) · `entry_date` · `market_comparison_kr_2026_09.py`
  docstring · QCOMP · PIT 시총 이력 · KSIC→투자테마 · A3c staleness

## 7. 작업트리

```
 M research/strategy-lab/data/market-regime/_manifest_fred_extended.json
```

§6 참고 — 의도적으로 둔다.
