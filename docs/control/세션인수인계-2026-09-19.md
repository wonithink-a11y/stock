# 세션 인수인계 — 2026-09-19 (무한매수법 취소·재접수 구현 · 실험실 재점검)

`세션인수인계-2026-09-18.md`(부분익절 재검토·PBR/EY 🔴 결정) 이후 세션.
오늘은 (1) 낡은 CLAUDE.md 문구 2건 정정, (2) 무한매수법 매일 자동 실행의 선결 조건이던
취소·재접수 로직 구현, (3) 실험실 KEEP/HOLD·단기 실험·외부 전략 재점검이다.

---

## 0. 다음 세션이 먼저 볼 것

### ① 무한매수법 자동 실행 — 코드는 끝, **남은 건 사용자의 VM 작업뿐**

사용자가 "시간 날 때 진행"하기로 했다. Claude는 VM 접근 권한이 없다(설계상).

```
1. 복사   ~/collector-venv/infbuy/ 로
           research/strategy-lab/data/leveraged-etf/_rules.local.json
           state/TQQQ_vts.json · state/SOXL_vts.json  → ~/collector-venv/infbuy/state/
           ★ 상태 파일을 빼먹으면 T 가 0 에서 새로 시작한다(주문 수량이 어긋난다)
2. 설치   git pull → deploy/infinite-buying-vts.{service,timer} 등록·timer 활성화
3. 확인   dry-run 로그(~/collector-venv/logs/infinite-buying-vts.log)에 DRY 주문 줄
           (pandas·pyarrow·yfinance 가 VM venv 에 없으면 여기서 ImportError)
4. 켜기   sudo systemctl edit infinite-buying-vts.service → --execute 추가 (사용자 몫)
```

켠 뒤 첫 며칠은 다음 날 미체결 조회로 **지정가가 당일물인지** 확인한다(아직 미확정 —
09-12 두 주문이 확인 시점에 이미 전량체결이었다). 로그에 `잔존 미체결` 줄이 찍히면
취소 로직이 실제로 동작한 것이다. **실제 취소 호출은 아직 한 번도 안 돌려봤다**
(잔존 주문이 없어 시험 대상이 없었다) — 첫 실행에서 처음 검증된다.

### ② 낡은 CLAUDE.md 문구 정정 2건 (같은 실수 반복 주의)

- RV20 "오늘 첫 실주문 예정" → 09-14 첫 실주문 성공(주문번호 0000001627)이 이미
  09-14 인수인계에 있었다. 09-18 인수인계만 읽고 "공백"이라고 단정했다가 정정.
- "다음은 별도 연구(Path Dependence/Execution Reality)" → 3갈래 전부 09-12 에 완주
  (`-12-d.md`). **인수인계가 여러 개면 최신 하나만 믿지 말고 같은 날짜의 -b/-c/-d 까지 본다.**

### ③ 실험실 재점검 결과 → §3

---

## 1. 커밋 (오늘, 전부 push 완료)

```
a68a131  docs(claude.md): RV20 선물 첫 실주문 성공(09-14) 반영
ba131d6  docs(claude.md): 무한매수법 '다음 연구' 3갈래는 09-12 완주
c605be5  feat(infinite-buying): vts 취소-후-재접수 + 상태 경로 env 분리
3ecedc4  feat(deploy): 무한매수 슬리브 vts 일일 실행 systemd 유닛 (dry-run 배포)
c36fd40  docs: 세션인수인계 2026-09-19 + CLAUDE.md 무한매수 항목
bbcd55c  research(futures): 장중 신호 스터디 사전등록
(선물 스터디 결과·이 문서 갱신은 후속 커밋)
```

## 2. 무한매수법 취소·재접수 — 설계 요약

- `KisVtsOverseasClient.cancel(symbol, order_no, qty, dry_run=True)` — VTTT1004U,
  `RVSE_CNCL_DVSN_CD=02`·단가 `"0"`. 스펙은 KIS 공식 예제(open-trading-api) 원문 확인.
  기본 dry-run, 재시도 없음(주문과 같은 원칙). 취소 수량은 잔량(원수량−체결분).
- `run_vts`: **취소 → 재조회로 미체결 0 확인 → 계좌 조회 → 재계획 → 재접수.** 순서가
  계약이다(취소 전에 계좌를 읽으면 그 사이 체결분이 T 에 안 잡힌다). 취소가 실패했거나
  미체결이 남으면 그 종목은 그날 주문을 **안 낸다**(중복 매수보다 하루 건너뛰는 게 낫다).
- `INFBUY_STATE_DIR` — 상태 파일(git 추적 파일)을 VM 에서 고치면 다음 `git pull
  --ff-only` 가 막히므로 VM 은 저장소 밖에 둔다.
- 회귀: 러너 selftest 22→31(가짜 브로커로 동작 검증), 클라이언트 14→20,
  strategy-lab 전체 pytest 296건 통과. 읽기 전용 dry-run 으로 실계좌 미체결 조회 경로 확인.
- 유닛은 **dry-run 으로 배포**(`--execute` 없음). 21:30 KST 평일 — 미국 개장(서머 22:30,
  표준 23:30 KST) 전. 미국 휴장일엔 직전 종가로 다시 계획하지만 다음 실행이 취소·재접수한다.
- 자동 모드 분류기가 systemd 유닛 작성을 처음엔 막았다(Bash heredoc). 사용자가 (a)안으로
  재승인한 뒤 Write 도구로 만들었다. 우회가 아니라 사용자 GO 후 재시도.

## 3. 실험실 KEEP/HOLD · 단기 실험 · 외부 전략 재점검

### 3.1 KEEP/HOLD 중 더 검증할 것

`ui/data/findings.json` 326건: KEEP 14 · HOLD 25 · REJECT 83 · UNCLASSIFIED 204.

- **새로 열 만한 것은 없다.** HOLD 는 EY(보류 확정)·PBR+EY 결합·DRAM 라인(사용자 동결)·
  부분익절(정황상 세그먼트 잡음)·크립토 표본부족·내부자거래 표본부족이고, 전부 이미
  "재개 조건 없음/새 가설 필요/시간 필요"로 기록돼 있다.
- **KEEP 의 실질은 PBR combined 하나**(연도 집중 45.7%로 완화됐으나 완전분산 아님) —
  이미 production GO.
- 값싼 확인 후보 하나: 부분익절 "세그먼트가 잡음을 만든다" 가설(전체 재면 VALID 악화 1/10).

### 3.2 단기·초단기 — 개선 여지

**구조적 사실**: KR 초단기는 신호를 여러 번 찾았고 **전부 비용에서 죽었다.**
오프닝 페이드 gross +34.6bp/일 vs 최소 드래그 60bp, VWAP 회귀는 **실재하지만 최대 14bp**
(비용의 1/5), DMA+Stoch50·ORB 방향·갭 리버설(TEST 소멸)·모멘텀(부호가 반대, 반전) 전부.
5개 외부 제안(ST-1~5, ChatGPT) 중 3개는 이미 연구됨, VWAP 은 이번에 REJECT.

남은 여지 두 가지(1번 미착수·사용자 GO 대기, 2번은 실행해서 닫힘):

1. **CAND1(PMCRASH_REVERSAL, 오후급락→익일 09:35 청산)의 forward OOS.** 분봉 연구가
   "조건부 채택"한 유일한 단기 후보이고 파라미터가 train-only 로 고정돼 선택편향이
   통제돼 있다(TEST +0.37%/일 t=3.9, 20bp 비용 후). 연구창은 2026-08-21 에 끝났는데
   저장소 manifest 는 이미 273일 → **연구 이후 ~20거래일이 진짜 표본외**다. 단
   **로컬 미러(`.cache/minute_raw`)가 08-21 에서 멈춰 있다**(253일) — 신규분은 OCI 에서
   내려받아야 한다(사용자 몫의 대량 다운로드, 확인 필요). 표본이 작다(~20일) — 부호와
   크기가 유지되는지만 볼 수 있고 채택 근거로는 부족.
2. **비용 구조 회피 — KOSPI200 선물 분봉의 장중 전략(ORB·갭·VWAP).** → **같은 날 실행, 전부 REJECT**
   (아래 §3.2.1). 이 항목은 닫혔다.

#### 3.2.1 선물 장중 신호 스터디 결과 (사전등록 bbcd55c → 결과 findings)

`findings/futures-intraday-signal-preregistration-2026-09.md`(결과 전 커밋) →
`findings/futures-intraday-signal-study-2026-09.md` · 스크립트 `futures/futures_intraday_signal_study.py`
(`--selftest` 9/9). 250일·10셀·TRAIN/VALID/TEST 150/37/63·왕복비용 ≈1.4bp·난수 바닥선 |t| 2.86.
**10셀 전부 REJECT, INFORMATION 조차 0개**(TRAIN 최대 |t| 1.36).

- "비용 벽이 원인" 가설 **미지지** — 비용이 1/40 인 선물에서도 gross 신호가 안 보인다.
- 일 단위 셀은 검출력이 낮다(최소검출효과 ≈40bp/일) — "효과 없음"이 아니라 "40bp 급은 없다".
- VWAP 페이드는 표본이 충분한데(663건) gross ≈1.5bp 로 비용과 같은 자릿수, TEST 에서 부호 반전.
- 정정: 제안할 때 "선물 장중은 미검증"이라 했지만 Stage 1 이 5m 돈치안/MA 를 이미 돌렸었다
  (격자 최고값·단일 창이라 표본외 근거는 아님). 이번 스터디는 고정 규칙 10개로 다른 종류의 검정이다.
- 재개 조건: 표본 2배 이상 또는 횡단면(종목 대비 선물 상대) 새 가설. 셀 추가·임계 조정은 재개 사유가 아니다.
  선물 분봉 수집은 로컬이고 2026-09-03 에서 멈춰 있다.

### 3.3 외부(GitHub·커뮤니티·전문가) 전략 가져오기

- GitHub 저장소 검색(API, 2026-09-19): ORB·VWAP 평균회귀·intraday 평균회귀·갭 페이드로
  검색. 결과는 **별 0~16개짜리 개인 백테스트**이고 전부 미국/선물/크립토 대상, 한국
  비용구조를 고려한 것은 0건, 한국어 검색은 결과 0건. **우리가 이미 검증한 계열의
  재구현**이라 새 후보가 아니다(ORB 6건·VWAP 3건·평균회귀 6건).
- 공개 문헌의 대표적 주장(Zarattini 등 "Stocks in Play" ORB, SPY intraday momentum)은
  **미국·저비용 전제**다. 우리 실측은 KR 장중이 모멘텀이 아니라 **반전**(IC −0.0589,
  t=−10.4)이라 방향이 반대다. 이 문헌 내용은 기억 기반이고 이번 세션에서 원문을
  확인하지 않았다 — 인용하려면 원문 확인이 먼저다.
- 결론: **외부 전략을 그대로 이식할 근거는 없다.** 가져올 만한 것은 전략이 아니라
  **"저비용 시장(선물)에서 같은 가설을 재본다"는 관점**이었고, 그건 §3.2.1 에서 시도해 REJECT 로 닫혔다.

## 4. 작업트리

```
research/strategy-lab/data/market-regime/_manifest_fred_extended.json   자동 갱신분 — 손대지 않음(여덟 문서째 같은 판단)
research/strategy-lab/eval_f2_shadow.py · run_f2_shadow.py · futures/stage5_4_*·stage6_1_*   OpenCode 병행 작업 — 손대지 않음
그 외 클린. 이 세션 산출물 전부 커밋·push 완료.
```
