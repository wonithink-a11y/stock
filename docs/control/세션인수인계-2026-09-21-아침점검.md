# 2026-09-21 아침 점검표 (전날 09-20 세션이 남김)

**이 문서는 정본이 아니다** — 정본은 CLAUDE.md 상태 블록이다. 여기 있는 건
"09-20 에 켜 둔 것들이 내일 처음 도는데, 그때 뭘 확인해야 하는가" 하나뿐이다.
확인이 끝나면 이 파일은 지워도 된다.

★ **접속은 `ssh stock-new` 다**(hostname 이 `stock`). `ssh stock` 은 폐기 시험 중인
옛 대시보드 기계로 간다 — 파일이 없다고 나오지 오류가 아니라서 고장으로 오진하기 쉽다.

---

## ★ 09-20 저녁에 사고 하나를 고쳤다 — 내일 이것부터 본다

어제(09-20) 배선 중 유닛 **3개가 치환 안 된 `@REPO@` 로 덮여** `bad-setting` 이 됐다.
`minute-collect.timer` 가 16:11 발화에서 `Failed to queue unit startup job` 으로 죽어
**다음 스케줄이 사라져 있었다**. 같은 날 저녁에 치환·복구했고 셋 다 1회 실행으로 검증했다.

```
collector-pull(06:30) · minute-collect(16:10·17:40) · minute-oci-upload(18:00)
```

내일 이 셋이 실제로 도는지가 첫 확인 대상이다.

```bash
systemctl --failed --no-pager
tail -8 ~/collector-venv/logs/minute-collect.log
```

★ **어제 만든 서비스 `OnFailure` 는 이 사고를 못 잡았다** — 유닛 파일이 깨지면 로드가
막혀 실행 자체가 없고, 실패하는 것은 타이머 쪽이다. 그래서 09-20 에 타이머 7개에도
`OnFailure` 를 붙였다(발화함을 실측 확인, 커밋 040157d·9f28964). 알림 이름이
`minute-collect.timer` 처럼 `.timer` 로 오면 그건 이 종류의 실패다.

★ 분봉이 09-18 까지인 것은 **정상이다** — 09-19 는 토요일이다.

## 09:05 KST — 두 개가 나란히 돈다

### ① 크립토 상승충격 슬리브 — **첫 청산이다**

09-20 에 처음 샀다(KRW-AVAX 7.262164 @14,620 · KRW-INJ 9.182736 @11,300).
`max_holding_sessions=2` 이고 `sessions_held` 가 1 이므로 **내일 09:05 에 둘 다
시간청산**되고, 같은 실행이 09-20 일봉 기준 새 신호를 잡는다.

```bash
tail -40 ~/collector-venv/logs/crypto-upshock-paper.log
```

보여야 할 것: `매도 제출` 2건 → `매도 체결 확인` 2건 → 포지션 삭제. 그 뒤 새 신호가
있으면 `SIGNAL -> INTENT`.

★ **이 청산 경로는 합성 테스트로만 확인됐다**(`test_paper_engine_fractional.py` 의
`test_fractional_position_exits_for_its_full_quantity`). 실물 첫 회차다.
소수 수량이 전량 팔리고 포지션이 **삭제**되는지가 핵심 — 잔량이 남아 `OPEN` 으로
되돌아오면 그건 결함이다.

판정은 신호일 60일 도달 후 1회. 내일이 2일차다. 중간 숫자로 판정하지 않는다.

### ② RV20 선물 — **실패 후 첫 실행이다**

09-18(금) 09:36 에 KIS 잔고조회 타임아웃으로 죽었다(`ExecMainStatus=1`). 실패
지점이 주문 직전이라 **그날 주문은 안 나갔다**. 주말엔 안 돌았으니 내일이 복구 첫 회다.

```bash
tail -30 ~/collector-venv/logs/rv20-futures-paper-order.log
```

## 21:30 KST — 무한매수법 해외 슬리브 (첫 자동 실행)

09-20 에 VM 배선을 마쳤고 **dry-run 상태**다(`--execute` 없음). 내일 21:30 이
타이머로 도는 첫 회차다.

```bash
tail -30 ~/collector-venv/logs/infinite-buying-vts.log
```

수동 실행 때 나온 값: TQQQ 보유 15주 평단 $71.42 **T 0.86** · SOXL 8주 $123.58
**T 0.79**. **T 가 0 으로 보이면 상태 파일을 못 읽은 것이다** — 그게 이 배선의
유일한 실질 위험이었다(`INFBUY_STATE_DIR=~/collector-venv/infbuy/state`).

주문을 켜는 것은 **사용자 몫**이다(`systemctl edit` 로 `--execute` 추가).
Claude 는 이 스위치를 켜지 않는다.

## 아무 때나 — 실패 알림이 살아 있는지

09-20 에 신설했다. 감시 7개(rv20 · 크립토슬리브 · paper-trading-poll ·
minute-collect · minute-oci-upload · collector-pull · infinite-buying-vts)가
실패하면 **텔레그램 그룹 `주식 알림`** 으로 3줄이 간다. 1:1 대화에는 콘텐츠
(뉴스·공시·일일분석·장중급등락·로그인)만 온다.

**그룹이 조용한 것이 정상이다.** 뭔가 뜨면 그게 곧 고장 신호다.
Actions 실패(`notify-failure.yml`)도 같은 방으로 온다.

```bash
systemctl list-timers --no-pager | grep -E "crypto-upshock|rv20|infinite-buying|paper-trading"
```

## 거래일에만 — 분봉 애프터마켓 실측 2건 (미착수)

09-14 거래시간 연장으로 애프터마켓 16:00~20:00 이 생겼다. 남은 실측은 둘이다
(CLAUDE.md "다음" 참고) — **KIS 분봉이 연장분을 섞어 주는가** · **일봉 volume 이
애프터마켓을 합산하는가**. 방법은 16:40 과 20:30 두 번 떠서 diff 하는 것이라
거래일이 필요하다. 09-21 이 첫 기회다.

## 건드리지 않는 것

- 작업트리의 `_manifest_fred_extended.json` · `ui/data/macro.json` — 자동 갱신분.
- 미커밋 OpenCode 산출물(`eval_f2_shadow.py` · `stage5_4*` · `stage6_1*`) — 사용자가
  직접 위임한 실험이다. 손대지 않는다.
- `stock-monitor` VM — 09-20 에 `stock.service` 를 정지했다(폐기 시험). 2주쯤 아쉬운
  게 없으면 삭제. 되살리려면 `sudo systemctl enable --now stock.service`.
  앱 백업은 PC 의 `C:\Users\User\vm-backups\` 에 있다(저장소 밖, KIS 키 포함).
