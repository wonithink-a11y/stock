# 세션 인수인계 — 2026-09-26-b · 워뇨띠 후속 전략화 3건 · 무한매수법 크립토 스크리닝

같은 날 다른 창의 인수인계: `세션인수인계-2026-09-26.md`(알림·us-pit·해독 카드 등 — 이 문서와 겹치지 않는다).
정책·config/policies·criteria 변경 없음. 커밋은 전부 push 완료. **결론: 이 세션의 연구 4갈래 전부 닫힘 — 이어서 할 일 없음.**

## 0. 다음 세션이 알아야 할 것

- **워뇨띠 라인은 다시 열지 않는다.** 09-22 탐색(REJECT) 뒤 사용자가 "오퍼스 5.5로 다시" 요청 → 같은 데이터 재분석은 데이터 마이닝이라 권하지 않았고,
  대신 **시험한 적 없는 가설만** 사전등록으로 돌렸다. 셋 다 REJECT. CLAUDE.md 워뇨띠 '안 한다' 항목에 한 줄로 반영(a1fe3fbc).
- **무한매수법 크립토**: 09-12 기각을 18변형·붕괴 코인까지 넓혀 재확인 — 기각 유지, 라인 닫음.
- 재론 조건(워뇨띠): 주문 접수·취소 기록 또는 틱·호가 데이터. **BitMEX 공개 틱·호가(public.bitmex.com, 2014~, 하루 40~160MB, 전체 ~100GB)는 존재를 확인했다.**
  다만 체결 자체에서 돈이 안 났고(체결가 기준 60분 +0.6bp) 성패는 보유 판단(AUC 0.5)이 갈라서, 틱으로도 전략이 나올 기대값이 낮다 — 사용자와 합의로 미수집.

## 1. 결과 (모두 사전등록 → 결과 전 커밋 → 실행)

| 연구 | 사전등록 | 결과 | 판정 | 요지 |
|---|---|---|---|---|
| ③ 워뇨띠 운용 구조(급락 분할 매수 + 되돌림 청산) | ca8d0656 | 8b4f4c59 | **REJECT** | 432조합, TRAIN 최선 t 5.06 ≥ 바닥선 4.92 → BTC OOS 2022~ net10 −7.6bp, 무작위 진입 −4.0 보다 나쁨. 27종은 +18.7(무작위 +6.1)이나 사전 기준은 둘 다 요구. 승률 88~95%·왜도 −6~−7 = 그의 손익 모양만 재현 |
| ② 어림수 지지·저항 | fdb1da4a | 69cba84c | **REJECT** | 바닥선(가짜 격자) 6.81 > 최선 5.52. 가짜 격자가 더 큰 t — '짧은 신저점 뒤 되돌림'이지 어림수 효과 아님. OOS gross 0.3~0.7bp < 비용 10bp |
| 무한매수법 크립토 18변형 | e8dbf002 | 479dce60 | **REJECT** | 일봉 40~320분할·주봉 20~40 × base 10/20/30. BTC·ETH 전부 Sharpe 가 보유 이하(최선 0.62 vs 0.98 · 0.45 vs 0.70). 같은 비중 보유 대비 수익 절반. FTT 붕괴 연 −22~−39% |
| ① 펀딩 극단 역행 | — | — | 착수 안 함 | 사전등록 전 확인해 보니 **이미 기각된 가설**(crypto-short-horizon C6·C1, funding-predictive-baseline). 09-22 부록의 'BTC 24h +191bp' 는 그 안의 한 조각 — 재시험하면 데이터 마이닝 |

파일: `research/strategy-lab/findings/{wonyotti-ladder-structure,round-number-levels}-{preregistration,results}-2026-09.md` ·
`crypto-infinite-buying-screen-2026-09.{md,json}` · 코드 `research/strategy-lab/wonyotti/{ladder_structure,round_levels}.py` · `crypto_infinite_buying_screen.py`.

## 2. 새 데이터 (전부 gitignore)

- `research/strategy-lab/data/crypto/1m/ETHUSDT_1m.parquet` — 바이낸스 현물 1분봉 478만 봉, 2017-08~2026-09-25(기존 수집기 `--symbol ETHUSDT`).
- `research/strategy-lab/data/crypto/daily-binance/*.parquet` — 알트 26종 + LUNA(2022-05-13 에서 자름)·FTT 현물 일봉(data.binance.vision).

## 3. 함정 (다음에 같은 데 물리지 않게)

- ★ **바이낸스 자료실 시각이 2025년부터 마이크로초**다. 파일을 이어 붙인 뒤 한 단위로 판별하면 옛 행이 1970년이 된다(이번 첫 실행이 그랬다 —
  판정 기준 불변, 행마다 판별로 고쳐 재실행). `collect_binance_spot_1m.py` 는 파일마다 판별해 안전.
- ★ **`git stash; pull --rebase; stash pop` 금지** — 추적 변경이 없으면 pop 이 옛 autostash(09-21~23, macro.json·FRED manifest)를 꺼낸다.
  이번에 실제로 났다: 추적 파일은 HEAD 로 복구, 옛 stash 항목은 사용자 것이라 그대로 둠, **로컬 전용 FRED manifest 가 09-22 사본으로 덮였다**(다음 FRED 예약작업이 다시 씀).
  `git pull --rebase --autostash` 를 쓴다(메모리 feedback-no-blind-stash-pop).
- 무한매수 엔진은 정수 수량이라 코인 가격을 첫 종가 1000 으로 정규화하고 tick 을 1e-6 으로 덮어써야 한다(스크리닝 스크립트에 있음). 규칙 값은 여전히 로컬 전용.
- 크립토 '낙폭이 작다'는 대부분 현금 비중 효과다 — 비교는 **같은 비중만 보유**와 해야 한다.
