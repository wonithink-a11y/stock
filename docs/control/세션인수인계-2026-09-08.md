# 세션 인수인계 — 2026-09-08 (진행상황 점검에서 시작해 부채 5건 정리)

"진행상황 체크"로 시작한 세션이다. 확인만 하려다 **4일째 죽어 있던 워크플로**와
**라이브 모의계좌가 잘못된 설정으로 도는 것**을 연달아 발견해서, 발견한 것을
그 자리에서 갚았다. 커밋 9개, 전부 push 완료.

전 세션(`세션인수인계-2026-09-07.md`)의 §0·§1·§3·§5·§8 도 이번 작업에 맞춰
갱신했다 — 그 문서는 09-07 세션의 기록이지만 §0 이 "다음 세션이 할 일"을
들고 있어서 그대로 두면 낡은 지시가 남는다.

---

## 0. 다음 세션이 가장 먼저 할 일

**확인 하나뿐이다.** 2026-09-08(화) **15:40 KST** `shares-snapshot` 자동 실행 후:

```bash
gh run list --workflow=shares-snapshot.yml --limit 1
```

```bash
python -c "import json; d=json.load(open('docs/data/shares-snapshot.json',encoding='utf-8')); print(d['status'], d['okCount'], d['failCount'], d['asOf'])"
python -c "import json; d=json.load(open('docs/data/sector-strength.json',encoding='utf-8')); print(d['sharesSourceCounts'], d['sharesFallbackCount'], len(d['sharesUnverified']))"
```

기대값: `status=OK · 353 · 0 · 이월 0`, `{'KRX': 352} · 0 · 0`.

- **OK 353 이면** 분리(`9309cb9`)가 답이었다. 이 항목을 닫는다.
- **또 PARTIAL 이면** 원인이 인접 워크플로가 아니라는 뜻이다. 그때 **실패분만
  재시도하는 2차 패스**로 간다(`SLEEP_SECONDS` 를 올리는 것보다 먼저 —
  한도에 부딪힌 뒤 더 천천히 미는 건 같은 벽이다). **표본이 아직 2개뿐**임을
  잊지 않는다.

★ `maxSharesStaleDays`·`sharesAsOf` 는 **그룹별 필드다**(최상위 아님,
`scripts/build-sector-strength.py:431`). 최상위에서 읽으면 `None` 이 나온다.

---

## 1. 고친 것 5건

### (1) 리서치랩 워크플로가 4일째 죽어 있었다 — `ece7c38`

`Update ui/data/findings.json (Research Lab UI)` 가 **2026-09-03 이후 전 실행
실패**(마지막 성공 09-03T08:25, 이후 8회 연속 `ModuleNotFoundError: No module
named 'yaml'`).

커밋 `856415f` 가 `build_findings_registry.py` 에 `import yaml` 을 넣으면서
워크플로에 설치 스텝을 안 넣었다. 그 커밋 주석의 **"PyYAML은 이미 설치돼
있다(6.0.3)"** 는 로컬 얘기였다.

`pip install pyyaml` 한 줄. **로컬에 있다는 것은 러너에 있다는 뜻이 아니다.**

### (2) 스냅샷을 daily-analysis 에서 분리 — `9309cb9`

09-07 실행에서 KRX 가 252콜에서 막혔다(성공 252/353 + 이월 21 = 273, PARTIAL).
같은 스크립트를 단독 실행한 전날은 353/353 · 3분55초. 원인을 계정 한도로
특정하지는 못했지만 **단독 실행이 성공한 것은 실측**이라 분리를 골랐다.

`shares-snapshot.yml` 에 cron `40 6 * * 1-5`(06:40 UTC = 15:40 KST) 추가 —
daily-analysis(07:40 UTC)보다 1시간 앞이라 그쪽 checkout 이 그날 스냅샷을
집어간다. 함정 하나: **`schedule` 이벤트에는 `inputs` 가 없다.** 그대로 뒀으면
`--limit` 이 빈 문자열로 렌더되고 커밋도 안 됐다.

```yaml
run: python scripts/build-shares-snapshot.py --limit ${{ inputs.limit || 0 }}
if:  success() && (inputs.commit == 'true' || github.event_name == 'schedule')
```

### (3) maxPositions 드리프트 — `41a5732`

`build_factor_selection.py:431` 이 `maxPositions: 200` 을 하드코딩한 채
selection 리프레시마다 policy.json 을 **통째로 재생성**한다. 커밋 `bcb3c8a`
(09-04)에서 실행돼 `factor_earnings_yield_v1`·`factor_rv60_v1`·`factor_rev1m_v1`
셋이 30→200 이 됐다(커밋 메시지에 없음). `paperEngine.py:229` 가 이 값으로
슬롯예산(`capital // maxPositions`)을 잡으므로 **모의계좌가 333만원이 아니라
50만원 슬롯으로 4일간 돌았다.**

재생성이 기존 `portfolio` **블록 전체**를 보존하게 고치고(다음에 밀릴 값이
`initialCapital` 일 수도 있다) 셋 다 30 으로 복구했다. 30 인 근거 셋: git
이력(`f0c282d`) · MTM 재확인 문서가 EY 를 mp=30 으로 명시 ·
`factor_earnings_yield_mtm_reverification.py` docstring 이 과거 오염 때
`policy.json.bak` 대조로 "mp=30이 진짜 원본"임을 확인한 기록.

`tests/test_paper_sleeve_policy.py` tripwire 신설. **mp 를 200 으로 되돌려
실제로 깨지는 것을 확인했다**(교훈61·72 — 통과가 정보를 주는지 먼저 묻는다).

### (4) EY 수치 갱신 — 갱신할 수치가 없었다 — `bcc999c`

실험실이 "selection 갱신으로 CAGR 4.68% → 1.98% 붕괴"를 보고했는데
**재현되지 않는다.** mp=30 복구 후 정밀 MTM 으로 다시 재니:

| | 08-30 → 09-08 |
|---|---|
| earnings_yield (mp=30) | CAGR **4.86% → 4.80%** · Sharpe 0.483 → 0.469 |
| ew_benchmark_liquid_v1 | 3.04% → **2.30%** |
| rev1m | -0.91% → **-1.30%** (실험실 보고 "+1.83 부호반전"도 재현 안 됨) |

붕괴는 selection 갱신이 아니라 **mp=200 드리프트 + exit_date 실현손익 회계**의
산물이었다. 부모 문서(08-30 MTM 재확인)의 수치는 그대로 유효, 판정 HOLD 유지.

★ **EW 벤치마크가 2.30% 로 떨어졌다** — `market-benchmark-comparison-2026-09`
이 하향편향으로 지목한 그 수치다. EY 겉보기 초과 2.50%p 를 알파로 읽으면
안 되고, 편향보정 EW(4.37%) 대비로는 0.4%p 대다.

`12e01f2`(selection 갱신)는 리프레시가 아니라 **버그 수정**이었다 — "마지막
리밸런싱월이 fwd1m 필터에 걸려 빠지던 것". 갱신 후 데이터가 더 맞는 쪽이다.

### (5) PBR 재현성 — `bd1cdf8` · `4d3392e`

**사슬은 소스에서 끊겨 있지 않았다.** `build-a5-valuation-panel.js` 의 입력
(calendar·universe/a1a·price/a2a·fundamentals/a3,a3c,a3d·resolver)이 전부
커밋돼 있고 전략 디렉터리 4종도 이미 추적 중이다 — `세션인수인계-2026-08-26.md`
§3 의 "미커밋이라 로컬 전용" 서술이 그 뒤 커밋되면서 낡은 것이다.

끊긴 것은 **출처 기록**이다. 생성기가 manifest(sha256·행수·기간)를 쓰게 하고
현재 빌드를 앵커로 고정했다.

```
sha256   e55330bf21155a774c6ef0dc9a56243fcb88e3a359c6a5de6e7e6a2ff773b804
rows     175,250 (pbr 72.14%) · asOf 2016-01-04 ~ 2026-09-01
baseline CAGR 5.49% · Sharpe 0.521 · MDD -21.09% · 청산 777  ← 정본
```

**패널을 재생성하지 않았다** — 다시 만들면 상류가 갱신된 만큼 값이 움직여
고정하려는 앵커가 사라진다. 사후 manifest 의 `gitHead`·CLI `end` 는 확인할
수단이 없어 `null` 로 뒀다(교훈57, 추정해 안 채운다).

별건으로 `pbr_value_v1_combined` 의 실물이 **nDrop=3** 인데 KEEP finding 은
nDrop=2 선택이라고 적어 둔 것을 발견해 **3 으로 확정**했다(사용자 결정).
근거: dropout policy 의 `nDropNote`(Qlib 예제와 같은 topN 10% 비율)라는 사전
근거 + 재현에서 TRAIN 최선이 2↔3 으로 뒤집혀 차이가 노이즈 + OOS 부호반전
0건이 두 값 모두에서 유지. tripwire 에 factor pin 추가.

---

## 2. 실험실 미커밋 파일 일괄 커밋 — `de61ad3`

findings 11건 · 실험 스크립트 14건 · `strategies/factor_rv60_v1_top30/`.

판단 근거는 저장소가 갖고 있었다 — **findings 223건 · 루트 스크립트 366건 ·
전략 디렉터리 35개가 이미 추적 중**이다. 이 묶음만 예외로 남아 있었다.

`dashboard/sweep-dashboard.html`(592KB)만 gitignore — 생성기와 템플릿이 둘 다
커밋돼 있어 재생성된다(valuation-panel 과 같은 원칙: 산출물이 아니라 재생성
경로를 커밋한다).

★ 2026-09-06 의 "연구용 commit/push 금지" 지시 중 `market_comparison_kr_2026_09.py`
와 `findings/market-benchmark-comparison-2026-09.md` 두 개는 **뒤집어 커밋했다.**
이번에 커밋한 두 finding 이 이 문서를 근거로 인용해서, 빼 두면 커밋된 문서가
저장소에 없는 문서를 가리킨다. 되돌리려면 그 두 파일만 별도 revert.

---

## 3. 함정 6가지 — 다음 세션이 알아야 할 것

**(1) `git status` 로는 실험실 산출물이 원리적으로 안 보인다.**
`research/strategy-lab/.gitignore:2` 가 `reports/*` 를 통째로 무시한다.
09-06 실험 11개 디렉터리가 그 안에 있었다. 실험실 작업을 인수할 때는
`git status` 말고 **`reports/` 를 직접 본다.**

**(2) 판정을 담은 파일이 frontmatter 없이 올 수 있다.**
`07-pbr-quality-vt-filter-oos-2026-09.md` 는 `# 제목` 으로 바로 시작했다.
`parse_frontmatter()` 가 `None` 을 주면 **본문 키워드 추론으로 판정이 지어진다**
(`build_findings_registry.py:151`). 커밋 전에 파싱을 확인한다.

**(3) 로컬에 있다는 것은 러너에 있다는 뜻이 아니다.** 위 §1-(1) 참고 — 커밋
주석이 "이미 설치돼 있다"고 단언했는데 워크플로가 4일 죽었다.

**(4) policy.json 오염 경로가 최소 두 개다.**
`build_factor_selection.py` 의 전체 재생성(이번에 막음)과
`run_capacity_test.py` 의 파일 바꿔치기(중단되면 못 되돌림, 과거 mp=50 잔존
전례 있음 — 손 안 댔다). tripwire 가 어느 경로든 잡는다.

**(5) `factor` 블록은 엔진이 안 읽는다.** 선택 로직이 selection.json 에
구워져 있다(그 블록 자신의 note). **policy 만 보고 라이브 동작을 단정하면
틀린다** — 구워진 selection 쪽(`pbr_value_v1_dropout/selection.json` 의
`nDrop`)까지 봐야 한다. tripwire 도 양쪽을 본다.

**(6) 순위가 데이터 갱신마다 뒤집히면 그건 "1위가 바뀐 것"이 아니라
"구분이 안 되는 것"이다.** 이번 세션에서만 두 번 나왔다 — EY capacity
mp=30↔50, PBR nDrop 2↔3. 전례도 셋(`pbr-topn-strength-oos` TRAIN→VALID 완전
역전 · `pbr-roe-quality-overlay-oos` gate50 · combo-sweep). **사후 재선택하지
않는다.**

---

## 4. 관측한 것

```
daily-analysis (스냅샷 스텝 포함)     38분 34초   ← 예상 41~42분보다 짧았다
shares-snapshot 09-07 실행           성공 252 + 이월 21 = 273 (PARTIAL)
                                     250콜까지 무결 -> 이후 JSON 파싱 예외
krxMarketCapCheck                    n=272 · medianRatio 1.0 · beyond2pct 0
research-lab-ui 재실행               49초 green
factor_earnings_yield_mtm_rever...   938초 (약 16분)
pytest tests/ (strategy-lab)         200건 통과 · 8분 59초
저장소 회귀 (scripts/test-*)          32건 통과 · 약 8초
```

★ **GitHub 스케줄러가 약 6시간 밀렸다**(09-07, 07:40 UTC 크론이 13:39 UTC 에
실행). 밀림이 균일해 순서는 유지됐다.

★ Windows 콘솔(cp949)에서 회귀 3건이 FAIL 로 보인다 — `✅`·`—` 를 못 찍는
것뿐이다. `PYTHONIOENCODING=utf-8` 로 돌리면 전부 OK.

---

## 5. 열려 있는 것

- **§0 의 15:40 KST 확인** — 유일하게 시한이 있는 항목.
- **A3c staleness 180일 임박** — 폴백 80종목이 175~179일이고 임계가 180.
  며칠 안에 `sharesStale: True` 가 그룹 절반에서 켜진다. **임계를 미리 올리지
  않는다** — 켜진 뒤에 "A3c 는 6개월 낡은 게 정상"을 어디 적을지 정하는 것이
  순서다.
- **`run_pbr_value_v1.py` 실현손익 회계** — CLAUDE.md 함정 (5)가 미수정으로
  적어 둔 것. PBR baseline 5.49% 는 MTM 으로 낸 값이라 앵커는 영향 없다.
- **`run_capacity_test.py` 의 policy 바꿔치기** — 위 함정 (4). 손 안 댔다.
- **QCOMP 엔진 검증 · PIT 시총 이력 · KSIC→투자테마 매핑** — 전 세션 §6 그대로.

---

## 6. 작업트리

```
 M research/strategy-lab/data/market-regime/_manifest_fred_extended.json
```

**이것 하나뿐이다.** 2026-09-05 FRED 재실행의 로컬 산물(rowCount 3108→3111).
이번 세션 것도 실험실 것도 아니라 손대지 않았다. 기술하는 parquet 이
gitignore 대상이라 이 manifest 만 커밋하면 저장소에 없는 행을 주장하게 된다 —
의도를 아는 사람이 정리한다.

세션 중 `ui/data/macro.json` 의 로컬 수정은 rebase 하며 버렸다(writer 가
Actions 이고 원격에 더 새 산출물이 있었다).

---

## 7. 커밋 9개

```
ece7c38  fix(ci)                research-lab-ui 에 pyyaml 설치 추가
9309cb9  refactor(shares)       스냅샷을 daily-analysis 에서 분리해 자체 스케줄로
7491396  docs                   인수인계 §0 을 09-07 결과 + 09-08 확인으로 교체
41a5732  fix(paper-trading)     maxPositions 드리프트 복구 + 재생성이 portfolio 를 덮지 않게
bcc999c  research(ey)           selection 갱신 후 수치 재확인 - 갱신할 수치가 없다
bd1cdf8  research(pbr)          재현성 사슬 점검 - 끊긴 자리는 소스가 아니라 출처 기록
4d3392e  research(pbr)          nDrop=3 확정 + factor 파라미터 pin 추가
de61ad3  research               실험실 2026-09-06 실험 일괄 커밋
9a0438d  docs                   인수인계 §3·§5 정리 + 딸린 절 갱신
```

`f6bd1fa`·`60fbfc4`(ui/data/findings.json)는 워크플로가 자동 커밋한 것이다.
