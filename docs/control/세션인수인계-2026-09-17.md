# 세션 인수인계 — 2026-09-17 (A2a 품질검사 사각지대 수정 · 매도 타이밍 연구 재개)

`세션인수인계-2026-09-15.md`(업비트·빗썸 시세·차트) 이후 세션. 그 문서가
가장 먼저 보라고 한 것(RV20 선물 VM 확인)부터 시작해, A2a 데이터 품질
버그 발견·수정·재수집까지 완주하고, 사용자가 예전부터 가장 아쉬워하던
"언제 팔아야 하는가"(매도 타이밍) 연구를 새로 열었다.

---

## 0. 다음 세션이 먼저 볼 것

### ① ★★ 오늘 만든 연구 스크립트 3개 + findings 1건 — 전부 미커밋

```
research/strategy-lab/sweep_momentum_window.py        DCF 이전, 모멘텀 윈도우 스윕
research/strategy-lab/dcf_discount_v1.py               DCF 내재가치 팩터 v1 + 규칙1 실험
research/strategy-lab/partial_exit_segment_sweep.py     부분익절 세그먼트·트레일링 스윕
research/strategy-lab/findings/partial-exit-segment-sweep-2026-09.md   위 스크립트 결과 findings
```

사용자가 findings 정리는 요청했지만 **커밋은 아직 안 물어봤다** — 다음
세션 시작 시 커밋 여부부터 확인한다. (A2a 수정은 이미 커밋·푸시·재수집까지
완료됨, 아래 ② 참고 — 이것과 헷갈리지 않는다.)

### ② A2a 품질검사 사각지대 수정 — 커밋·재수집 완료, production 반영됨

`scripts/build-price-a2a.py`의 `find_violations()`가 "거래정지 직전 마지막
실거래일 ↔ 재개 직후 첫 실거래일" 비교를 아예 안 하고 있었다(양쪽 다
거래량 0 또는 캘린더 비인접 조건에 걸려 사각지대). lowmom60_v1이
2026-09-01에 뽑은 062970·101000(둘 다 최근 감자/병합으로 장기 거래정지)
사례를 조사하다 발견했다. 수정: 거래량>0 행만 남긴 부분수열에서 "캘린더
간격 == 행 간격"인(=그 사이 전 거래일에 명시적 거래량 0 행이 있는, 즉
행 자체가 안 빠진) 쌍만 다리를 놓아 추가로 잰다 — 행이 아예 빠진
수집공백(기존 `susp_gap`)은 그대로 안 건드린다.

커밋 `e8c9c74`(회귀 2건 추가, 기존 9→11건 전부 통과) → push →
`backfill-price-a2a` workflow_dispatch 트리거 → 성공(`5648c30`, 봇 커밋).
**품질 제외 종목 22→54개(신규 32개), 제외율 2.10%(상한 3% 안쪽)**,
062970·101000은 이번에도 제외 안 됨(재개 당일 변동폭이 062970 +29.9%·
101000 -18.1%로 임계 50% 미만 — 이건 데이터 결함이 아니라 진짜 그렇게
움직인 것으로 확인됨, 아래 ③ 참고).

### ③ 062970·101000은 데이터 버그가 아니라 진짜 폭락주였다 — 결론 확정

lowmom60_v1이 9월 뽑은 두 종목의 mom60(-56.4%·-60.8%)을 실제 KIS 체결
기록·DART 공시로 역추적했다. 결론: **정지 전에 이미 -50%대 진짜 폭락이
있었고(101000은 감자결정, 즉 자본잠식 회피용 구조조정), 정지 재개 후
변동성이 커진 게 한 겹 더 얹힌 것** — 회계 착시가 아니라 진짜 심각하게
망가진 기업을 산 것이다. lowmom60_v1은 애초에 REJECT 판정(2026-09-04,
t=0.79)이었고 이번 조사로 그 판정이 더 구체적으로 뒷받침됐다. 재론 안 함.

### ③-b RV20 선물 VM — 09-15 문서가 지목한 VM이 틀렸다, 실제로는 정상

`세션인수인계-2026-09-14.md`가 "VM(`stock`)에서 확인"이라고 적어뒀는데
**틀렸다** — 실제 RV20 자동화(systemd 유닛·`.env`의 선물 계좌 키)는
`stock-new` VM에 있다(`stock`은 유닛도 자격증명도 없음). `stock-new`에서
확인: 2026-09-14 09:05 첫 실주문 성공(매수 1계약, 주문번호 0000001627),
09-15·09-16도 정상 실행(조정 불필요). **정상 작동 중** — CLAUDE.md
"다음" 섹션 최상단 항목은 완료로 갱신해도 된다(아직 안 함).

### ④ 안 한 것 — "지침파일 속 주식종목 분석" 섹션을 못 찾았다

사용자가 "내 프로젝트 지침파일에 '주식종목 분석'이라는 부분이 있다"고
했는데, 저장소 전체(*.md/*.json/*.js/*.py)를 검색해도 안 나온다(딱 하나
나온 건 완전히 무관한 옛 블로그 프로젝트 이름이었다, 완료-이력.md:758).
CLAUDE.md·CHATGPT.md·AGENTS.md·TASKS.md에도 없다. `stock-project-architect`
스킬은 로컬 파일로 안 잡히는 원격 정의라 내용도 못 봤다. **경로를 못 찾은
채로 넘어갔다** — 다음 세션이 사용자에게 정확한 위치를 다시 물어봐야 한다.

---

## 1. 오늘 세션 흐름

```
① 09-15 인수인계 확인 → RV20 VM(위 ③-b)·크립토 티커 UI 라이브 검증
   (Upbit CORS 한도 재현·재시도 버튼 동작 확인)
② KIS API로 062970·101000 체결내역 추적 → 감자/병합 사실관계 확정(위 ③)
③ A2a find_violations() 사각지대 발견 → 수정 → 회귀 → 커밋·push →
   backfill-price-a2a 재수집 → 성공 확인(위 ②)
④ 사용자가 공유한 ChatGPT의 Anthropic financial-services 분석에 대해
   실제 GitHub 저장소(skills/commands 원문) 대조 확인 → 대체로 사실이나
   두 가지 놓침: (a) 커맨드 파일은 스킬 로더일 뿐 로직은 SKILL.md에 있음
   (b) 유료 데이터 커넥터(Daloopa·CapIQ 등) 없이는 작동 안 하고 한국시장
   커버리지 전무 → "통째로 가져오지 말고 프레젠테이션 레이어 하나만
   참고"로 결론
⑤ 사용자가 붙여준 AITAS-EQ(5인 가상전문가 종목분석) 프롬프트 검토(실행 안 함)
   → 기술적/거시/기본적 일부만 데이터로 뒷받침되고 산업·리스크·해자는
   전부 검증 안 된 텍스트 생성 → 페르소나 토론 형식 자체가 "5명 합의"
   착시를 만들 위험 지적, 목표주가·손절가 산출은 조회·알림 전용 원칙에서
   벗어남 → 채택 안 함
⑥ DCF 내재가치 팩터 v1 신설(`dcf_discount_v1.py`) — FCFE 방식 간이 DCF,
   자기자본비용=국고채3년+ERP(5.5%, [ASSUMPTION]), 베타=1 고정(v1 단순화),
   성장률은 매출 CAGR→5년 fade to terminal. **같은 스크립트로 규칙1
   ("결측치 채우지 않는다") 준수/위반 두 변형을 동시에 백테스트** —
   DCF 자체는 REJECT(t=0.60, 난수 바닥선 3.58에 한참 못 미침), 규칙1
   실험은 필드가 15,067건 늘었는데도 결과가 거의 동일(순초과 +1.28%
   vs +1.26%) → 이 팩터·이 결측 유형에서는 규칙1이 알파를 안 깎았다
⑦ 매도 타이밍 재조사 — 사용자가 예전에 낸 아이디어("점수 떨어지면
   판다")가 2026-09-04에 이미 REJECT였다는 걸 재발견해서 공유
   (`score-based-exit-precheck-2026-09.md`, "비싸져서 하락한 그룹의
   이후수익률이 오히려 더 높다", TRAIN t=-3.72). 대신 이미 있던
   `partial-exit-scale-out-2026-09.md`(HOLD)를 세그먼트로 쪼갬:
   시가총액(대형=효과없음·중소형=+40%) → 변동성 3분위(전부 +40%로
   일치, "저변동=낮은%" 가설 기각) → 업종 20그룹(표본부족·반도체
   N=0·판정불가) → 잔여분 트레일링 손절 스윕(중형 3%가 진짜 봉우리,
   Sharpe 0.45→0.64. 소형 20%는 비단조라 노이즈 의심)
⑧ findings 신설 + 트레일링 결과 추가 반영(`partial-exit-segment-
   sweep-2026-09.md`)
```

## 2. 커밋 목록 (오늘, push 완료분만)

```
e8c9c74  fix(a2a): 거래정지 재개 직후 급변도 품질검사 대상에 포함
5648c30  chore(backfill): A2a 현재 상장분 일봉 가격 (봇, 재수집 결과)
```

위 두 개만 push 완료. §0-①의 연구 스크립트·findings는 미커밋.

## 3. 주요 발견 요약 (findings 갱신·신설)

| 이름 | 판정 | 한 줄 |
|---|---|---|
| (기존, 재확인만) `score-based-exit-precheck-2026-09.md` | REJECT | "고평가되면 판다"는 반대 방향 증거만 있음 — 재론 안 함 |
| (기존, 재확인만) `tier2-exit-policy-oos-2026-09.md` | REJECT | 손절 규칙은 MDD만 개선, 수익엔 무손절이 최선 |
| (기존, 세그먼트로 확장) `partial-exit-scale-out-2026-09.md` | HOLD | +10~30% 부분익절 12/12 개선(2026-09-04 원 결과) |
| **신설** `partial-exit-segment-sweep-2026-09.md` | HOLD | 대형=효과없음, 중형=+40%매도+잔여3%트레일링(Sharpe 0.45→0.64, 검증된 봉우리), 소형=+40%매도까지만 신뢰, 업종축은 판정불가 |
| (연구, 문서화 안 함) DCF discount v1 | REJECT | t=0.60, 규칙1 준수/위반 차이 거의 없음(+1.28% vs +1.26%) — findings 미작성, 필요하면 다음 세션이 작성 |

## 4. 검증되지 않은 채 넘어간 것 (다음 세션 주의)

- **소형주 트레일링 최적값 미확정** — 20%가 최적으로 나왔으나 15%에서
  움푹 파지는 비단조 패턴, 트레일링 발동 건수가 적어(27~48건) 노이즈
  가능성. 중형주처럼 촘촘한 재스윕 필요.
- **TRAIN/VALID/TEST 구간별 분해를 전혀 안 했다** — 오늘 나온 모든
  세그먼트·트레일링 최적값은 ALL(전체 기간) 기준이다.
  `partial-exit-scale-out-2026-09.md` 원 finding이 "VALID 구간에서는
  기준선보다 나쁘다"고 이미 경고했는데, 오늘 확장판이 그 경고를
  물려받았는지 확인 안 됐다. **production 검토 전 반드시 확인.**
- DCF v1은 베타를 전 종목 1.0으로 고정했다 — 종목별 베타를 넣으면
  결과가 바뀔 수 있다(v2 후보, 착수 안 함).
- "지침파일 속 주식종목 분석" 경로 못 찾음(위 §0-④).

## 5. 작업트리 상태 (세션 종료 시점)

```
research/strategy-lab/data/leveraged-etf/_manifest.json        루틴(반복 판단, 커밋 안 함)
research/strategy-lab/data/market-regime/_manifest_fred_extended.json  루틴
research/strategy-lab/eval_f2_shadow.py 등 futures/stage5_4_*, stage6_1_*   OpenCode 병행 작업 - 손대지 않음
research/strategy-lab/sweep_momentum_window.py                  오늘 신규, 미커밋(§0-①)
research/strategy-lab/dcf_discount_v1.py                        오늘 신규, 미커밋(§0-①)
research/strategy-lab/partial_exit_segment_sweep.py             오늘 신규, 미커밋(§0-①)
research/strategy-lab/findings/partial-exit-segment-sweep-2026-09.md   오늘 신규, 미커밋(§0-①)
```
