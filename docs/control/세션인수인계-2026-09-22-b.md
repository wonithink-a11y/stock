# 세션 인수인계 — 2026-09-22-b · 아침 확인 결과 · autotrader 인증·실전 대조·보안 · 알림 정리 · ETF 횡단면

이전: `세션인수인계-2026-09-22.md`(새벽 autotrader 작업). 작성 2026-09-22 밤. 정책·config·criteria 변경 없음. 전부 push 완료.

## 0. 다음 세션이 먼저 할 것

1. **09-23 아침**: ① 장중 급등락 알림이 `[object Object]` 없이 문장으로 오는지(Actions `intraday-alert` 로그) ② 국내 장중에 **모의 1주 주문**으로
   새 주문 본문(`EXCG_ID_DVSN_CD=KRX`·`SLL_TYPE`)이 받아지는지 ③ 미국 장중 SOXL 주문이 `AMEX` 로 받아지는지(모의 주문은 사용자 결정).
   ②③ 통과 → 사용자가 서버 설정 `live.tr_ids_reviewed: true`. 이것이 실계좌 연결 전 마지막 기술 항목.
2. 정기: 07:30 미국 스냅샷 · 09:05 RV20·크립토(크립토 5종 BTC·DOGE·PEPE·SHIB·SUI 09-23 청산) · 21:30 infbuy dry-run.
3. **10-01 월간 점검**(CLAUDE.md 절차): A2a → refresh-selections 첫 workflow_run 실물 검증, 부분 익절 그림자, O2b 그림자, **ETF 그림자 첫 관측**.

## 1. 아침 확인 결과(09-21-b §1)

- **동시호가 1단계 시험 FAIL** — 매도 체결 282,500 vs 공식 시가 283,000(−17.67bp, 분봉 09:00 봉 시가로 교차 확인). 모의는 시초 단일가가 아니라
  개장 뒤 연속매매가로 체결하는 것으로 보임(추정). **O2b·O3u 모의 실행 중단**, 다른 체결 방식은 새 GO 없이 안 함.
  `단기규칙-모의실행-1단계시험-결과-2026-09-22.md`.
- 미국 S&P500 스냅샷 첫 자동 실행 성공(07:31) · RV20·크립토 09:05 정상(git pull 수정 첫 검증 통과) · RV20 이 30분 걸림(원인 미확인, 반복되면 본다).
- 옛 `infinite-buying-vts.timer` disabled 는 **의도된 전환**(00:36:24 사용자 sudo 기록). `infbuy` 프로필 auto=execute(사용자 18:53) — 서비스에
  `--execute` 가 없어 **dry-run**. 21:30 실행 정상(계획 8건, 오류 0, SOXL 시세 AMS 경로로 $137.51).

## 2. autotrader

| 커밋 | 내용 |
|---|---|
| f51488d0 | 로그인 30일 유지(세션 파일 `state/web_sessions.json`, 토큰 sha256 만) · 조작·금액 보기는 **지문(패스키)** · 재확인 5분 |
| e7baf4bc | **실전 TR 대조**(KIS 공식 open-trading-api @b4e6249): TR_ID 13개 일치. 주문 본문 2건 수정 — 해외 거래소 코드(시세에서 찾은 NAS/AMS/NYS → NASD/AMEX/NYSE, 못 찾으면 주문 거부) · 국내 `EXCG_ID_DVSN_CD=KRX`. `autotrader-실전TRID-대조-2026-09-22.md` |
| 382f2cfd · f4f3e20d · 762c6046 | **보안 재검토**(opus 서브에이전트, 19만 토큰) N1~N8 중 N1~N6·N8 수정: 패스키 있으면 인증앱 재인증 거부 · 요약 화면 금액은 재확인 뒤 · `web-setup --reset`·`passkey-reset`·새 `logout-all` 이 모든 로그인 무효(세션 세대 `sessGen`) · 지문 경로는 IP 별 잠금만 · 일일 한도 = max(원장, 증권사 오늘 주문) + 조회 실패면 주문 안 냄 · `web_auth.json` flock. N7 은 운영(실계좌 `web_live_allowed:false`·최소 한도) |
| 40117f9d | 체결 알림(스냅샷 5분, 늘어난 체결만) · 매매·보안 알림 방 분리 코드 |
| 5a395381 | 웹 `/channels` — 매경 자이앤트·크립토·월가월부 공개 채널 글(15분, 개인 열람, VM state 에만). 비트캐쳐는 미리보기 꺼져 불가. 유닛 `channels-feed` 설치 |

- 보안 검토 기준표·미룬 범위: `autotrader-보안검토-2026-09-22.md` 끝. **다음 검토 범위**: e7baf4bc · 40117f9d · 5a395381 · 762c6046.
- 회귀: test-autotrader 97 · web 67 · webctl 42 · passkey 58(VM, N6 flock 포함) · profiles 33 · pnl 18 · notify 23 · infbuy 15 · channels 17.
- 전 기기 로그아웃: `cd ~/collector && ~/collector-venv/bin/python3 -m autotrader --config ~/collector-venv/autotrader/autotrader.local.json logout-all`.

## 3. 알림

- **장중 급등락 `[object Object]`** 원인: `intraday-check.js` main 이 알림 객체를 그대로 join, 쿨다운 도장도 안 찍어 체크마다 반복. `deliver()` 로 수정·회귀 2건.
- 텔레그램 **세 갈래**(봇 @wonistock_bot 하나): 1:1 = 뉴스·공시·일일분석·장중급등락 · `주식 알림` 그룹 = 장애 전용 · **`매매•보안` 그룹(신설)** =
  로그인·웹 조작·주문 접수·체결. VM `.env` 에 `TELEGRAM_TRADE_CHAT_ID` 추가(백업 후), 테스트 메시지 도착 확인.
- 구글 계정 2단계 인증 켜짐 · DuckDNS 는 구글 로그인(연결된 앱 목록 확인) → 완료.

## 4. 연구 — ETF 횡단면

- 수집기 `collect_etf_ohlc_krx.py`: KRX Open API 전 필드(OHLC·NAV·기초지수명), 2010-01-04~2026-09-21, 172만 행, gitignore.
  **폐지 ETF 포함 실측**(2010~14 의 155개 중 55개가 현재 없음). 2014 이전 휴장일에도 가격 공란·거래량 0 행이 온다 → 거래일은 거래가 있는 날만.
- 순서: 사전등록 9741b465 → 명확화 d5463de3(분류 단위=코드·전 기간 이름) → 호가 189d7846(2023-12-11 전 5원) → 유니버스 e7d0b677(1,428 → 국내주식형 502,
  사람 검토 69건) → 계산 코드(실행 전) d7844237 → **결과 ace62dd8**.
- **결과**: O2b **REJECT**(2010~20 정보 +1.2bp·t 0.54 < 바닥선 1.95 — ETF 에서 신호 없음. 2021~ 에만 양: TEST +17.7bp·t 4.9, OOS net +18bp 는 기록만) ·
  O3u **INCONCLUSIVE**(신호일 TRAIN 35·VALID 26). 기간 재절단·재시험 금지.
- **forward 그림자 사전등록 c42e17dc**: O2b·O0, 신호일 ≥ 09-22, 유니버스 502개 동결, 셀별 250일 1회 판정(t ≥ 2.24), 연장 500일 1회.
  `run_etf_close_open_shadow.py`(selftest 11) — 매월 1~5일 점검에서 실행.
- 외부 검토(ChatGPT) 지적(분류 6가지·생존편향·축소 규모 기록)은 전부 반영.

## 5. 사용자 몫

| 항목 | 상태 |
|---|---|
| DataGuide 문의 발송 | 문안 확정(marketing@fnguide.com) — Gmail 커넥터가 세션에서 무효라 직접 발송하기로 |
| 모의 1주 주문(국내·미국) | §0-1 — 실계좌 연결 전 마지막 기술 항목 |
| 실계좌 프로필 | 위 통과 뒤 · `web_live_allowed:false`·처음 dry·최소 종목·작은 한도 · `--execute`/`AUTOTRADER_ALLOW_LIVE` 는 서비스 하나라 모든 프로필 공통 |
| `sudo systemctl daemon-reload` | 여러 유닛의 '파일 바뀜' 경고 정리(동작 영향 없음) |

## 6. 알게 된 것

- KIS 모의 체결가 ≠ 시초 단일가(1건) — 모의로 동시호가 전략을 검증할 수 없다.
- 동기화 패스키는 서명 카운트가 항상 0 — 복제 탐지 대신 구글 계정 2단계 인증이 방어선.
- 텔레그램 봇은 자기가 보낸 메시지 기록을 조회할 수 없다 — 화면에 넣으려면 보낼 때 기록해야 한다. 공개 채널은 `t.me/s/<채널>` 미리보기로 읽을 수 있다(주인이 끄면 불가).
- KRX Open API 는 휴장일에도 요청 날짜 그대로 빈 가격 행을 준다(교훈 81 의 변형) — 거래일 판정은 거래량으로.
- ETF 호가단위는 2023-12-11 전후로 다르다(전 5원 일괄, 후 2천원 미만 1원).
- Windows 파이썬 `write_text` 치환 중 `\n` 이 실제 줄바꿈으로 들어가는 사고 3회 — 문자열 치환은 Edit 도구나 `chr(10)` 로.
- 저장소에 제가 만들지 않은 로컬 변경(`research/strategy-lab/data/market-regime/_manifest_fred_extended.json`, `Crypto_won/`)이 있다 — 손대지 않았다.
  `ui/data/macro.json` 로컬 변경은 rebase 충돌로 원격본을 쓰고 원래 변경은 `git stash` 보관함(autostash)에 남겼다.
