# 세션 인수인계 — 2026-09-20-b (밤샘 단기·초단기·크립토 실험)

사용자 요청: "단기·초단기 전략을 기존 개선·외부·커뮤니티 어디서든 가져와 밤새 실험, 묻지 말고 진행, 아침 보고"
+ ChatGPT 제안(주식 분봉 7개·크립토 8개) 중 미실행분 전부 + "더 파볼 곳은 변형도 실험". 전부 **사전등록 → 커밋 → 실행** 순서.
코드는 전부 `research/strategy-lab/futures/`(연구 전용), lib·scripts·config·정책 변경 없음, 실주문·VM 작업 없음.

## 0. 결론 먼저

| 실험 | 셀/형태 | 판정 | 결과 문서 |
|---|---|---|---|
| 1차 지수 일봉 16년(ETF 비용) + 선물 분봉 | 15 | 전부 REJECT | short-horizon-lowcost-results |
| 2차 주식 5분(Failed ORB·압축·소진) + 선물 + 미국 ETF | 17 | ECONOMIC 0, S5 급등소진 되돌림 INFORMATION | 같음 |
| 3차 10일저점 규칙 해외 14 ETF | 1 | 실질 재현 실패 | 같음 |
| **장중·밤사이 비대칭 12년** | 서술 | **SUPPORTED(11/11년)** — 활용은 둘 다 막힘 | kr-intraday-overnight-asymmetry (HOLD) |
| PBR 체결 시각 적용 | 3,319건 | 개선 불확인(−16bp, t −0.96) → 규칙 유지 | 같음 |
| 고변동 밤사이 보유 | 3 | INFORMATION(+22~25bp < 비용 30bp) | 같음 |
| 선물 밤사이 2010~15 | 1 | PARTIAL(비용 미달) | 같음 |
| S5 forward 17일 | 1 | PARTIAL(+43bp, t 1.73) | short-horizon-lowcost-results |
| 크립토 10셀(ChatGPT A·B·C·D·F) | 10 | 전부 REJECT(무료 3bp 에서도) | crypto-short-horizon-results (HOLD) |
| 크립토 UTC 시각 | 24 | NO SIGNAL | 같음 |
| **크립토 상승충격 → 다음날 지속** | 규칙 1 | **2015~19 REPLICATED(+144bp) · 2020~26 CONFIRMED(+81bp)** — 단 전액 포트폴리오 MDD −87%, Sharpe=균등보유 | 같음 |

**채택(KEEP) 0건.** 쓸 만한 것 둘 — (1) 크립토 상승충격 지속(크기 설계 후 모의 관측 후보, 사용자 GO 필요),
(2) KR 장중 음·밤사이 양 비대칭(고변동·급등 종목에 집중, 새 고회전 롱 전략의 체결 기본값을 종가로).

## 1. 사용자 결정 대기

1. **크립토 상승충격 슬리브 모의 관측** — 업비트/빗썸 모의 인프라에 소액 고정 비율로. 새 슬리브라 🔴(규칙 추가). 권고: GO(모의만).
2. **S5 보유자 매도·재매수 규칙** — forward 60일 쌓인 뒤 `replication_s5_cup.py` 재측정(≈2026-11 말). 지금은 결정 없음.
3. PBR 체결 시각 변경 — **권고: 하지 않는다**(실거래일 적용에서 개선 불확인).

## 2. 이번에 겪은 함정

- **전체 평균 효과가 특정 전략의 거래일엔 안 옮겨간다.** 장중 편향은 −24bp/일로 확고한데 PBR 매수일(월 둘째 거래일·저변동 가치주)엔
  +8bp 였다. 월 코호트 125개는 24bp 를 가를 검출력이 없다. "평균 이상치 → 즉시 규칙 변경"을 막은 사례.
- `kr-gap-overnight-effect`(2026-08-28)의 "TRAIN intraday 평균 +29bp"와 이번 −15~−24bp 가 부호부터 다르다 — 그 문서는 A2a open 과
  A4 close 를 섞었고 이상치 제외가 없다. 분봉 독립 출처가 이번 쪽과 일치. 그 문서의 분해 표는 인용하지 않는다.
- ChatGPT 의 크립토 가설 방향(과열 → 반전)이 실측과 **반대**(지속)였다. 외부 제안의 방향은 검정 대상이지 전제가 아니다.
- S 유니버스는 liq_base(min 20일) 때문에 250일이 아니라 230일 → 분할 150/37/**43**. 사전등록 문구(63)와 다르다 — 결과 문서에 명기.
- 1차 스크립트 M3 임계 계산에 풀기 버그(실행 시 즉시 예외) → 판정 규칙 무관, 수정 후 실행(9e0bc9f 에 포함).
- yfinance 를 연구 캐시용으로만 씀(SPY·QQQ·해외 ETF 14·크립토 15, `.cache/market_data/`, gitignore). 파이프라인 통합 아님.

## 3. 커밋

646ff25(1차 사전등록) · 9e0bc9f(2차) · 3차 · kr-overnight · crypto · followup · replication · crypto-upshock-recent 사전등록 커밋 각각이
**결과 전**이고, 결과 문서·인수인계는 마지막 커밋. 산출 JSON 은 `research/strategy-lab/futures/*.json`.

## 4. 작업트리

세션 시작 때의 자동 갱신분(`_manifest_fred_extended.json`·`ui/data/macro.json`)과 OpenCode 병행 파일(f2_shadow·stage5_4·stage6_1)은 손대지 않았다.
