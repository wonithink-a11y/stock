# 세션 인수인계 — 2026-09-26-c · 해독 요약·목표가·업종 재분류 · 미국 PIT 게이트·매핑 감사·PBR 재현(REJECT)

같은 날 다른 창: `세션인수인계-2026-09-26.md`(오전·오후 — 알림 VM 이전·us-pit 수집·해독 카드 v0, 이 창이 그 문서 §0 을 이어 갱신했다) ·
`-2026-09-26-b.md`(워뇨띠 후속·무한매수 크립토 — 겹치지 않는다).
정책·config/policies·criteria 변경 없음. 커밋은 전부 push 완료(마지막 eb7eadf9). 작업 트리 깨끗(사용자 녹화 파일 mp4 하나만 untracked).

## 0. 다음 세션이 먼저 할 것

1. **09-28(월) 확인** — 기존 목록(국내 모의 1주 시험 09:10/09:12 · events-dday 07:50 · news-alert 08:00)에 이 세션 몫을 더한다:
   - **daily-analysis 의 새 단계 `Build stock context`** 첫 정기 실행 — KIS invest-opinion 351콜(~6분)·토큰 1회. Actions 로그에
     `목표가: 351종목 · 있음 … · 실패 …` 줄 확인. 실패 20% 초과면 직전 값 유지(정상 동작).
   - 업종 강도 `misfits`(오분류 의심 목록) 정기 생성 — 오늘 수동 실행 기준 51종목.
2. **10-01~05 월간 점검**(CLAUDE.md 절차 그대로).
3. 결정 대기(급하지 않음): SK하이닉스 해독 시범 분석(두 번째 보고서) · KR-2.5 🔴 제안(10월 밸류/재무 역전 재조사와 묶기 권고 —
   오늘 미국 결과도 '저PBR 은 조건부' 쪽) · 국내 시장 히트맵.
4. 사용자 몫: 폰에서 해독 요약(종목 행 펼침)·검색창 한글 입력 확인.

## 1. 대시보드 (docs/index.html)

| 커밋 | 내용 |
|---|---|
| 6cdd814a | 메인 표 종목 행 펼침 = **해독 요약**(한 줄 결론·신호등·재무 칩·보고서 링크) + '🔎 전체 해독' 버튼(모달 해독 탭). PF-1.2·업종 비고 글은 접기. 수급 억/조. 폰 폭은 `.table-wrap` container query(100cqw) |
| 609b4d93 | **52주 위치·증권사 목표가** 타일 — `scripts/build-stock-context.py` → `docs/data/stock-context.json`. 52주 = prices.json 250봉(새 수집 없음) · 목표가 = KIS invest-opinion KR 351종목, 최근 180일 증권사별 최신 목표가 중앙값. 없음은 'none'(0 아님). **맥락 전용·점수 불사용**(절대 규칙 1). daily-analysis 에 단계 추가(continue-on-error) |
| 7c282165 · 349e5395 | `stock-context.yml` **수동 전용** 워크플로(52주·목표가·업종 강도·테마 강도 재생성). **기본 `--no-kis`** — `kis` 입력 체크 시에만 목표가 |

- 로컬 실측(09-26): 목표가 있음 271/351 · 없음 80 · 실패 0. 삼성전자 중앙 500,000원(22곳), SK하이닉스 3,350,000원(22곳).

## 2. 업종 재분류 (config/sectorGroups.json `tickerOverrides` — 21종목)

- 원인: KSIC(A1a) 세분류가 사업 실체와 어긋남(삼성전자 = '통신 및 방송 장비 제조업').
- **`tickerOverrides` 는 `build-sector-strength.py` 만 읽는다** — 연구 스크립트(`sector_step0.py`·`partial_exit_segment_sweep.py`)는 `groups` 만 읽어 무영향.
  KSIC 없는 신규상장도 지정 가능(70220d52). 사유는 파일 안 `_note` 에 전부 있다.
- 옮긴 종목: 삼성전자·두산테스나·티씨케이·원익QnC·동진쎄미켐·솔브레인→반도체 · LG전자·한온시스템·현대오토에버→자동차·부품 ·
  한화시스템→항공·방산 · 한화엔진→조선 · 큐렉소→기계 · 아모레퍼시픽·아모레퍼시픽홀딩스·LG생활건강·메가스터디교육·서부T&D·아난티→유통·소비재 ·
  S-Oil·SK이노베이션→화학 · 아세아→건설 · 솔브레인홀딩스·한화머시너리앤서비스홀딩스→금융(지주사 관례) · 두산에너빌리티→전력.
  **포스코인터내셔널 보류**(무역·에너지 혼합). **에스원만 미분류**(같이 움직이는 업종 없음).
- **오분류 의심 목록**(7d6a6119): 최근 126일 일수익률 상관이 자기 업종(자신 제외 중앙값)보다 다른 업종과 0.10 이상 높은 종목 —
  `sector-strength.json` `misfits`, 업종 탭 맨 아래 접기. **자동 이동 없음.** 남은 51종목은 지주사·방어주('음식료' 끌림)·테마주·유틸리티 —
  이유 있는 경우라 그대로. 경계선(0.10 근처) 종목은 업종 구성이 바뀌면 들고 난다(정상).
- **테마 트리 v0.2**: 소비재›가전(LG전자·코웨이) 신설, LG전자 자동차 부 테마. ★ **테마 선행관계 B형 판정(≈2028-10)은 v0.1
  (`git show e959ed21:config/themeTree.json`)로** — CLAUDE.md·트리 note 에 적었다.

## 3. 외부 데이터 판단

- **FnGuide/DataGuide 회신(사용자 수신)**: 컨센서스 이력은 **PIT 아님**(사후 정정 덮어쓰기) · **별도 DB 저장 금지** · 연 720만원·1년 약정·
  Windows 엑셀 애드인 → 추정치 라인 **종결 확정**(소스조사 문서 §7, fe633e7e). WICS 업종분류도 wiseindex.com 법적고지상 복제·배포에
  사전 동의 필요 + 저장소·대시보드 공개라 **쓰지 않는다**(유료 고객도 저장 금지라 허락 요청 실익 낮음).

## 4. 미국 PIT — 가격 게이트 통과 → 매핑 감사 → PBR 재현 REJECT

| 단계 | 커밋 | 결과 |
|---|---|---|
| 가격 패널 게이트 | b78b1bf9 | FAIL(결손 0.365%) → **통과**(커버리지 0.9903 · 설명 안 되는 결손 91일 · 구간 실패 0) |
| 재무 매핑 감사 | 773a456d | **us-map-1.0 동결** — `docs/control/미국-재무매핑감사-2026-09-26.md`. 완전 98.5% · 2020-12-31 시총 12/12 공개값 ±0.5% |
| PBR 재현 사전등록 → 구현 → 실행 | a881dc18 → 40930672 → eb7eadf9 | **REJECT** — B/M 상위 10% 월 +43.6bp(NW t 1.44 < 2.0) · 20% +18bp. `findings/us-pbr-revalidation-results-2026-09.md` |

- 기록 전용: 금리 상승 달 +120bp / 하락 −37bp(상관 +0.30) — 한국 '금리 상승기 조건부'와 같은 방향이나 셀의 58% 가 금융이라 은행 효과와 못 가른다.
- **인프라는 남는다**: `panel/prices.parquet`(sha 1489d7f8fc4b) · `panel/fundamentals_monthly.parquet`(613d61fe1d15) — 둘 다 gitignore, 로컬 PC 에만.
  재생성: `us_pit_master.py build` → `us_pit_prices.py panel` → `audit_us_mapping.py`(SEC_USER_AGENT 필요 — `.env` 에 없고 run_chain.cmd 에만 있다).
- 손 관리 파일(커밋됨): `segments.csv`(구간을 회사별 조각으로) · `price_check_exceptions.csv`(FRC 파산) · `renames.csv`(+EQR→VMRK).

## 5. 실수·함정 (이 세션)

1. **KIS 토큰 9회 발급 사고** — Actions 러너는 매번 새 기계라 토큰 캐시가 없다. 업종만 바꾸려고 stock-context 를 8번 dispatch 했는데 매번 목표가
   단계가 KIS 를 불렀다(+로컬 1회). 사용자가 발급 알림으로 알아챘다. → 워크플로 기본 `--no-kis` · 메모리 `feedback-kis-token-per-actions-run`.
2. **R1(현재 멤버)이 오늘의 CIK·가격을 구간 전체에 씌웠다** — IR 2017-05~2020-02 가 다른 회사(Gardner Denver) 가격. 결손 검사는 그 앞 공백만 잡았다.
   → R1 도 '현재 CIK 첫 정기보고서 > 구간 시작+120일'이면 REVIEW. 같은 검사로 **지주사 전환 CIK 교체 7건**(APA·AVGO·BG·BLK·CI·DIS·XOM)도 나왔다 — 안 나눴으면 재무가 앞 기간 통째로 빠졌다.
3. **PARA 2022~2025 가 SPAC(Banzai) 가격이었다** — 가격 검사는 '끊김'만 본다. 매끄러운 다른 회사 가격은 **시총 < $1B 점검**(가격·재무 병합 뒤)에서만 드러났다.
   yfinance PSKY 가 CBS→ViacomCBS→Paramount 이력을 이어 담아 CBS·VIAC 구간까지 되살렸다.
4. **야후 splits 열에 분사 가격 조정이 섞여 있다**(DD 1.487·0.4725·2.39, HON 0.9535) — 진짜 분할(정수·1/k·3:2·5:4·4:3·5:2)만 주식수를 바꾸게 분리.
   DD 2019-06-03 은 1:3 병합과 분사가 한 계수라 손 지정(`SPLIT_SHARE_PART`).
5. **분사 이중 계산**: 가격 조정 계수 + 같은 날 배당(DHR 2016-07-05 하루 +35% 가짜 수익 등 4건) → 그날 배당 제외(구현 문서화, 실행 전 커밋).
6. 공시 주식수 단위 오타(×1000·×0.001)·CSO 가 발행주식수인 회사·2019 뒤 dei 차원화 — 전부 us-map-1.0 규칙으로 흡수(감사 문서 §1).
7. 업종 이동 첫 권고(LG전자→유통·소비재)가 틀렸다 — 오분류 목록을 만들고 나서야 자동차·부품과 동조(0.49 대 0.20)가 보였다. 손 분류 전에 데이터로 먼저 잰다.
8. 원격이 자동 커밋(`ui/data/findings.json`)으로 자주 앞선다 — 변경 파일 확인 후 `pull --rebase --autostash`(다른 창의 미커밋 파일이 있을 때 autostash 필요).
