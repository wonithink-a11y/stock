# 세션 인수인계 — 2026-09-18 (부분익절 production 재검토 · PBR/EY 🔴 결정 · HOLD 라벨 정리)

`세션인수인계-2026-09-17.md`(A2a 품질검사 사각지대 수정·매도 타이밍 연구 재개)
이후 세션. 그 문서의 미커밋 파일 3개는 이미 그 세션 끝에 커밋·push돼 있었다
(확인만 하고 새로 할 일 없었음). 오늘은 부분익절/트레일링 production 채택
검토 → 그 과정에서 발견한 프로젝트 전반 HOLD 라벨 정리 → PBR/EY 실계좌
배분 🔴 결정 → PBR+EY 결합축 재설계 검증(판정 정정 있었음) → 업종중립 PBR
라인이 이미 종결돼 있었다는 사실 발견·CLAUDE.md 정정까지 이어졌다.

---

## 0. 다음 세션이 먼저 볼 것

### ① PBR combined 실계좌 배분 — 연구 결정은 GO, 실행은 사용자 몫

`pbr_value_v1_combined`는 실계좌 배분을 검토할 근거가 있다고 사용자가
확정했다(2026-09-18). **단 계좌 개설·자금 이체·실주문 제출은 Claude가
하지 않는다** — 금융거래 실행은 항상 사용자 본인이 한다는 기존 원칙
그대로(CLAUDE.md에 명시함). 다음 세션이 이 항목으로 뭘 더 할 필요는
없다 — 사용자가 직접 진행하고, 코드 쪽에서 뭔가 필요해지면(예: 실계좌용
policy 분리) 그때 다시 요청이 올 것이다.

`factor_earnings_yield_v1`(EY)은 **실계좌 슬리브로 보류 확정**. 이유는
세 경로 전부 막혔기 때문(아래 §3 표 참고) — 재론하려면 새 가설이 필요하다.
모의투자 관측용으로는 계속 둔다(실손실 0, 기존 결정 유지).

### ② 부분익절/트레일링 production 배선 — 여전히 착수 안 함, 이유가 더 명확해짐

`partial-exit-segment-sweep-2026-09.md`는 여전히 **HOLD, production
미채택**이다. 오늘 두 가지가 추가됐다:
1. TRAIN/VALID/TEST 분해 — 중형주 후보(+40%+3%트레일링)가 원 전략보다도
   VALID를 깎는다(0.2033→0.0906). 소형주(+40%만)는 세 구간 전부 개선해서
   더 깨끗하다.
2. 다른 전략(factor_earnings_yield_v1·lowmom60_v1)에 같은 규칙을 독립
   재현 — 결과가 전략마다 갈린다(EY는 더 나빠짐, lowmom60은 개선). 그리고
   pbr_value_v1을 세그먼트 안 나누고 전체로 재면 VALID 악화가 10분의 1
   수준이라, **중형주 VALID 붕괴가 규칙의 결함이 아니라 시가총액
   세그먼트가 표본을 얇게 썰면서 생긴 잡음일 가능성**이 이전보다 커졌다.

다음에 이 라인을 다시 열 이유가 생기면 이 잡음 가설(세그먼트가 잡음을
만든다)부터 확인하는 게 순서다 — 트레일링 규칙 자체를 다시 스윕하지 않는다.

### ③ ★★ 업종중립 PBR 라인 — CLAUDE.md가 낡아 있었다, 오늘 정정함

CLAUDE.md 09-02 항목이 "업종중립 PBR 계열만... HOLD"라고 적혀 있었는데
**실제로는 다음날(09-03)에 이미 REJECT로 종결**돼 있었다(문서 자신이
"라인을 종료한다"고 적음). 세 블로커(성장계열 묶기·축 단독·top-30
전체53축 재검증) 전부 09-03에 순서대로 시도돼 실패했다. CLAUDE.md와
원본 findings 2건(sector-neutral-pbr-growth·sector-neutral-engine-
verification) 라벨을 오늘 REJECT로 정정했다. **이 라인은 더 볼 게 없다
— 재론 안 함.**

### ④ 미커밋 파일 — 없음

이 세션에서 만든 것 전부 커밋·push 완료(§2 목록). 작업트리에 남은
`research/strategy-lab/futures/*`·`eval_f2_shadow.py`·`run_f2_shadow.py`는
**OpenCode 병행 작업**이라 이번에도 손대지 않았다(여러 세션째 그대로).

---

## 1. 오늘 세션 흐름

```
① 어제 인수인계 확인 → 미커밋 3개 파일이 이미 커밋돼 있었음을 확인
   (파일이 실제로 git log에 있는지 대조해서 확인, 그냥 믿지 않음)
② 소형주 부분익절 트레일링 재스윕(10~30%, 1%p 단위) — 5점 격자의 "20%
   봉우리"가 착시였음을 확인(이봉 곡선, 진짜 최댓값은 24%로 이동) → 노이즈
   확정 → findings 갱신·커밋(2e7a87c)
③ TRAIN/VALID/TEST 구간별 분해(사용자 요청) — 중형주 후보가 원 전략보다
   VALID를 깎는다는 것 발견 → findings 갱신·커밋(6214c4f)
④ "production 채택 검토 시작해줘" → 라이브 엔진(paperEngine.py) 구조 확인,
   현재 부분매도·트레일링 로직이 라이브 경로엔 전혀 없다는 것 확인 →
   HOLD 유지 권고
⑤ "트레일링 더 조사할 수 없나? HOLD 전략들 정리하자" → 4트랙으로 나눠
   전부 진행(사용자가 AskUserQuestion에서 4개 다 선택):
   - 라벨 정리 4건(dd252·pead·lowmom60 2건) — 커밋(5069798)
   - 재판정 2건(kr-foreign-flow·quality-composite) — 같은 커밋
   - 트레일링 독립 재현(EY·lowmom60) — 커밋(1ae51db)
   - PBR/EY production 🔴 조사 착수 — keep-paper-candidate-final-
     verification 문서 발견, EY의 "final-robustness" finding을 전문
     읽고 PBR 중복 문제 발견 → 권고 제시
⑥ "EY+PBR 결합 축으로 재설계하면 살아나는지 봐줘" → 기존 raw composite
   REJECT(09-06) 확인 → PBR로 잔차화한 EY 버전 새로 만들어(스크립트
   `10_pbr_ey_resid_composite_oos.py` 신설) 검증 → 처음엔 REJECT 판정 →
   커밋(8598a58, 이후 정정됨)
⑦ 사용자가 "-0.8%가 뭔 뜻이냐, 뺄 정도는 아닌거 아니냐" 지적 → 판정 로직이
   "실질 동등" 조항을 빠뜨린 기계적 오판정이었음을 인정 → 연도별 분해
   추가해서 재확인 → **하락장 3개 해(2018·2022·2024) 전부 composite이
   PBR보다 더 잃는다**는 새 문제 발견(2022는 거의 2배) → REJECT를 HOLD로
   정정 → 커밋(cc8a8c2)
⑧ "PBR combined만 진행, EY는 보류하고 섹터뉴트럴 콤보스윕까지 볼까" →
   CLAUDE.md에 PBR/EY 🔴 결정 기록(f276856) → 섹터뉴트럴 조사 시작 →
   09-03에 이미 REJECT로 종결돼 있었다는 것 발견 → CLAUDE.md·findings
   라벨 정정(051dcc1)
```

## 2. 커밋 목록 (오늘, 전부 push 완료)

```
2e7a87c  research(strategy-lab): 소형주 부분익절 트레일링 재스윕 — 노이즈 확정
6214c4f  research(strategy-lab): 부분익절 후보 TRAIN/VALID/TEST 분해 — 중형 VALID 약화 확인
5069798  research(strategy-lab): HOLD 라벨 정리 6건 — 4건 stale 갱신, 2건 재판정
1ae51db  research(strategy-lab): 트레일링 독립 재현 — 전략마다 다르다, 중형주 붕괴는 세그먼트 잡음 의심
5b4c052  research(strategy-lab): PBR+EY 잔차화 composite 재설계 — REJECT 유지, 격차는 절반 이하로 축소
cc8a8c2  research(strategy-lab): PBR+EY잔차 composite 판정 정정 REJECT→HOLD — 하락장 약점 신규 발견
f276856  docs(claude.md): PBR combined production GO, EY 보류 확정 기록 (2026-09-18)
051dcc1  docs: 업종중립 PBR 라인 REJECT 종결 반영 — CLAUDE.md·findings 라벨 정리
```

(8598a58는 5b4c052와 cc8a8c2 사이의 실수 커밋 — REJECT 오판정을 그대로
push했다가 바로 다음 커밋에서 HOLD로 정정. 히스토리에는 남아있음, revert
안 하고 앞으로 고침.)

## 3. 오늘 라벨·판정이 바뀐 findings 전체 목록

| 파일 | 이전 | 이후 | 사유 |
|---|---|---|---|
| dd252-skip1m-verification-2026-08.md | HOLD | REJECT | 다른 finding이 이미 최종 REJECT, 라벨만 stale |
| pead-quarterly-pilot-2026-08.md | HOLD | REJECT | 〃 |
| lowmom60-candidate-c-engine-verification-2026-08.md | HOLD | REJECT | 〃 |
| lowmom60-exposure-overlay-timing-value-2026-08.md | HOLD | REJECT | 〃 |
| kr-foreign-flow-52wlow-per-2026-08.md | HOLD | REJECT | 문서 자신의 §6·7 서술을 판정기준에 대입(VALID/TEST 부호일관성 위반) |
| 06-quality-composite-oos-2026-09.md | HOLD | REJECT | 문서 자신의 표가 이미 적은 3가지 게이트 미충족 |
| sector-neutral-pbr-growth-2026-09.md | HOLD | REJECT | 다음날 최종 REJECT로 종결, 라벨만 stale |
| sector-neutral-engine-verification-2026-09.md | HOLD | REJECT | 〃 |
| pbr-ey-resid-composite-oos-2026-09.md(신설) | (REJECT 오판정) | HOLD | 판정기준 "실질동등" 조항 누락 정정 + 하락장 약점 신규 발견 |

**EY production 판정 요약** (CLAUDE.md에 기록됨, 재론 시 여기부터 시작):

| 경로 | 문서 | 결과 |
|---|---|---|
| EY 단독 | earnings-yield-final-robustness-2026-09.md | PBR로 잔차화하면 포트 알파 1/3로 퇴색 |
| PBR+EY raw 50:50 | pbr-ey-composite-oos-2026-09.md | REJECT (희석만 됨) |
| PBR+EY 잔차화 50:50 | pbr-ey-resid-composite-oos-2026-09.md | HOLD (TEST 실질동등, 하락장 3개 해 전부 PBR보다 더 잃음) |

## 4. 검증되지 않은 채 넘어간 것 (다음 세션 주의)

- **PBR+EY 잔차화 composite의 하락장 약점 원인 미검증** — 가설만 있다
  (EY잔차가 이익모멘텀/퀄리티 성격이라 가치주가 버텨야 할 하락장에서
  오히려 방어력을 깎았을 것). 검증 안 됨, 다음 후속 후보
  (pbr-ey-resid-composite-oos-2026-09.md §판정 참고).
- **부분익절 세그먼트 잡음 가설도 미검증** — "중형주만 뗀 세그먼트가
  잡음을 만든다"는 오늘 나온 정황 증거(전체 포트폴리오로 재면 VALID
  악화가 10분의 1)일 뿐, 왜 그런지는 모른다.
- 크립토 event-independence 3건(s1/s3a/s6)·insider-trading 1건은
  표본부족(N<30, 17~22개월)으로 여전히 대기 — 재분석으로 안 풀린다,
  시간이 더 지나야 한다. 이번 세션에서 손 안 댐.

## 5. 작업트리 상태 (세션 종료 시점)

```
research/strategy-lab/eval_f2_shadow.py 등 futures/stage5_4_*, stage6_1_*   OpenCode 병행 작업 - 손대지 않음(여러 세션째)
그 외 전부 클린 — 이 세션에서 만든 것 전부 커밋·push 완료
```

원격에는 이 세션 이후에도 봇 커밋(positions·findings.json·news 등)이
계속 쌓이고 있다 — 다음 세션 시작 시 `git pull`(또는 fetch+rebase) 먼저.
