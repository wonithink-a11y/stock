# 세션 인수인계 — 2026-09-14 (RV20 선물 모의투자 상시 자동화 가동 + 업비트·빗썸 인프라 신설)

`세션인수인계-2026-09-13-b.md`(DRAM 라인 보류 확정)의 다음 세션. 사용자가
Stage 6-1 독립검증을 요청한 데서 시작해, 연구 검증 → 규칙 동결 → 실제
모의투자 자동화 구축 → 업비트/빗썸 인프라 신설까지 하루 안에 쭉 이어졌다.
커밋 10개(`97026eb`~`a9e6c44`), 전부 push 완료.

---

## 0. 다음 세션이 먼저 볼 것

### ① ★★★ RV20 선물 sizing 규칙 — 오늘(2026-09-14) 09:05 KST 첫 실주문 예정, 아직 발생 전

**가장 먼저 확인할 것**: VM(`stock`)에 SSH 접속해서
`~/collector-venv/logs/rv20-futures-paper-order.log`를 확인한다. 이
세션 종료 시점(2026-09-14 00:57 KST)엔 아직 실행 전이었다 — 다음
세션이 열리는 시점엔 이미 돌았을 가능성이 높다.

**배경**: Stage 6-1(OpenCode 산출물) 독립검증에서 "Strict Holdout OOS"가
사실은 discovery 표본 재게시였음을 발견(§2 참고) → 진짜 OOS를 만들려면
규칙을 동결하고 시간이 지나기를 기다리는 수밖에 없다는 결론 →
`futures-rv20-sizing-rule-freeze-2026-09-13.md`로 동결 → 사용자가 그
동결 규칙(Rule B: RV20 rolling252 percentile Q5→0x, Q5=0.8)을 KIS
모의투자 국내선물 계좌에 실제로 적용해보자고 요청 → 당일 안에 전체
파이프라인 구축·VM 배포·kill switch 켜기까지 완주.

**핵심 설계**(전부 커밋 `b4cab0b`~`a9e6c44`):
- 신호: `rv20_sizing_paper_signal.py` — stage5_1/stage5_3 함수 재사용,
  동결 규칙 재구현 안 함.
- 실행: `rv20_paper_order.py` — 신호계산 → KIS 잔고조회 → front-month
  전광판 조회(표준 코스피200선물은 market class **"K2I"**, "MKI"는
  미니선물이라 승수가 다름 — 실측으로 확인) → 목표계약수 차이 계산 →
  주문(기본 dry-run, `--confirm-live`로만 실전송).
- **Kill switch**: `rv20_automation_enabled.json`(저장소에 커밋된 파일,
  기본값 false) — `--confirm-live`를 줘도 이 값이 false면 dry-run으로
  강등한다. 켜고 끄는 유일한 방법은 GitHub Actions
  "RV20 futures automation on/off switch"(workflow_dispatch, KIS
  자격증명 안 씀 - 순수 git 커밋이라 공개저장소에 안전) — **오늘
  `a9e6c44`로 사용자가 직접 켰다(enabled: true).**
- VM 자동화: `deploy/rv20-futures-paper-order.service`·`.timer`
  (평일 09:05 KST 1회). **설치는 사용자가 SSH로 직접 함** — 이 세션은
  VM 접근 권한이 없다(auto-mode classifier가 스케줄러 등록·자동주문
  실행을 Claude가 스스로 하는 걸 두 번 다 막았다 - 의도된 안전장치로
  받아들이고 존중, 우회 시도 안 함).
- UI 표시: `ui/tabs/chart.js` 상단에 on/off 카드(읽기 전용, 클릭 버튼
  아님 - 공개 정적 페이지에 쓰기 자격증명을 안 둔다는 원칙).

**VM 설치 중 실측으로 발견·수정한 버그 2건**(`f0dbb7d`·`63c50e0`,
로컬 Windows에서만 돌던 코드를 VM Linux에 처음 배선하며 드러남):
1. `collect_kospi200_daily_krx.py`·`stage5_1_volatility_event_study.py`
   가 `REPO = Path(r"C:\Users\User\projects\stock")`로 하드코딩 — VM에서
   `FileNotFoundError`. `Path(__file__).resolve().parents[N]`으로 포터블화.
2. `collect_kospi200_daily_krx.py`의 `load_env()`가 저장소 루트 `.env`만
   읽어서, VM의 `EnvironmentFile`(`~/collector-venv/.env`, 저장소 루트가
   아님)을 못 봤다. `kisVtsClient.py`의 `_load_env()`와 같은 패턴(파일은
   있으면만 읽고, `os.environ`에 있으면 그걸로 덮어쓴다)으로 고침.
   **VM에서 수동 테스트할 때는 `set -a; source ~/collector-venv/.env;
   set +a`를 먼저 해야 한다** — systemd EnvironmentFile은 자동으로
   환경변수를 주입하지만 사람이 `nohup python3 ...`으로 직접 돌리면 그
   자동 주입이 안 일어난다(이 세션에서 반복 확인).

**검증 완료 상태**: VM에서 전체 기간(2010~2026, 25,282행·4,108일) 첫
수집 완료, `rv20_paper_order.py --confirm-live`가 kill switch OFF일 때
정확히 dry-run으로 안전하게 강등되는 것까지 실측 확인(신호·잔고조회·
front-month조회·목표계약수 계산 전부 정상). kill switch는 이제 ON —
다음 09:05 실행이 **이 파이프라인의 첫 실제(모의) 주문**이다.

### ② ★ 업비트·빗썸 모의매매 인프라 신설 — 전략 없음, 연결만 확인됨

사용자가 "이 형태(RV20 선물 자동화)를 업비트·빗썸에도 만들어달라, 실투자도"
라고 요청 → **실계좌 자동매매(진짜 돈)는 명확히 거절**(Claude의 운영
원칙 - 실행 금지 항목, 프로젝트 규칙이 아니라 상위 규칙이라 예외 없음) →
"인프라만 먼저, 전략은 나중에"로 범위 합의.

- **업비트**: 이미 이전 세션이 `upbitClient.py`·`upbitBroker.py`(dormant)·
  `upbitPaperBroker.py`를 만들어둔 상태였다 — 새로 만들 게 없었음.
- **빗썸**: 오늘 신규 구축(`2301816`). `upbitClient.py`와 같은 구조지만
  apidocs.bithumb.com 공식 문서를 직접 확인해 스펙 차이 세 가지를
  반영했다 — JWT가 HS512가 아니라 **HS256**, payload에 **timestamp**
  필수, 주문 엔드포인트가 `/v1`이 아니라 **`/v2`**. 실계좌 주문 어댑터
  (`bithumbBroker.py`)는 업비트처럼 **dormant**(코드는 있지만 실행할
  드라이버가 없다). 회귀 17/17 통과, 실제 공개 API·키 확인까지 실측
  완료(BTC 0.00007824 보유 확인 - 필드명 추정이 맞았음이 검증됨).
- **오라클 VM 공인 IP**: 129.225.145.14로 업비트·빗썸 둘 다 화이트리스트
  등록 완료(오타 있었다 수정함). **Ephemeral(임시) 상태 그대로 진행하기로
  결정** — Reserved로 전환하는 안내는 했으나 사용자가 "일단 지금 IP로
  진행"을 선택. **재부팅 등으로 IP가 바뀌면 업비트·빗썸 화이트리스트
  둘 다 다시 등록해야 한다** — KIS·KRX는 키 기반 인증이라 영향 없음.
- **관찰(수정 안 함, 범위 밖)**: 업비트·빗썸 poll-once 스모크 둘 다
  `entry_price=0.0`으로 나온다 — `paperEngine.py` 공유 로직의 기존
  특성이고 업비트 것도 동일 재현된다(빗썸만의 결함이 아님).

### ③ Stage 6-1 독립검증 + provenance 감사(연구, 이미 종결) — 요약만

전체 경위는 이 대화의 앞부분(이 문서보다 먼저 나온 메시지들) 또는
`futures-rv20-sizing-rule-freeze-2026-09-13.md`를 참고. 핵심만:

- Stage 6-1(OpenCode의 "Strict Holdout OOS")을 clean-room 재구현으로
  검증 → 수치는 REPRODUCED(48개 조합 전부 일치).
- 그러나 provenance 감사에서 "OOS"라 부른 2025-2026 구간이 사실은
  Stage 5-1(최초 발견)이 애초에 전체 이력을 보고 "레짐 오염 검증"에
  능동적으로 썼던 바로 그 구간이었음을 확인 — 이 라인의 "OOS"는 전부
  discovery 표본 재게시였다.
- Stage 5-4(연속 sizing)도 별도로 독립 재현 완료(12개 조합 전부 일치).
- 결론: 규칙을 오늘(2026-09-13) 시점으로 동결하고, 이후 실제로 새로
  쌓이는 거래일만 "진짜 OOS"로 인정하기로 함 — 그 실행이 바로 §①.

### ④ 크립토 funding carry REJECT — 전날 이미 종결, 오늘은 손 안 댐

`세션인수인계-2026-09-13.md`에서 이미 REJECT·재도전조건 없음으로
닫힌 건. 오늘 세션 중 "실험실에 완료된 크립토 실험데이터 검증해달라"는
요청에 `run_f2_shadow.py`/`eval_f2_shadow.py`(OpenCode가 만든 forward
관측 도구, 전략 아님)를 코드 감사만 했다 — 읽기전용·공개API만 사용
확인, 결함 없음, 관측치 1건뿐이라 통계적으로 아직 아무 결론도 안 남.

---

## 1. 오늘 세션 흐름 (요약)

```
① Stage 6-1 독립검증 요청 → REPRODUCED 확인 + provenance 감사 →
   "OOS가 discovery 재게시였다" 발견 → 규칙 동결 메모 작성
② "동결 메모 finding으로 남겨줘 + 신뢰성 높은 규칙 모의투자 선물에
   50%만 적용해서 실험해볼 수 있나" → 범위 확정(자본 50%, Rule B) →
   신호계산+KIS futures client+주문 스크립트 구축, 로컬 dry-run 검증
③ "VM에서 자동으로 돌게 만들어줘" → schtasks 시도 2회 classifier 차단 →
   .bat 파일만 작성(실행은 사용자 몫)로 방향 전환
④ "VM+UI 토글까지 한번에" → kill switch(repo 커밋 flag) + GitHub
   Actions 토글 워크플로 + systemd timer + UI 카드 구축
⑤ 사용자가 VM에 직접 SSH해서 설치 → scipy 누락·REPO 하드코딩·
   .env 위치 불일치 3개 버그 실측 발견 → 전부 수정·push
⑥ VM에서 전체 수집 완료 확인 → dry-run 스모크 성공 확인
⑦ "크립토 funding carry와 futures 완료 실험데이터 검증해달라" →
   Stage 5-4 독립재현 + F2 shadow 코드감사
⑧ "업비트·빗썸도 이 형태로, 실투자도" → 실투자 거절 설명 →
   "인프라만 먼저"로 범위 합의 → 빗썸 신규 구축(업비트는 이미 있었음)
⑨ 빗썸 API 버전(2.0 선택)·IP 등록·오라클 Reserved IP 안내 →
   사용자가 Ephemeral 유지 결정 → 업비트·빗썸 키 연결 확인 완료
⑩ kill switch를 GitHub Actions로 사용자가 직접 켬(enabled: true) →
   이 인수인계 문서 작성
```

## 2. 커밋 목록 (전부 push 완료, `97026eb`~`a9e6c44`)

```
97026eb  RV20 sizing 규칙 동결 메모
b4cab0b  RV20 sizing 규칙 선물 모의투자 파이프라인 (Rule B, 50% 자본)
62a9e08  RV20 선물 모의주문 1회 실행기 + 등록용 배치파일
95f9acb  RV20 선물 모의주문 상시 VM 자동화 + kill switch + UI 표시
f0dbb7d  collect_kospi200_daily_krx.py·stage5_1 REPO 경로 포터블화
63c50e0  collect_kospi200_daily_krx.py가 VM의 EnvironmentFile도 읽게
2301816  빗썸 모의매매 인프라 (업비트와 동일 아키텍처)
bc3ea81  빗썸 키 확인 스모크 스크립트 (읽기 전용)
a9e6c44  RV20 futures automation true (GitHub Actions, 사용자 실행)
```
(사이에 `7e4a76b`은 무관한 자동 커밋 - weekly backtest report)

## 3. 남은 것 / 다음에 확인할 것

- **최우선**: 오늘 09:05 KST 첫 실주문 로그 확인(§0①). 성공/실패 모두
  기록으로 남긴다 — 실패해도 "왜"를 정직하게 적는 게 중요(교훈57 정신).
- 오라클 VM IP를 Reserved로 전환할지 — 미정, 필요해지면(재부팅 등으로
  IP가 실제로 바뀌는 사고가 나면) 그때 다시 논의.
- 업비트·빗썸에 얹을 실제 전략 — 미정. 오늘 확인된 크립토 관련 결론은
  "funding carry REJECT"뿐이라, 이 인프라에 바로 얹을 검증된 전략이
  아직 없다.
- Stage 6-1 provenance 감사가 제안한 "재개 조건"(규칙 동결 이후 실제
  새 거래일 축적)은 이미 §①의 VM 자동화가 그 실행 자체다 — 별도 착수
  불필요, 그냥 시간이 지나면 쌓인다.

## 4. 작업트리 상태 (세션 종료 시점)

전부 커밋·push 완료. 루틴 diff만 남음(반복되는 기존 판단 그대로 커밋
안 함): `research/strategy-lab/data/leveraged-etf/_manifest.json`·
`research/strategy-lab/data/market-regime/_manifest_fred_extended.json`·
`ui/data/macro.json`.

OpenCode가 병행 작업 중이던 미커밋 파일들(`futures/stage5_4_*`·
`stage6_1_*`·`eval_f2_shadow.py`·`run_f2_shadow.py` 등)은 이 세션이
손대지 않았다 — 사용자 직접 위임 실험이라 그대로 둠(기존 메모리 정책).
