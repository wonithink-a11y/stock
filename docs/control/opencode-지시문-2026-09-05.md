# OpenCode 작업지시서 — 기존 실험 재실행·수치 보강 (2026-09-05)

CLAUDE.md "OpenCode 위임 기준"(2026-08-19 확정)을 따른다.

```bash
# 반드시 프로젝트 루트에서, 반드시 모델을 매 호출 명시한다.
# 생략하면 다른 기본 모델로 조용히 돈다(실측 2026-08-19).
opencode.cmd run -m opencode/big-pickle "<지시문>"
```

**모델 순위 (2026-09-05 사용자 지정)**

| 순위 | 모델 | 비고 |
|---|---|---|
| 1 | `opencode/big-pickle` | **동시 3개까지 병렬 가능** |
| 2 | `opencode/nemotron-3-ultra-free` | 1순위가 막히면 |

★ **CLAUDE.md 가 적어둔 `opencode/deepseek-v4-flash-free` 는 더 이상 존재하지
않는다** — `opencode.cmd models` 실측(2026-09-05) 목록에 없다. 그 이름으로
호출하면 실패하거나 다른 모델로 조용히 돈다. 살아 있는 무료 계열은
`big-pickle` · `nemotron-3-ultra-free` · `nemotron-3.5-lightning-free` ·
`ling-3.0-flash-fin-free` · `mimo-v2.5-free` 다.

**어느 모델로 돌았는지 결과 문서 첫 줄에 반드시 적는다.**

## 전 지시문 공통 제약 — 지시문마다 이 블록을 붙인다

```
[제약]
- 결과는 research/strategy-lab/.cache/opencode-out/ 아래 새 .md 로만 쓴다.
  ★ docs/verification/ 에 직접 쓰지 않는다 - 그 경로는 Claude 가 검토 후
    옮겨 적는 relay 자리다(CLAUDE.md "쓰기 권한은 경로가 정한다").
- data/backfill/ · manifest · config/ · findings/ · docs/ 는 절대 수정하지 않는다.
- git commit / push 하지 않는다.
- 기존 스크립트를 고치지 않는다. 실행이 안 되면 "실행 불가"와 그 이유를 적는다.
- 판정(KEEP/REJECT/HOLD)을 내리지 않는다. 관측치와 표만 남긴다.
- 수치를 어떤 문서의 frontmatter 에도 써넣지 않는다.
- 어느 모델로 실행했는지 결과 문서 첫 줄에 적는다.
```

★ **산출 경로를 `.cache/opencode-out/` 로 바꾼 이유**: CLAUDE.md 경로 표는
`docs/verification/` 의 writer 를 "실험실 (또는 OpenCode 결과를 Claude 가 옮겨
적는다 — relay)" 로 정해두었다. OpenCode 가 직접 쓰면 두 번째 writer 가 생긴다.
`.cache/` 는 gitignore 대상이라 저장소를 오염시키지 않는다. Claude 가 읽고
검토한 뒤 `docs/verification/` 으로 옮긴다.

## 병렬 실행

`big-pickle` 은 동시 3개까지 돈다. 읽기 전용 지시문(2·3·4·8·9·10)은 서로
독립이므로 3개씩 묶어 돌린다. 실제 재실행(1·5·6·7)은 같은 파일
(`.cache/`·`reports/`)에 쓸 수 있으므로 **병렬로 돌리지 않는다.**

```
배치 1 (읽기 전용)   2 · 3 · 4
배치 2 (읽기 전용)   8 · 9 · 10
배치 3 (재실행)      1 단독 -> 그 결과를 보고 5 · 6 · 7
```

**왜 frontmatter 자동 기입을 금지하는가**: 2026-09-03 실측에서 `lowmom60`
문서의 본문 표 1열이 **폐기된 옛 값(5.09%)**이고 정정값은 2열(4.76%)이었다.
자동 추출은 전날 폐기한 숫자를 화면에 올린다(교훈57). 승격은 사람이 한다.

**생산자·검증자 겸임 금지**: OpenCode 가 만든 표를 OpenCode 가 검증하게 하지
않는다. 검증은 Claude 또는 사용자가 한다.

---

## 우선순위 요약

| # | 지시문 | 유형 | 값 |
|---|---|---|---|
| 1 | 전략 35개 MTM baseline 전수 측정 | 재실행 | ★★★ |
| 2 | 실현손익 회계 잔존 29파일 3분류 | 스캔 | ★★★ |
| 3 | 판정 있는데 수치 0인 17건 재실행 가능성 조사 | 스캔 | ★★★ |
| 4 | CLAUDE.md 인용 수치 ↔ 원문 findings 전수 대조 | 대조 | ★★★ |
| 5 | 비용 민감도 0/20/40/60bp 전수 재실행 | 변수변경 | ★★ |
| 6 | pbr_combined 파라미터 격자 확장 | 변수변경 | ★★ |
| 7 | 역변동성 가중 창 격자(20/60/120일) | 변수변경 | ★★ |
| 8 | 난수 바닥선 미적용 findings 목록화 | 스캔 | ★★ |
| 9 | 전체기간 풀링만 있는 findings 목록화 | 스캔 | ★★ |
| 10 | findings 수치 공백을 "정상/구멍"으로 분류 | 스캔 | ★ |

1~4 가 본체다. 5~7 은 실제 재실행이라 시간이 든다. 8~10 은 읽기만 한다.

---

## 1. 전략 35개 MTM baseline 전수 측정 ★★★

**왜**: `research/strategy-lab/strategies/` 에 전략이 35개인데 월별 시가평가
baseline 이 측정된 건 `5dc_v1a_p` 하나뿐이다
(`reports/2026-09-03-mtm-baselines/mtm-baselines.json`). 도구
(`mtm_baseline.py`)는 이미 있고 새 로직이 0줄이라 순수 재실행이다.

```
research/strategy-lab/mtm_baseline.py 를 사용해 research/strategy-lab/strategies/
아래 모든 전략 디렉터리(policy.json 이 있는 것)의 baseline 성과를 측정하라.

  python research/strategy-lab/mtm_baseline.py <strategyId> --out <경로>

- 먼저 `python research/strategy-lab/mtm_baseline.py --selftest` 로 도구가
  동작하는지 확인하고 그 결과를 적어라.
- 전략 목록은 strategies/ 하위 디렉터리 중 policy.json 이 있는 것으로 만든다.
  base.py 같은 파일은 전략이 아니다.
- 크립토 전략(crypto*)은 KR 캘린더와 다르므로 별도 표로 분리한다.
- 각 전략마다: strategyId · universeMode · runClass · CAGR · MDD · Sharpe ·
  Calmar · closedPositionCount · elapsedSeconds 를 표로 만든다.
- 실행이 실패하면 그 전략의 행에 실패 이유(예외 메시지 첫 줄)를 적고 넘어간다.
  하나가 실패해도 나머지를 계속 돌린다.
- 총 소요시간과 실패 건수를 마지막에 요약한다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-mtm-baseline-전수-2026-09-05.md
[공통 제약 블록]
```

**주의**: 전략에 따라 수 분~수십 분 걸린다(`5dc_v1a_p` 전체 재실행이 847초 걸린
전례). 순차로 돌리고 중간 결과를 계속 파일에 append 하게 한다.

---

## 2. 실현손익 회계 잔존 파일 3분류 ★★★

**왜**: CLAUDE.md 가 "eq += pnl 패턴이 13개 스크립트에 남아 있다"고 적었는데
실제 grep 히트는 29개다. 어느 것이 (a) 이미 수정됨 (b) A/B 비교라 방향 보존
(c) **절대 MDD·Sharpe 를 인용 중이라 무효**인지 갈라야 한다.

```
research/strategy-lab 아래 .py 파일 중 자산곡선을 "청산일에만 손익을 적립"하는
방식(eq += pnl / equity += pnl 류)으로 만드는 파일을 전부 찾아 다음 3분류로 나눠라.

  A. 이미 수정됨 - 월별 시가평가(schedule_with_monthly_mtm / curve_metrics)를
     import 하거나 호출하고, 옛 방식은 deprecated 로만 남아 있다
  B. A/B 비교 전용 - 두 갈래에 같은 회계를 쓰므로 방향(어느 쪽이 나은가)은
     보존된다. 단 절대 MDD·Sharpe 는 무효다
  C. 절대수치를 산출·인용 - 위 둘 다 아니고 resultTable 등에 CAGR/MDD/Sharpe 를
     그대로 뱉는다. 가장 위험하다

각 파일마다: 경로 · 분류 · 근거가 된 코드 줄 번호 · 그 파일이 만드는 산출물
경로(있으면) · 그 산출물을 인용하는 findings 문서(grep 으로 파일명 검색).

tests/ 아래 파일은 테스트 fixture 이므로 별도 표로 분리한다.

★ 코드를 고치지 마라. 분류표만 만든다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-실현손익회계-분류-2026-09-05.md
[공통 제약 블록]
```

---

## 3. 판정은 있는데 수치가 0인 17건 — 재실행 가능성 조사 ★★★

**왜**: KEEP 5 · PASS 4 · HOLD 8 건이 CAGR·Sharpe·MDD·승률을 하나도 안 갖고 있다.
판정을 내렸는데 숫자가 없다. 다시 돌릴 수 있는지부터 확인해야 한다.

```
아래 17개 findings 문서 각각에 대해 "지금 다시 돌릴 수 있는가"를 조사하라.

KEEP: kr-foreign-flow-5d-independent-verification-2026-08 ·
      order-flow-imbalance-audit-2026-08 · pbr-combined-2022-concentration-2026-08 ·
      pbr-combined-oos-validation-2026-08 · v3-5dc-signal-independence-2026-08
PASS: axis-weight-sweep-2026-09 · factor-earnings-yield-robustness-2026-08 ·
      factor-earnings-yield-train-valid-test-2026-09 ·
      kr-production-technical-macross-reversal-2026-09
HOLD: dd252-skip1m-verification-2026-08 ·
      factor-earnings-yield-2021-concentration-2026-08 ·
      factor-earnings-yield-macro-rate-regime-2026-08 ·
      factor-earnings-yield-mtm-reverification-2026-08 ·
      insider-trading-precheck-2026-08 · kr-foreign-flow-52wlow-per-2026-08 ·
      pbr-dropout-maxexcl-t1t3-decomposition-2026-08 · pead-quarterly-pilot-2026-08

각 문서마다 조사할 것:
  1) 이 문서를 만든 스크립트가 무엇인가 (문서 본문에 적혀 있거나, 파일명·팩터명으로
     research/strategy-lab 안에서 grep)
  2) 그 스크립트가 저장소에 커밋돼 있는가
  3) 지금 실행 가능한가 - 필수 인자, 입력 파일 존재 여부를 확인한다.
     ★ run_5dc_v1a_p_merged.py 처럼 로컬에 없는 CI 아티팩트를 필수 인자로 받아
     실행 자체가 안 되는 전례가 있다. --help 를 찍어보고 입력 경로의 존재를 확인하라.
  4) 그 문서가 백테스트 성과(CAGR/MDD/Sharpe)를 낼 수 있는 종류인가,
     아니면 IC·감사 문서라 애초에 낼 수 없는가

표: 문서 · 스크립트 · 커밋됨 · 실행가능 · 성과수치 산출가능 · 막힌 이유

★ 실제로 실행하지는 마라. 이번엔 "돌릴 수 있는가"만 본다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-수치공백-재실행가능성-2026-09-05.md
[공통 제약 블록]
```

---

## 4. CLAUDE.md 인용 수치 ↔ 원문 findings 전수 대조 ★★★

**왜**: 2026-09-04 에 낡은 인용 2건이 발견됐다(PBR baseline CAGR 4.72%→실제
5.49%, `factor-single-backtest-kr` 의 Sharpe·MDD 가 실현손익 회계라 인용 금지).
CLAUDE.md 는 매 세션 읽히므로 낡은 숫자가 계속 재생산된다.

```
CLAUDE.md 본문에 나오는 모든 성과 수치(CAGR · MDD · Sharpe · Calmar · IC · t값 ·
승률 · 거래건수)를 뽑아, 그 수치가 인용하는 findings 원문과 대조하라.

  1) CLAUDE.md 에서 수치와 그 옆에 적힌 출처(findings 파일명·커밋 해시)를 뽑는다
  2) 그 findings 파일을 열어 같은 수치가 있는지 찾는다
  3) 일치 / 불일치 / 원문에서 못 찾음 / 출처 미표기 로 분류한다

★ 특히 다음을 표시하라:
  - findings 안에 "인용 금지" · "폐기" · "정정" · "deprecated" 같은 표현과 함께
    있는 수치를 CLAUDE.md 가 여전히 인용하는 경우
  - 같은 지표가 CLAUDE.md 안에서 서로 다른 값으로 두 번 이상 나오는 경우

표: CLAUDE.md 줄번호 · 지표 · CLAUDE.md 값 · 원문 값 · 출처파일 · 판정

★ CLAUDE.md 를 수정하지 마라.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-CLAUDEmd-수치대조-2026-09-05.md
[공통 제약 블록]
```

---

## 5. 비용 민감도 0/20/40/60bp 전수 재실행 ★★

**왜**: 비용 가정이 문서마다 제각각이다(30bps RT · 60bps RT · 편도 20~30bp ·
"cost 50bps"라고 부르면서 실제로는 왕복 100bp 였던 전례까지 있다). 하나의
격자로 통일해야 비교가 된다. 격자는 lead-lag 사전등록 §16-E 와 맞춘다.

```
research/strategy-lab 에서 거래비용을 인자로 받는 백테스트 스크립트를 찾아,
동일한 격자(왕복 0 / 20 / 40 / 60 bp)로 재실행하라.

  1) 먼저 비용 인자를 받는 스크립트를 전부 찾는다(cost_bps · entryCostBps ·
     exitCostBps · --cost 류). 각 스크립트가 그 인자를 "편도"로 쓰는지 "왕복"으로
     쓰는지 코드에서 확인해 표에 적는다. ★ 이게 과거에 실제로 틀린 적이 있다 -
     entryCostBps = exitCostBps = cost_bps 로 두고 "50bps"라 부르면 실제는 왕복 100bp다
  2) 편도/왕복 규약을 확인한 뒤, 왕복 기준 0/20/40/60bp 가 되도록 인자를 넣는다
  3) 각 전략 × 각 비용에서 CAGR · MDD · Sharpe · 거래건수를 표로 만든다

★ 어느 비용에서 부호가 바뀌는지(순수익이 양->음) 를 표에 별도 열로 표시하라.
★ 규약이 코드에서 불분명하면 추측하지 말고 "규약 불명"으로 적고 그 스크립트는 건너뛴다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-비용격자-2026-09-05.md
[공통 제약 블록]
```

---

## 6. pbr_combined 파라미터 격자 확장 ★★

**왜**: CLAUDE.md 가 직접 인정한 한계다 — "격자가 3×3으로 좁고 (nDrop<2 ·
percentile 극단값 미확인)". 격자 확장은 판단이 아니라 기계적 재실행이다.

```
pbr_value_v1_combined 의 파라미터 스윕을 기존 3x3 격자에서 확장해 재실행하라.

기존: nDrop ∈ {2,3,5} × percentile ∈ {0.7, 0.8, 0.9}   (최선: nDrop=2, pct=0.8)
확장: nDrop ∈ {1,2,3,4,5,7} × percentile ∈ {0.6,0.7,0.75,0.8,0.85,0.9,0.95}

  1) 기존 스윕을 돌린 스크립트를 research/strategy-lab 에서 찾는다
     (파일명에 combined/paramsweep 이 들어간 것부터 본다)
  2) ★ 먼저 기존 격자점 (nDrop=3, pct=0.8) 을 재현해 기존 finding 수치와
     일치하는지 확인하라. 일치하지 않으면 거기서 멈추고 보고한다 - 확장한
     격자를 믿을 수 없다
  3) 재현이 되면 확장 격자로 돌리고 CAGR · MDD · Sharpe · Calmar · 회전율을
     격자표로 만든다
  4) nDrop 이 낮아질수록 CAGR 이 단조 증가하는지(기존 관측) 확장 격자에서도
     유지되는지 표에서 보이게 한다

★ "최선 조합"을 고르지 마라. 격자표만 만든다. 선택은 OOS 절차가 있는 별도 작업이다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-pbr-combined-격자확장-2026-09-05.md
[공통 제약 블록]
```

---

## 7. 역변동성 가중 창 격자 ★★

**왜**: CLAUDE.md 가 "단일 실행(60일 창 하나)이라 노이즈와 구분 안 됨,
결론 안 냄"이라고 명시했다. 창을 바꿔 돌리는 건 기계적이다.

```
pbr_value_v1_combined 의 역변동성(1/변동성) 가중 실험을 변동성 창을 바꿔가며
재실행하라. 기존은 60일 창 하나만 돌렸다.

창: 20 · 40 · 60 · 90 · 120 · 250 거래일
대조군: 동일비중(가중 없음) - 반드시 같은 실행에서 같이 낸다

  1) 기존 실험 스크립트를 찾고, 60일에서 기존 finding 수치
     (CAGR 6.87%→6.68% · MDD -18.90%→-17.94% · Sharpe 0.6809→0.6811)를
     재현하는지 먼저 확인하라. 재현 안 되면 멈추고 보고한다
  2) 재현되면 창별로 돌려 CAGR · MDD · Sharpe · Calmar 표를 만든다
  3) 동일비중 대조군 대비 각 지표의 차이(%p)를 열로 추가한다

★ 어느 창이 좋은지 판정하지 마라. 표만 만든다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-invvol-창격자-2026-09-05.md
[공통 제약 블록]
```

---

## 8. 난수 바닥선 미적용 findings 목록화 ★★

**왜**: 난수 귀무분포 절차는 2026-09-02 에 도입됐다. 그 이전 findings 는
바닥선을 안 걸었고, 이 프로젝트 실측으로 **난수 팩터 4,845조합에서 최고
t=3.21** 이 나온다. 바닥선 없이 t≥2 로 내린 결론이 어디에 있는지 알아야 한다.

```
research/strategy-lab/findings/ 의 모든 .md 를 읽고, 다음을 분류하라.

  A. 난수 바닥선(난수/random baseline/귀무분포/null bar/p95)을 실제로 적용했다
  B. t값·IC·유의성으로 결론을 냈는데 바닥선 언급이 없다   ← 목록의 핵심
  C. 애초에 통계 판정 문서가 아니다(데이터 경로·감사·인프라 노트)

B 로 분류된 문서마다: 파일명 · verdict · 문서가 근거로 든 최대 t값 또는 IC ·
작성일 · 그 결론이 CLAUDE.md 에 인용돼 있는지(파일명으로 grep).

★ B 라고 해서 틀렸다는 뜻이 아니다. "바닥선을 안 걸었다"는 사실만 적는다.
★ 재실행하지 마라. 목록만 만든다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-난수바닥선-미적용목록-2026-09-05.md
[공통 제약 블록]
```

---

## 9. 전체기간 풀링만 있는 findings 목록화 ★★

**왜**: `factor-earnings-yield` 가 "즉시 운용 가능"이라고 적었는데 실은 전체기간
풀링값(IC t=6.10)이었고, 구간 분해하니 등급이 내려갔다(2026-09-02). 같은 형태가
더 있는지 봐야 한다.

```
research/strategy-lab/findings/ 의 모든 .md 를 읽고, 통계적 결론을 낸 문서 중
TRAIN/VALID/TEST(또는 IS/OOS, 시간순 분할) 분해가 없는 것을 찾아라.

  A. 구간 분해가 있다 (TRAIN/VALID/TEST · IS/OOS · walk-forward · 연도별)
  B. 전체기간 풀링값만으로 결론을 냈다   ← 목록의 핵심
  C. 통계 판정 문서가 아니다

B 마다: 파일명 · verdict · 인용한 대표 수치 · 표본 기간(적혀 있으면) ·
CLAUDE.md 인용 여부.

★ 특히 verdict 가 KEEP/PASS/HOLD 인데 B 인 것을 표 맨 위로 올려라.
★ 재실행하지 마라.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-구간분해-누락목록-2026-09-05.md
[공통 제약 블록]
```

---

## 10. findings 수치 공백을 "정상 / 구멍"으로 분류 ★

**왜**: frontmatter 있는 findings 200건 중 141건에 수치가 없는데, 그중 98건이
UNCLASSIFIED(IC·감사 노트)라 **수치가 없는 게 정상**이다. "다 채워라"는 잘못된
목표다. 채워야 할 것만 골라야 한다.

```
research/strategy-lab/findings/*.md 의 frontmatter 를 PyYAML 로 파싱해
(build_ui_findings.py 가 쓰는 방식과 같다) 수치 필드(cagr · sharpe · mdd ·
win_rate)가 비어 있는 문서를 다음으로 분류하라.

  A. 정상 공백 - 백테스트를 돌린 문서가 아니다(IC 측정 · 데이터 경로 확인 ·
     코드 감사 · 재현성 검증 · 인프라 노트). CAGR 이 존재할 수 없다
  B. 진짜 구멍 - 본문에 백테스트 성과가 서술돼 있는데 frontmatter 가 비었다
  C. 애매 - 판단이 필요하다

B 마다: 파일명 · verdict · 본문에서 찾은 수치와 그 줄번호 · 본문에 그 수치가
"정정 전" 또는 "폐기"로 표시돼 있는지 여부.

★★ 본문에서 찾은 수치를 frontmatter 에 써넣지 마라. 표에만 적는다.
   실측 근거: lowmom60 문서는 본문 표 1열이 폐기된 옛 값(5.09%)이고 정정값은
   2열(4.76%)이다. 자동 추출은 폐기된 숫자를 화면에 올린다.

결과: research/strategy-lab/.cache/opencode-out/OPENCODE-수치공백-분류-2026-09-05.md
[공통 제약 블록]
```

---

## 실행 순서 권고

```
읽기만 하는 것 먼저 (빠르고 실패해도 잃는 게 없다)
    3 · 4 · 8 · 9 · 10 · 2
        ↓
실제 재실행 (시간이 든다)
    1  (전략 35개 - 가장 오래 걸린다)
        ↓
    5 · 6 · 7  (1 에서 실행 가능한 전략이 확인된 뒤에 돌린다)
```

**1 번을 먼저 돌리고 싶더라도 3 번(재실행 가능성 조사)을 먼저 하는 게 낫다** —
어느 전략이 실행 자체가 안 되는지 모르는 채로 35개를 순차 실행하면 중간에
막힌다.

## 인수 조건

각 결과 문서가 다음을 만족하는지 사람이 확인한 뒤에만 CLAUDE.md·findings 에
반영한다.

- 첫 줄에 실행 모델명이 있다
- 표에 "실행 불가"·"재현 실패" 행이 숨겨지지 않고 그대로 있다
- 판정(KEEP/REJECT/HOLD) 문구가 없다
- `git status` 가 깨끗하다(OpenCode 가 파일을 고치지 않았다)
- 6 · 7 번은 **기존 격자점 재현 확인**이 문서 안에 있다. 없으면 확장 격자를
  믿지 않는다
