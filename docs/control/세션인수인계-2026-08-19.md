# 세션 인수인계 — 2026-08-19

같은 날 여러 창(이 대화·Codex·DeepSeek)이 병렬로 작업했다. **정본이 아니다** — 정본은
GitHub main의 실제 파일과 각 검증 문서 원본이고, 여기는 "오늘 뭘 했고 다음에 뭘
봐야 하는지"만 모은다.

## 새 세션은 여기부터 — 순서대로

### ① 가장 먼저 — Codex의 slot-marginal 발견부터 읽는다

```
research/strategy-lab/findings/slot-marginal-contribution/SLOT-MARGINAL-CONTRIBUTION.md
```

이게 아래 ③(KR-2.3 설계안)보다 **먼저** 읽어야 하는 이유: 이 문서가 KR-2.3
설계안의 핵심 가정 하나를 실측으로 흔든다.

- **현재 코드 그대로(5일 추세 분류)로 측정하니 외국인·기관 IC가 음수다**
  (d120: 외국인 -0.0319, 기관 -0.0205). 내가(Claude) 오늘 낸 A4 분석은 **20일
  연속값**으로 양의 IC(외국인 +0.012, 기관 +0.022)를 찾았는데, **분류
  방식(consistentBuy~consistentSell 5단계)으로 바꾸면 부호가 뒤집힐 수 있다**는
  걸 5일 버전에서 실측으로 확인한 것 — 정확히 KR-2.3 설계안 끝에 내가 "아직
  검증 안 됨"이라고 남겨둔 우려가 실제로 터진 사례다.
- **pbr 슬롯 하나가 전체 19개 슬롯 중 marginal 기여가 압도적으로 크다**
  (ΔIC d120 = +0.0385, full 모델 IC를 0.038→0.076으로 두 배 이상 올림).
  supplyDemand보다 valuation(특히 pbr)이 더 큰 레버일 수 있다 — 지금까지
  이 세션은 supplyDemand에만 집중했는데, 우선순위를 재고할 근거가 나왔다.
- **현재 production이 실제로 연결하는 조합(재무+기술)은 등급별 forward
  return이 전 config에서 단조적이지 않다** — IC가 양수여도 grade 경계
  설정 자체가 별도 문제로 존재한다는, 이번 세션에서 안 다룬 새 이슈.
- 이 문서 자체가 "Claude가 검토한 뒤 docs/verification/·CLAUDE.md에 옮겨
  적어야 한다"(AGENTS.md §3)고 명시하고 있다 — **아직 안 함.** 다음 세션의
  최우선 작업.

### ② 그 다음 — 오늘 이 대화에서 만든 것들

순서대로: TradingAgents 구조 smoke → A4 재분석(marginal IC·개인축 상관관계) →
KR-2.3 SD 축 재설계 스코핑 → 설계안. 아래 §2·§3에 정리.

### ③ 커밋 상태부터 확인

```bash
git log --oneline -10
git status --short
```
오늘 세 번의 커밋이 이미 다른 창에서 일어났다(§4). 이 인수인계가 쓰인 시점
이후에도 다른 창이 계속 커밋할 수 있으니 새 세션은 반드시 재확인한다.

---

## 1. 오늘 일어난 일 — 전체 그림

세 갈래가 동시에 진행됐다:

```
이 대화(Claude, 메인)     TradingAgents PoC → A4 재분석 → SD 축 재설계 스코핑
다른 창(Claude+DeepSeek)  A4 연구 데이터셋 구축 → REV20 결합견고성 → SL-2 재현
다른 창(Codex)            19개 슬롯 전체 LOO marginal 분석 (§0 참고)
```

세 갈래가 결과적으로 서로를 검증하거나 뒤집는 관계다 — 아래 §5(정합성)에서
교차 정리한다.

## 2. 이 대화(메인)에서 한 일 — 시간순

1. **VM 분봉 246영업일 백필 상태 확인** — 진행 중, 이 세션 작업과 무관.
   `docs/control/세션인수인계-2026-08-18-c.md` 참고, 완료 시점 미확인(재조회 필요)
2. **Strategy Lab/A4 기존 실험 조사** — 5DC-v1A-P·TREND-BREAKOUT-v1·survivorship
   측정 등 기존 산출물 인벤토리
3. **CODEX-2/DEEPSEEK-1 결과 검토** — SL-2 stop_distance 재현(summary 일치,
   ATR%-MAE 상관계수 재현 안 됨) · REV20 결합견고성(최악 조건 CAGR -12.96%로
   기각) — 둘 다 커밋됨(§4)
4. **TradingAgents PoC 검토**
   - 아키텍처 분석(analyst 단계 생략 설계 확정)
   - coverage 구조적 상한 발견: fundamental+technical 2축만으로는 **최대
     57.9%** — 60% 게이트를 원리적으로 못 넘음(19칸 분모 산식 때문)
   - 2~3종목×1~2snapshot 구조 smoke를 **API 호출 없이 Claude가 직접
     Bull/Bear/Judge 작성**하는 방식으로 진행(005930×2·000660×1, 3쌍)
   - 결과: `research/strategy-lab/reports/2026-08-19-tradingagents-structure-smoke/results.json`
     (미커밋) — raw technical이 강해 보이는데 axis score는 낮은 괴리를 Bear가
     3번 다 정확히 짚음(raw feature를 같이 준 설계가 유효했다는 근거)
   - 스크립트: `research/strategy-lab/tradingagents_structure_smoke.js`(미커밋,
     ANTHROPIC_API_KEY 있으면 실제 파이프라인 오류 검증용으로 나중에 실행 가능)
5. **A4 재분석**
   - 다른 창이 커밋한 `a4-analysis-results.json`(2,558종목 전량) 직접 대조 —
     기관 20일 순매수 d120 IC +0.022(t=25.1)로 강한 신호 재확인
   - 공백 3개(marginal IC·개인축 상관관계·개인축 규모별 IC) 발견 → Claude가
     `research/strategy-lab/analyze_a4_marginal.py` 작성·직접 실행(API 아님,
     순수 pandas 통계) → **개인축 = 외국인+기관의 완전 선형결합(항등식)**
     발견(시장청산 항등식 + 카테고리 완전분할 때문 — build_a4_research_dataset.py로
     직접 대조 확인)
   - DeepSeek 독립 재현 요청(`DEEPSEEK-2`) → **전부 일치**, 구조적 원인까지
     추가 확인. 결과 문서화: `docs/verification/DEEPSEEK-2-A4-marginal-IC-결과.md`(미커밋)
6. **KR-2.3(supplyDemand 축 재정의) 스코핑**
   - **중요 정정**: 처음엔 "SD-1.1"로 부를 뻔했는데, `supplyDemand.v1.json`은
     **수집 정책**(A4 백필용)이지 채점 정책이 아니다. 실제로 바뀌는 건
     `config/criteria/KR-2.2.json`의 `supplyDemand.metrics` — 새 버전(`KR-2.3`
     후보)이 맞는 대상
   - Q1~Q5 전부 결론: 20일 창 · 개인축 기각(항등식) · 대형주 한정 보류(시총
     원자료 없음) · 2슬롯(largeShareholderChange·buybackOrRetirement) 제거 ·
     weight는 재설계 후 확정
   - 설계안 작성: `docs/control/KR-2.3-supplyDemand-설계안.md`(미커밋) —
     criteria.json 초안 + `lib/scoringEngine.js`의 `scoreSupplyDemand()` 수정
     범위(당초 예상보다 넓음 — 필드명이 하드코딩돼 있어 criteria만으로 안 됨) +
     신규 `lib/a5/supplyDemandFrom.js` 스펙
   - **이 설계안 자체가 지금 §0의 Codex 발견으로 재검토 필요** — 5일 분류가
     역방향이었다면 20일 분류도 검증 없이 그대로 채택하면 안 됨

## 3. 오늘 나온 파일 — 미커밋 (다음 세션이 정리해야 함)

```
docs/control/KR-2.3-supplyDemand-설계안.md
docs/control/handoff/DEEPSEEK-2-A4-marginal-IC-검증.md
docs/verification/DEEPSEEK-2-A4-marginal-IC-결과.md
research/strategy-lab/analyze_a4_marginal.py
research/strategy-lab/data/a4/a4-marginal-analysis.json
research/strategy-lab/tradingagents_structure_smoke.js
research/strategy-lab/reports/2026-08-19-tradingagents-structure-smoke/results.json
```
전부 production 코드·정책 무변경 확인됨(`config/criteria`·`config/policies`·
`lib/scoringEngine.js`·`lib/a5/resolver.js` 실제로 안 건드림 — KR-2.3 설계안은
문서 안의 제안 코드일 뿐 실제 파일 아님).

## 4. 오늘 다른 창에서 이미 커밋된 것

```
d323bf1  A4 수급 데이터 Strategy Lab 연구용 데이터셋 추가 (5,348,454행)
79a93ac  독립 전략 후보 리포트(2026-08-18) 최초 커밋 + REV20 결합 견고성(§11)
6d45f4d  SL-2(3) stop_distance 비교 결과 독립 재현 검증 문서화
```
REV20은 최악 결합 조건(비용60bps+유동성1억+가격5000원)에서 CAGR -12.96%로
사실상 기각됨(README §11에 반영 완료). SL-2 ATR%-MAE 상관계수는 재현 안 됨(문서화됨).

Codex의 `research/strategy-lab/findings/slot-marginal-contribution/`도
**아직 미커밋** — 파일 자체는 로컬에 있지만 git 추적 대상 아님(§0 참고).

## 5. 정합성 — 세 갈래가 서로 맞는지

| 발견 | 내 A4 분석(20일 연속값) | Codex 분석(5일 분류, 실제 프로덕션 코드) |
|---|---|---|
| 외국인 d120 IC | +0.0121 (양) | -0.0319 (음) |
| 기관 d120 IC | +0.0223 (양) | -0.0205 (음) |
| 원인 | — | 5일·20일 상관은 +0.31~0.34로 어느 정도 겹치지만 **분류(ordinal) 자체가 역방향으로 측정됨** |

**모순이 아니다** — "수급 신호가 무효"가 아니라 "**현재 슬롯 정의(5일 분류)가
무효/역방향**"이라는 게 Codex 결론이고, 이건 내 20일 연속값 분석과 배치되지
않는다. 다만 **KR-2.3 설계안이 재사용하려던 `trendFromWindow()`(같은 분류
메커니즘, 창만 20일로 늘림)가 안전하다는 보장이 없어졌다** — 구현 전에
20일 버전의 분류 IC를 먼저 재봐야 한다.

## 6. 다음 세션 우선순위

```
1  Codex의 slot-marginal 발견을 docs/verification/·CLAUDE.md로 옮겨 적기
   (AGENTS.md §3가 이미 Claude 몫으로 지정해둠 — 미착수)
2  KR-2.3 설계안 재검토 — trendFromWindow()를 20일 창으로 바꿨을 때 실제
   classification IC가 양수로 나오는지 먼저 확인(analyze_a4_marginal.py에
   raw 대신 trendFromWindow 로직을 추가 실행하면 됨, API 불필요)
3  pbr 슬롯(ΔIC d120 +0.0385, 최대 레버)이 A5-3의 어느 부분과 겹치는지 확인 —
   supplyDemand보다 우선순위가 높을 수 있음
4  등급 경계 비단조 문제(§0)는 이번 세션에서 손 안 댐 — 별도 트랙으로 남음
5  오늘 만든 미커밋 파일들(§3) 커밋 여부 판단 — 사용자 지시 대기
```

## 안전 관련 메모

이 세션은 `config/criteria`·`config/policies`·`lib/scoringEngine.js`·
`lib/a5/resolver.js` 등 production 코드를 전혀 수정하지 않았다. TradingAgents
smoke는 API 호출 없이 Claude가 직접 텍스트로 작성했다(비용 0). A4 marginal
분석은 로컬 pandas 계산만 했다(API 아님). 커밋은 전부 다른 창에서 이뤄졌고
이 세션은 한 번도 `git commit`을 실행하지 않았다.
