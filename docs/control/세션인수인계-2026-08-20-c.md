# 세션 인수인계 — 2026-08-20 (c) — A5-3 D4 구현 + A3d 백필 단계 신설(5차 스모크로 버그 4개 제거) + 전체 실행 진행 중

이 문서는 정본이 아니다 — 정본은 GitHub main의 실제 파일과 `docs/A5-3-peg-조정기준-결정브리프.md`
원본(§14~§21)이다. `세션인수인계-2026-08-20.md`(어제 미커밋 정리)·`-b.md`(A5-3 D4 결정 브리핑)와는
별개 세션 — 여기는 그 이후 D4를 실제로 구현하고 백필 단계를 새로 만든 세션을 다룬다.

## 새 세션은 여기부터

1. **A3d 전체 유니버스 collect가 지금 이 순간 GitHub Actions에서 돌고 있다.**
   `gh run view 32378542700` 먼저 확인 — 8샤드 다 끝났으면 `mode=finalize`를 **별도로**
   트리거해야 한다(collect→finalize 자동 연결 안 됨, 워크플로가 `mode` 입력으로 갈린다).
2. `git log --oneline -15`·`git status --short`로 이 문서 이후 변경이 있었는지 먼저 확인한다.
3. **A5-3 D4는 코드 구현까지 끝났지만 운영 투입 전이다** — `featureRegistry.js`의
   `available`은 여전히 `false`. §5(아래)의 "남은 것"부터 이어받는다.

---

## 1. A5-3 D4 — resolver.js 구현 완료 (커밋 920d543)

`docs/A5-3-peg-조정기준-결정브리프.md` §18이 정본. 이번 세션에 한 일:

```
lib/a5/corporateActionAdjustment.js (신규) — §17.1 판정 순서·§16.2 보정 공식 구현.
  splitLike(레코드가 사건보다 먼저 접수 → 소급 보정) vs capitalReal 유보(레코드가
  사건과 같거나 이후 접수 → 유보) — 게이트 방향이 반대라는 걸 이 세션에 회귀로
  발견(§18.2). 034020 실사례로 잡았다.
lib/a5/resolver.js — sharesOutstandingFrom()·valuationD4From() 추가.
  resolve()가 sharesOutstanding(A3c 레코드)을 넘겼을 때만 stockData.valuation을
  채운다. 기존 valuationFrom()(A3b 기반, 알려진 결함으로 미사용)은 안 건드림.
scripts/test-a5-d4.js (신규) — N=13 실사례(이 세션에 DART API 직접 호출로 확인한
  034020·065170·079650·005930·000860·014200·000050·케이에이치건설·루트로닉·
  020560·079160) + 경계값, 37건 통과.
```

**아직 안 끝난 것**: `featureRegistry.js`의 `available`은 여전히 `false`다 —
`corporateActions`(공시 라벨)를 실제로 공급할 백필 단계가 없었기 때문(§18.3). 이걸
메우는 게 §2~§4(아래)다.

---

## 2. A3d — 공시 기반 기업행위 라벨 백필 단계 신설 (FN-1.8, 커밋 8ed25d9 이후 5개 fix 커밋)

`docs/A5-3-peg-조정기준-결정브리프.md` §19~21이 정본.

### 2.1 설계 결정 — DART에 순수 분할·병합 배수를 주는 API가 없다

DS005(주요사항보고서) 36개 항목 전수 확인 + `irdsSttus` 실측으로 공백 확정. 대안:
공시(list.json)로 "무엇이 일어났는지"만 확인하고, 실제 배수는 이미 수집된
`data/backfill/fundamentals/a3c`의 그 corp 레코드에서 disclosureDate를 감싸는
전이값 비율로 계산한다(A3c 재수집 없음).

### 2.2 구조 — 최종 버전 (여러 번 재설계됨, §2.3 참고)

```
split·reverseOrConsolidation   I타입 list.json → classify() → A3c 브래킷 배수
mergerSpinoff                  B타입 list.json → classify() → 배수 없음(D4 범위 밖)
rightsOffering                 piicDecsn.json 자체가 그 corp의 사건 목록(무조건 호출)
capitalReduction               crDecsn.json 자체가 그 corp의 사건 목록(무조건 호출)
bonusIssue                     fricDecsn.json 자체가 그 corp의 사건 목록(무조건 호출)
```

rightsOffering·capitalReduction·bonusIssue는 **list.json으로 후보를 걸러 rcept_no로
상세와 대조하지 않는다** — 최초 설계는 그렇게 했다가 스모크에서 깨졌다(§2.3의 2번째
버그). 상세 API 자체가 이미 그 corp의 실제 결정을 중복 없이 준다(crDecsn 실측 확인).

### 2.3 스모크 테스트 5차 반복 — 버그 4개 발견·수정 (전부 완료, 실사례 회귀로 재확인됨)

각 사이클: `gh workflow run fundamentals-a3d.yml -f mode=collect -f limit=5` →
진단(`gh run download`) 확인 → 버그면 수정+회귀 추가+커밋+`data/backfill/fundamentals/_shards_a3d/`
(버그 있는 이전 실행이 커밋한 상태) 삭제+push → 재실행.

```
1차(gh run 32358558995)  end_de 날짜 포맷 버그 — today_kst()가 하이픈 포함
  형식을 주는데 하이픈 제거를 안 해 DART가 매 콜 status 100으로 거부, 40법인
  전량 0건. 커밋 7988af0.
2차(gh run 32362600405)  158건 수집됐지만 RIGHTS_ICMTHN_UNMATCHED(160건)가
  정상 수용(158건)보다 많음 — list.json 매칭 방식 자체가 틀렸다(정정공시가
  원본과 별개 후보를 만드는데 상세 API는 실제 결정 건당 하나). 상세 API
  직접 수집으로 구조 재설계. 커밋 66b99cc. 부수 발견: ic_mthn 패턴이
  "일반공모증자"·"주주우선공모증자" 실제 값을 놓쳤던 것도 같이 수정.
3차(gh run 32363842456)  RIGHTS_ICMTHN_UNMATCHED 해소(0건)됐지만 A3c 브래킷이
  raw 시간순 정렬만 써서 같은 접수일 co-filing(009810 실사례, 세 회계연도
  동시 접수)을 못 갈라 배수가 0.81038로 지저분. A3c 자신의 PIT 규칙
  (fiscalYear 최댓값 우선, lib/a5/pitSelector.js와 동일 원리)을 파이썬으로
  재현(_pit_select_asof)해 0.9505로 정정. 커밋 9c26336.
4차(gh run 32364764232)  bracketOutOfTolerance 0건으로 브래킷은 깨끗해졌지만
  "결정" 공시("주식분할결정")와 "변경상장" 공시("거래정지해제(액면분할...)")가
  같은 사건의 두 단계인데 별개 split 레코드 2건(011040 실사례, 80일 간격·
  같은 배수)을 만들어 COMPOUND_EVENT 오탐 위험. _dedup_same_event() 추가
  (같은 corp·category·배수가 120일 안이면 가장 늦은 것만 남김). 커밋 f3aff8f.
5차(gh run 32367336743)  ★ 완전히 깨끗했다 — listErrors 0·중복 0·
  bracketOutOfTolerance 0·117건 정상 수집. finalize 승인받아 트리거.
```

### 2.4 finalize 1차 시도 — 실패했지만 버그 아님 (gh run 32375292846)

`corpsIncomplete` 인수 조건이 정확히 의도대로 막았다: "미완료 법인 3761건(담당
3801·완료 40)". **finalize는 스모크 40법인을 전체로 착각하지 않는다** — A3c와
같은 게이트. 산출물은 안 쓰임(manifest 계약대로). 이게 다음 단계(§3)로 이어졌다.

---

## 3. A3d 전체 유니버스 실행 — 진행 중 (다음 세션이 이어받을 실제 작업)

```
gh run 32378542700 — mode=collect, limit=0(전체 3,801법인), 8샤드
트리거 시각 기준 40분 경과 시점(이 문서 작성 시각): 8샤드 중 5개 완료(각 35~37분),
  3개(shard 1·3·5) 아직 진행 중. 완료된 샤드 소요 시간 기준 규모 추정: 전체
  40~50분 내외로 끝날 가능성.
```

**다음 순서**:
```
1  gh run view 32378542700 으로 8샤드 전부 끝났는지 확인(commit-shards까지)
2  gh workflow run fundamentals-a3d.yml -f mode=finalize 트리거
3  결과 확인 — 인수 조건 통과 여부(§19.4 acceptance, WARN 위주로 설계됨),
   회귀(test-fundamentals-a3d.py, workflow 안에서 자동 실행됨), manifest 작성
4  통과하면 data/backfill/fundamentals/a3d/{category}.jsonl.gz 산출물이
   실제로 커밋된다 — §14 4번의 "N=20~30 표본 회귀를 실제 수집 데이터로
   재작성"이 이제 가능해진다(지금 test-a5-d4.js·test-fundamentals-a3d.py는
   둘 다 손으로 만든 정답표였다)
5  그 다음에야 featureRegistry.js의 peg/pbr available:true + V7 수직 슬라이스
   (§14의 남은 항목)
```

실패하면(특히 quota 초과 — DART 일 한도 40,000, 안전여유 4,000, 8샤드
activeShards 분배) 다음날 재실행하면 이어받는다(resume 설계, corpsDone 기준).
**실패 원인이 새 버그라면 §2.3과 같은 패턴(수정→회귀→`_shards_a3d` 정리→재실행)을
반복한다.**

★ **알아둘 것 — 전체 실행 트리거 전에 5차 스모크의 `_shards_a3d` 정리를 깜빡했다.**
5차 스모크(gh run 32367336743)의 commit-shards가 커밋(`56ec6a6`, 40법인·117행)을
푸시한 걸 못 지우고 전체 실행(`limit=0`)을 트리거했다 — 지금까지 매 스모크
사이에 했던 "정리 후 재실행" 절차를 이번엔 건너뛴 것이다. **다행히 무해하다** —
전체 실행의 각 샤드가 이 상태를 checkout 시점에 물려받아도, resume 로직(`run_shard()`의
"corpsDone에 있으면 기존 shard-N.jsonl 행을 재사용"거)이 그 5법인을 정상적으로
스킵하고 재사용할 뿐이다 — 같은(최종 버그수정 완료) 코드로 만든 데이터라 다시
수집해도 같은 값이 나왔을 것이다. **다만 앞으로는 매번 실행 전에 `_shards_a3d`가
깨끗한지(`git log -1 -- data/backfill/fundamentals/_shards_a3d`) 먼저 확인하는
습관을 유지한다** — 이번엔 우연히 무해했을 뿐 다른 상황에서는 진짜 충돌이 될 수
있다(예: 서로 다른 버전의 코드로 만든 데이터가 섞이는 경우).

---

## 4. VM 1분봉 249영업일 백필 — 완료 확인, 서비스 정지함

`docs/control/세션인수인계-2026-08-18-c.md`가 원 설계 문서. 이번 세션에 확인:

```
로그 마지막 줄 "할 일 없다" — 스캔한 모든 날짜가 이미 TRADING_CONFIRMED
manifest를 통과해 스킵됨. 백필 자체는 끝났다.
minute-backfill-loop.service·minute-backfill-monitor.service를 정지함
(while true 루프라 완료 후에도 계속 헛돌고 있었다 — 정지 확인:
systemctl is-active 둘 다 inactive).
```

**산출물은 여전히 GitHub에 없다** — VM `~/minute-raw`에만 있고 승격 파이프라인이
미구현(CLAUDE.md "AI 협업 구조" 절 참고). 이건 이번 세션 범위 밖.

---

## 5. OpenCode 실험 2건 — 완료, 결과는 문서화 안 됨(다음 세션이 정리 필요)

기존 데이터(`slot_marginal_analysis.js` + `data/backfill/`)로 재실행한 실험 둘.
**원본 스크립트·산출물은 안 건드림** — 복사본으로 실행, 출력도 별도 경로.

```
실험 A (research/strategy-lab/slot_marginal_analysis_window20.js)
  SD5D_WINDOW 5→20(수급 추세 판정 창). CLAUDE.md가 이미 "KR-2.3 재개 조건"으로
  적어둔 그 실험. 결과 → findings/slot-marginal-contribution-window20/snapshots.json
  ★ 발견: base_inst(기관 수급 단독 IC) d20이 -0.0011(5일)→+0.0092(20일)로
  부호 반전. base_foreign d120도 0.0155→0.0310으로 강화. 단 LOO marginal은
  두 창 다 작아서 "완전한 승리"는 아니고 "부분적 개선 근거".

실험 B (research/strategy-lab/slot_marginal_analysis_seed2.js)
  SEED 20260819→20260820(다른 120종목 표본). pbr 지배 결론의 시드 견고성 확인.
  결과 → findings/slot-marginal-contribution/snapshots-seed2.json
  ★ 발견: pbr marginal 지배는 재현됨(두 시드 다 압도적 1위). 반대로 수급
  슬롯의 standalone IC는 시드 의존적 — 새 시드에서는 base_foreign이
  d60(-0.0049)·d120(-0.0100) 전부 음수로 뒤집힘.

주의: OpenCode가 스크립트 실행까지는 했지만 IC 표 분석(analyze_slot_marginal.py
2단계)은 안 돌렸다 — Claude가 경로만 바꾼 임시 스크립트로 직접 돌려서 얻은 결과.
이 발견들은 어디에도 파일로 안 남겼다 — KR-2.3 재개를 실제로 결정할 때
이 세션인수인계 문서가 유일한 기록이다. 필요하면 docs/control/KR-2.3-supplyDemand-설계안.md
(미커밋, git status에 이미 잡혀 있음)에 옮겨 적는 것을 고려한다.
```

---

## 6. 별도로 플래그해 둔 것 — survivorship PnL 재현성 불일치

`verify_a1b_pnl.py`(기존 미해결 이슈, 이전 세션이 남김)를 재검산한 결과 "240 vs
359" 불일치는 착시였다(서로 다른 두 숫자를 잘못 비교한 것 — 실제로는 정확히
일치). 대신 더 근본적인 문제를 발견: 단순 치환 모델("밀려난 366건 제거 +
새 359건 편입")로는 전체 손익 변동(+6.76M)이 안 맞는다 — 두 실행에 공통으로
존재하는 1,226건의 거래 자체가 실행마다 다른 손익을 낼 가능성. 이건 A5-3/A3d와
무관해서 별도 백그라운드 작업으로 스폰해 뒀다:

```
task_id: task_3347c5cf
제목: survivorship 리포트 손익 재현성 불일치 조사
```

사용자가 원하면 이 task_id로 바로 착수할 수 있다(칩 클릭 또는 직접 요청).

---

## 안전 관련 메모

이번 세션은 `lib/a5/`·`config/policies/fundamentals.v1.json`(a3d 블록 신설,
FN-1.7→FN-1.8)·`.github/workflows/fundamentals-a3d.yml`(신규)를 수정했다 —
전부 커밋·푸시 완료. **실제 DART API 실행(collect)은 사용자 사전 확인 후 총
7회**(스모크 5회 + finalize 시도 1회 + 전체 실행 1회) 진행했고 매번 결과를
`gh run view`/`gh run download`로 실측 확인했다. `data/backfill/`에 로컬
커밋은 없다(전부 GitHub Actions 산출물, 규칙 4 준수) — 단 각 스모크가 남긴
`_shards_a3d/` 진행 상태는 GitHub Actions 봇이 자동 커밋했고, 다음 재실행 전에
매번 삭제해 재실행이 옛 버그의 결과를 "완료됨"으로 건너뛰지 않게 했다.
VM(`ssh stock`)에는 서비스 정지(`systemctl stop`) 1건만 실행 — 사용자가
명시적으로 요청했다("할일없으면 정지해줘"). credential은 어디에도 노출되지
않았다(DART_API_KEY는 로컬 `.env`·GitHub Secrets에서만, OpenCode에 전달 안 함).
