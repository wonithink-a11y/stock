# 세션 인수인계 — 2026-09-26-d · 시장 히트맵(국내 장중·미국) · Gemini 공시 해독 자동화 · 지분·이벤트 카드

같은 날 다른 창: `-2026-09-26.md`(알림 VM·us-pit) · `-b.md`(워뇨띠 후속) · `-c.md`(해독 요약·목표가·업종 재분류·미국 PBR). 겹치지 않는다.
정책·config/policies·criteria 변경 없음. 커밋은 전부 push 완료. **모두 관찰용 맥락 — 점수·매매 불사용(절대 규칙 1).**

## 0. 다음 세션이 먼저 할 것

1. **09-27(일) 20:00 decode-gemini 정기 실행 = 전 종목 재해독**(DECODE_VERSION 2, §6). 확인: `gh run list -w decode.yml -L 1` 초록 ·
   로그 `완료 N · 실패 M` · `docs/data/decode/index.json` 의 `ver: 2` 건수 ≈ 351. 약 90분·Flash-Lite 351건(하루 500 안).
   실패가 분당 한도(429) 몇 건이면 정상 — 다음 날 자동 재시도. `통과 항목 0` 이 여럿이면 원문 자르기부터 의심(§2-3·§6).
2. **09-28(월) 09시 이후 섹터강도 탭 히트맵 '장중 09-28 HH:MM'** — `/kr-intraday` 첫 실물(§1). 안 뜨면 VM
   `~/collector-venv/intraday/kr-snapshot.json` 존재 · `curl -s 127.0.0.1:8766/kr-intraday` · nginx 순으로.
3. 기존 09-28 목록(국내 모의 1주 시험 09:10/09:12 · events-dday 07:50 · news-alert 08:00 · daily-analysis `Build stock context` 목표가) 그대로.
4. **④ 공시 한 줄 요약(AI)** — 해독 353종목이 한 번 다 채워진 뒤(09-28~) 착수. 수주·증자·기술수출 공시 원문에서 상대방·금액·매출 대비 %,
   decode 와 같은 인용 대조. Flash-Lite 하루 500 을 해독과 나눠 쓴다.
5. 10-01~05 월간 점검(CLAUDE.md 절차).

## 1. 시장 히트맵 (섹터강도 탭 맨 위)

| 커밋 | 내용 |
|---|---|
| 5a0ccc1d | 국내: `build-sector-strength.py` 가 `stocks`(종목·업종·최근 시총·1d/1w/1m/3m) 추가 → squarified 트리맵, 면적 시총/동일, 종목 클릭 = 상세 |
| 0ba3e278 | 미국: **TradingView 공식 임베드 위젯**(S&P500, 데이터 수집 없음). innerHTML 의 script 는 안 돌아서 `mountTvHeatmap()` 이 render 뒤 DOM 으로 붙인다 |
| f95570fb | 국내 '장중'(기본값): VM `intraday-check.js`(10분)가 `kr-snapshot.json` 에 현재가·전일종가를 남김 → `kis-minute-history-api.py` `GET /kr-intraday` → nginx `/kr-intraday`(요청 제한·CORS, **반영 완료**). 화면은 2분마다 확인·카드만 교체, 실패하면 1일로 + 사유 표시 |

- ★ **TradingView 는 KRX(와 LSE)를 외부 임베드에서 S&P500 으로 강제 교체**한다(위젯 코드의 `permissionOverrides` — 라이선스 고객만). 우회 안 함.
- 네이버 시세를 브라우저에서 직접 부르는 길은 CORS 로 막힌다(실측) — 그래서 VM 경유.
- 색은 대시보드 관례(초록 오름·빨강 내림). 한국 앱 관례(빨강 오름)로 바꿀지는 미정.

## 2. Gemini 공시 해독 (해독 탭 '🤖 AI 공시 해독')

- `scripts/decode-gemini.py` — DART 원문 '사업의 내용' → Gemini(JSON, 항목마다 원문 인용) → **코드 검사**: 인용 조각마다 원문 완전 일치(6자 미만 조각 이어붙이기 금지) ·
  주장 속 숫자가 인용 안에 **토큰 단위**로 있음(단위 환산 금지) · 연도는 원문 어딘가 · 날짜 표기 통일 · 제품코드 숫자(ALT-B4) 제외 · 요약·해석은 숫자가 원문에 있어야.
- `--auto`: DART 정기공시 목록(88일, corp_code 없이 1회 훑기)으로 관심종목 KR 중 새 보고서만, 시총 큰 순, 429 PerDay 면 멈춤·150분 예산·같은 보고서 2회 실패 건너뜀·통과 0 은 완료 아님.
- Actions `decode-gemini`(매일 20:00 KST) → `docs/data/decode/{ticker}.json` + `index.json`. `GEMINI_API_KEY` 시크릿은 이번에 갱신(08-17 옛 값은 어디서도 안 쓰던 것).
- 시범·정답지 대조: `docs/cards/005930-gemini-pilot-2026-09.md`(정답지 = 수작업 `005930-pilot`) · `196170-gemini-pilot-2026-09.md`.

### 2-1. 무료 한도 (AI Studio 실측 09-26, 프로젝트·**모델마다 따로**)
| 모델 | 분당 | 하루 |
|---|---:|---:|
| Flash 2.5·3·3.5·3.6·3.7·3.8 | 5 | **20** |
| **3.5 Flash-Lite**(자동 해독 기본) | 15 | **500** |
| Pro 전부 | 0 | 0 (2.5 Pro 는 신규 사용자 차단) |
- 공식 문서는 표를 내렸다 — 숫자는 로그인한 AI Studio `rate-limit` 페이지에만 있다. 응답 헤더에도 없다. **계기판 반영이 20분 넘게 늦다**(§2-3).

### 2-2. 품질 (삼성전자 반기, 정답지 핵심 사실 15개)
| 단계 | 통과/항목 | 핵심 |
|---|---|---|
| 첫 지시문 3.5 Flash | 24/25 | 9 |
| 표 행 펴기·체크리스트·주요계약·해석 | 29/29 | 13 |
| 숫자 토큰·날짜·분량(4~8/섹션) | 33/33 | 13 |
| **3.5 Flash-Lite**(최종) | 30/32 | 12 |
- 알테오젠(바이오, 두 번째 종목) Lite 27/27 — MSD·Sanofi·Daiichi 계약, 할로자임, ALT-B4 82.8%, 임상 위험 전부.
- Lite 가 "1조 4,285억, 아 아니 142조…" 같은 자기수정 문장을 낸 적 있다 — 검사기가 잡았다. **검사기를 빼지 말 것.**
- 한계: 단위('백만 달러')는 검사 못 한다 · 여러 사실을 엮은 해석(사이클 정점 위험 등)은 수작업보다 얕다.

### 2-3. 실수·함정
1. **첫 실행을 멈춘 줄 알고 취소했다** — AI Studio 사용량이 3건에서 안 움직여서. 실제로는 돌고 있었고 계기판이 늦었다. 처리분 ~30건 미커밋·한도만 소진.
   Actions 진행 로그는 `python -u` 없으면 버퍼에 묶여 안 보인다(f3d5744b 로 고침). **돌고 있는 실행은 끝날 때까지 두고, 판단은 로그로.**
2. **목차를 본문으로 잘랐다**(SK하이닉스, 338자 → 모델이 '원문에 정보 없음') → 가장 긴 '사업의 내용' 구간 선택(78ca6ae0).
3. 표가 칸마다 줄이 갈려 Tesla 계약·점유율 표를 못 읽었다 → `<TR>` 단위 한 줄.
4. 부분 문자열 숫자 대조가 '5,794만 달러'를 '57,941,459' 안에서 통과시켰다 → 토큰 단위.

## 3. 지분·이벤트 카드 (해독 탭 '🏦 지분·이벤트', 11c5bb7e)

- `scripts/build-stock-events.py` → `docs/data/stock-events.json`(353종목, 0.7MB, 로컬 3분·Actions 10분+) · Actions `stock-events`(평일 19:40) · 첫 실행 성공(8eedf7d).
- ① 5% 이상 주주 전원(국민연금 강조) 비율·직전 대비·흐름 ② 10% 이상 주주 ③ 임원 6개월 건수만 ④ 최근 1년 주요 공시 8분류(수주·희석·자사주·출자·인수·기술·계약·주주환원·감자·최대주주·해명).
- ★ DART `majorstock`·`elestock` 은 **최근 약 2년치만** 준다 → 맥락 표시 전용, '따라 사기' 검증 재료 아님.
- ★ 첫 보고는 증감 = 보유 전량(S-Oil 국민연금 +11,314,188주) → '첫 보고' 표시. DART 가 0% 임원에 '10%이상주주' 를 붙이는 오표기 → 1% 미만 제외.
- ★ 임원 증감은 주식보상·장내매수 구분 불가(삼성전자 2년 3,390건 대부분 보상) → 매수 신호로 안 읽는다.
- 국민연금이 5% 주주로 잡힌 종목 215/353 · 최근 증가 상위: 파라다이스 +4.24%p · DL이앤씨 +3.14 · HD건설기계 +2.70.
  삼성전자는 국민연금이 2년간 1%p 이상 변동이 없어 보고 자체가 없다 — 목록에 안 나오는 게 정상.

## 4. 인스타 영상 분석 (moneybulkup, 저장소 루트 mp4 — gitignore 처리, 파일은 그대로)

- 내용: DART 로 국민연금 지분 확대(GS건설·NHN 10%, S-OIL 9.7%) → "힌트일 뿐, 외국인·기관이 움직이는 순간을 봐라" → 유료 시그널룸 홍보.
- 대조: S-OIL 9.70% 는 사실. **'알림 뒤 +15%'(삼성SDI·한전기술·삼성전자 7/30)는 폭락 바닥 반등** — 07-30→09-23 유니버스 352종목 중앙값 +15.7%, 절반이 +15% 초과.
  외국인·기관 수급(기관 REJECT·외국인 고도화 REJECT)·거래량 돌파(Stine REJECT)는 이미 검증됨. 가져온 것은 지분·이벤트 카드(§3)뿐.
- 국민연금 따라 사기 검증은 안 함 — API 2년치뿐·5% 보고가 매수보다 ~1개월 늦음(기관 다음 달 10일)·원문 대량 수집 필요 대비 기대값 낮음.

## 6. 첫 전체 해독 결과(09-26 22:15~23:45)와 두 번째 자르기 버그 (09-27 새벽)

- 첫 전체 실행 #36244580738: **346종목 89분**(종목당 ~15초, 150분 예산보다 빨라 대기열 소진) · 실패 4 = 분당 한도 429(한화에어로·한화오션·LG·두산로보틱스) ·
  종목당 통과 중앙 18 · 탈락 중앙 4 · 전체 통과율 78.8% · 통과 10 미만 28종목. SK하이닉스(목차 버그 수정 후) 통과 15.
- 통과 10 미만 28종목 조사 → **본문 속 "'III. 재무에 관한 사항'을 참조" 문구를 섹션 끝으로 읽었다**(LG생활건강 5.2만 자 중 1,483자, 티에스이 570자).
  고침: 시작·끝 제목 모두 **줄 맨 앞**만 인정(`(?m)^`). 전 종목 옛/새 비교: **347종목 중 29종목이 20% 넘게 짧게 잘렸고, 그중 18종목은 통과 10 이상이라
  겉으론 멀쩡했다** — 통과 수만 보고 고르면 놓친다. 그래서 선택 재해독이 아니라 전 종목 재해독.
- `DECODE_VERSION`(현재 2): 올리면 `--auto` 가 옛 결과를 전부 다시 한다(index 의 `ver`). `--max 0 --outdir <임시>` 로 대기 수만 점검 가능(실측 351).
- 나머지 저수확 원인(자르기 문제 아님): 금융지주(JB·우리·신한 — 본문 15~18만 자, 180k 상한에 잘림·표 위주)·작은 회사(우리기술투자 6.9천 자) 는 원문 자체 특성.
  탈락 많은 성우하이텍(11)·네이처셀(11)은 재해독 뒤에도 같으면 탈락 사유를 본다(사유는 JSON 에 안 남긴다 — 필요하면 로컬 단건 실행의 부록).
- 이 조사 중 DART 원문 다운로드가 가끔 20초+ 걸린다(성우하이텍) — Actions 는 문제없었다.

## 5. 커밋

5a0ccc1d · 0ba3e278 · f95570fb · e3f660b4 · 12ad4c41(setup-keys Gemini·번호 선택) · 59f19667 · dda8b819 · 3d247f99 · 44891810 · acc060a9 · f3d5744b · 78ca6ae0 · 11c5bb7e ·
0be0a310(이 문서 첫판) · f8e41c81(줄 맨 앞 경계·DECODE_VERSION 2). Actions 자동 커밋: 8eedf7d(stock-events 첫 실행) · 첫 전체 해독 결과.
