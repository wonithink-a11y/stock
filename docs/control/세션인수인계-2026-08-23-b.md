# 세션 인수인계 — 2026-08-23-b — Ox Alpha 산출물 검증·병합 커밋 2건 + CAND1 체결검증 + VM 이전 계획

이 문서는 정본이 아니다 — 정본은 GitHub `main`의 실제 파일이다.
`세션인수인계-2026-08-23.md`(Ox Alpha 후속배치 5+2건 + 분봉 데이터 확보·감사)의
바로 다음 세션이다. 그 문서의 "새 세션은 여기부터" 6개 항목은 **아직 대부분
유효** — 여기서 새로 해소된 건 4번(오프닝 페이드 후속 3건)과 5번(V6
daily-vs-minute 오차 실측)뿐이고, 나머지(V6/V3 엔진 스모크 판단·V5b 판단)는
그대로 남아 있다. 여기서는 **이번 세션에 새로 한 일만** 다룬다.

## 새 세션은 여기부터 — 결정 대기 항목

1. **Oracle VM 이전 실행** — 계획 문서 `docs/control/VM-이전계획-2026-08-23.md`
   확정 완료, **실행은 오늘(2026-08-23) 오후 5시 이후로 사용자가 직접 보류**함
   (사용자 주간사용량 한도 이후 진행 예정). 사용자가 SSH 키 2개를 이미
   다운로드했다고 밝혔으나 **정확한 로컬 파일 경로는 아직 확인 안 됨** —
   1단계(SSH 설정) 시작 전에 반드시 확인. 채팅에 키 내용을 붙여넣지 않기로
   이미 합의됨(경로만 사용). 계획 문서의 8단계 각각은 실행 직전 별도 확인 필요.
2. **Ox Alpha에 신규 지시 3건 발송, 결과 대기 중** (§4 참고) — 사용자가
   "돌려놓겠다"고 해서 진행 중일 가능성 높음. 새 세션은 먼저
   `findings/minute-opening-fade-cost/`, `findings/cand1-regime-decomposition/`,
   `findings/h6-last30-execution/`가 생겼는지부터 확인.
3. **CLAUDE.md 상태블록 미반영 사항 3개** — 이번 세션이 GPT/Ox Alpha 로그
   검토(§2)에서 찾았지만 아직 CLAUDE.md 자체는 손대지 않았다:
   - 5DC-v1A-P 정본 냉동 베이스라인이 커밋 `7e1bc61` 이후 HEAD로 재실행하면
     1,592→1,590건으로 드리프트한다는 사실(§2-2). "정본은 engine commit
     바운드"라는 각주가 CLAUDE.md 어디에도 없음.
   - CAND1(오후급락→익일반등) 후보를 "결정 대기"/"착수 가능" 목록에 올릴지.
   - 이번 세션이 정정한 사실: "분봉 LIVE 적립 시작 여부"는 실제로는 이미
     2026-08-10부터 상시화돼 운영 중이었다(§5) — 8-23 원본 인수인계 §"새
     세션은 여기부터" 3번 항목이 자기모순 상태로 남아 있었다. **원본 문서를
     고치지 않고 여기 정정만 남긴다** — 다음 세션이 원하면 원본을 직접 고칠 것.
4. `세션인수인계-2026-08-23.md`의 나머지 미해결 항목(V6/V3 엔진 스모크 판단,
   V5b 반직관적 결과 판단)은 이번 세션에서 다루지 않았다 — 그대로 유효.

---

## 1. Ox Alpha 산출물 독립 재확인 (실행 자체는 병렬 세션이 이미 완료)

사용자가 지시한 5개 오프닝 페이드 실험(§ 아래 표)과 V6 daily-vs-minute-entry
검증을, 이 세션이 시작하기 직전에 **다른 병렬 세션이 이미 전부 실행**해뒀다.
이 세션은 산출물을 파일 직접 읽기로 독립 재확인만 했다(재계산 없음).

| 실험 | 폴더 | 결과 | 분류 |
|---|---|---|---|
| 오프닝 페이드 기본 | `findings/minute-opening-fade/` | Q5−Q1 스프레드 −34.6bp(09:05→종가), 단조 사다리 | 추가 연구 가치 있음 |
| 거래량 프로파일 | `findings/minute-volume-profile/` | S1(09:00-10:00)=29.3% 집중 | 추가 연구 가치 있음(실행현실성 진단자료) |
| 상하한가 제외 | `findings/minute-opening-fade-exlimits/` | 제외 후 스프레드 오히려 확대(−43.2bp) | 기각(오염 우려 기각) |
| 월별 분해 | `findings/minute-opening-fade-monthly/` | 12/13개월 음수, 세기만 국면의존(최근4개월 3배) | 추가 연구 가치 있음 |
| 창 민감도 | `findings/minute-opening-fade-window/` | W1/5/15 전부 같은 방향 | 기각(왜곡 우려 기각) |

종합본 신규 작성: `findings/minute-opening-fade-comparison/summary.md`(미커밋).

`findings/v6-daily-vs-minute-entry/comparison.md`도 동일 방식으로 독립
재확인 — V6 신호 114,413건, 파악한 방향(근사가 과대평가 아니라 소폭
과소평가)·검증(fastPath 오차 0.0)까지 전부 일치 확인.

## 2. GPT/Ox Alpha 대용량 로그 검토 (`gpt OX alpha.txt`, 사용자 업로드)

25단계 분봉 리서치 지시서 실행 전체 + 5DC-v1A-P 베이스라인 독립검증/컨벤션
감사/드리프트 감사 스레드가 섞인 대용량 로그. 산출물 실재 여부를 전부 파일로
대조 확인. 새로 발견한 것(CLAUDE.md·이전 인수인계 어디에도 없던 사실):

1. **★ CAND1(PMCRASH_REVERSAL) 후보 전략** — `findings/intraday-final-report/report.md`.
   오후급락(전일 14:00→종가 ≤−2~3%, 저거래량 필터)→익일 아침반등. Walk-forward
   test 구간 20bps 비용 반영 후 **+0.369%/일(t=3.94), Sharpe +2.88, MDD −10.8%**.
   이번 배치 유일한 A등급(전략화 후보) — 단 VALID 구간 약화(국면의존성 미해명),
   아직 이벤트스터디 구조일 뿐 엔진 미연결.
2. **★ 5DC-v1A-P 정본 베이스라인 드리프트** — `reports/2026-08-22-reproducibility-drift-audit/`.
   PBR용으로 커밋된 `7e1bc61`(겹침판정 `<=`→`<`)이 **같은 엔진을 쓰는 5DC-v1A-P
   냉동 베이스라인도 같이 바꿨다**(1,592→1,590건, 최종자본 28.47M→30.36M,
   MDD −75.00%→−73.61%). 원인 코드·데이터로 직접 입증됨. 냉동본은 "engine
   commit 바운드"로만 유효.
3. **미커밋 코드 2건 발견 → 검토 후 커밋함** (§3).
4. 부수적: A3B `dividendPerShare`/`eps`, A3 `opProfit` — 미활용 신규 필드
   (A4 세분류·A8 공매도는 이미 아는 내용이라 중복).

## 3. 코드 리뷰 후 커밋 2건 (push 안 함)

발견한 미커밋 diff 2개를 각각 review-then-commit 절차로 처리:

**[377dd6e] `fix(strategy-lab): 당일 재진입+same-bar 청산 이중 큐잉 KeyError 방지`**
- `engine/runner.py` — `_schedule_portfolio()`의 exit 큐잉에 `exit_symbols_queued`
  가드 추가. v3_bollinger_rsi SMOKE(종목 189330)에서 발견된 실제 크래시 수정.
- 독립검증: 가드를 임시로 되돌려 새 회귀 테스트가 진짜 같은 KeyError로 죽는
  것 확인 → 복원 후 통과 재확인(테스트가 진짜 그 버그를 잡는다는 증거).
  드리프트 감사 스크립트(`drift_audit.py`)를 재실행해 가드 적용 후에도 5DC
  냉동 베이스라인(1,592건)이 비트 단위로 그대로 재현됨을 직접 확인
  (cash=28,471,028.93155645) — 이 케이스가 5DC 데이터셋엔 없어 무영향.
- 회귀 신설: `tests/test_runner_scheduling.py`에 1개 추가.

**[cf9e1e4] `test(strategy-lab): 5DC-v1A-P samebar 러너에 TR/CAGR anchor convention 적용`**
- `run_5dc_v1a_p_samebar.py` — equity curve 앞에 `(start_date, initial_capital)`
  prepend(기능변경 1줄). 냉동 산출물 안 건드리고 신규 출력 경로로 redirect.
- frozen 1,592거래 격리 재측정 로그와 대조: TR −71.3490%→−71.5290%(−18bp),
  CAGR −9.8053%→−9.7663%(+3.90bp), 나머지(MDD·거래수·승률·PF·최종자본) 불변.
- 신규 테스트 파일 2개 커밋: `tests/test_metric_conventions.py`(10개),
  `tests/test_run_5dc_v1a_p_samebar_convention.py`(5개).

**검증**: 관련 pytest 21개 직접 실행 통과 + 전체 스위트 `python -m pytest -q`
**115 passed**(249.9초) 직접 확인.

남은 4개 caller(`run_5dc_v1a_p_merged.py`·`replay_scheduler_5dc.py`·
`run_pbr_value_v1.py`·`run_pbr_value_v1_oos.py`)에 같은 convention 적용은
범위 밖 — 다음 세션 후보.

## 4. CAND1 체결 미시구조 검증

`cand1_execution_microstructure.py`(다른 세션이 이미 작성해둔 것, 코드
변경 없이 그대로 실행) — 신호 13,420건, 약 74분 스캔. 결과:
`findings/cand1-execution-microstructure/study.md`(미커밋).

- "저유동성 함정" 우려는 중앙값 기준 **기각**(CAND1 신호가 유니버스 평균보다
  09:35까지 거래량 비중·거래대금 오히려 높음).
- 단 **하위 5~10%는 실제로 얇다**(거래대금 500만~1000만원대) — report.md가
  이미 찾은 "유동성 디사일에서 엣지가 큰 것"과 정확히 겹친다. 엣지가 큰
  쪽이 체결이 어려운 쪽 → 실전 마진이 report.md 우려대로 깎일 근거 보강.
- 미시구조 자체(진입가<VWAP<청산가)는 전략 방향에 유리.
- **스크립트 버그 발견**(수정 안 함, 이유 명시): `le0905`(09:05 누적거래량)
  계산의 `mins <= 5`가 절대분 기준이라(09:00=540분) 상수 오류로 전부 0.
  09:30/09:35 창은 정상. 진입이 시가 단일가라 5분 누적치가 핵심 지표가
  아니라는 이유로 재스캔(74분) 없이 그대로 보고.

## 5. 분봉 LIVE 적립 상태 정정

사용자가 "오라클 VM에서 이미 하고 있는 거 아니냐"고 지적해 확인한 결과
**맞았다** — `docs/operations/minute-실측기록.md`에 2026-08-10 상시화 완료
(`systemd timer 16:10·17:40 + cron + logrotate`)로 이미 명시돼 있었다.
CLAUDE.md "운영 중" 블록도 이걸 가리키는 것이었다. 실제로 안 된 건 수집
자체가 아니라 **VM→Object Storage→Actions→commit 승격 파이프라인**
(GitHub에 공식 manifest로 반영하는 부분)뿐 — 이건 로컬에 원본이 안전하게
쌓이고 있어서 매일 손실되는 종류의 긴급성은 아니다. §"새 세션은 여기부터"
3번 항목 참고.

## 6. Oracle VM 이전 계획

사용자가 오라클 콘솔에서 신규 VM `Stock`(`VM.Standard.A1.Flex`, 1 OCPU/10GB,
Always Free 범위 확인됨, IP 129.225.145.14)을 만들었다 — CLAUDE.md
"수집 VM 운영 기준"이 예고해 둔 "나중에 큰 VM이 생기면" 시나리오. 계획을
`docs/control/VM-이전계획-2026-08-23.md`에 8단계로 정리했다. **핵심 함정**:
`stock-Monitor`(기존, 129.225.177.125)는 x86, `Stock`(신규)은 **ARM** —
단순 증설이 아니라 venv 재생성 + `scripts/check-vm-readiness.py` 재검증
필요. 병행 시험수집 → 구조적 대조(byte-diff 아님, T1 FAIL 결론 때문) →
전환 순서. 실행은 오늘 오후 5시 이후로 보류(§"새 세션은 여기부터" 1번).

## 7. Ox Alpha 신규 지시 3건 발송 (결과 대기)

사용자가 "돌려놓겠다"고 해서 아래 3건을 ChatGPT/Ox Alpha에 전달용으로
작성해 전달함(이 세션이 직접 실행한 건 아님) — 결과가 왔는지 새 세션이
먼저 확인:

1. **오프닝 페이드 비용 반영 실현성** → `findings/minute-opening-fade-cost/`
   (20/30/40bps 비용 반영 net 스프레드, breakeven 지점)
2. **CAND1 국면 조건 분석** → `findings/cand1-regime-decomposition/`
   (VALID 구간 약화가 특정 변동성 국면에 몰려 있는지)
3. **H6(마감 급등 되돌림) 역활용 실행 프로토콜** → `findings/h6-last30-execution/`
   (15:20 신호 재정의 + `cand1_execution_microstructure.py` 방식 재사용한
   유동성 측정 + 재정의 신호로 익일 초과수익 재측정)

## 안전 관련 메모

- SSH 키는 채팅에 내용을 붙여넣지 않고 파일 경로만 쓰기로 사용자와 합의.
- 이번 세션 커밋 2건(377dd6e, cf9e1e4) 모두 push 안 함 — 다음 세션 판단.
- 미커밋 상태로 남은 신규 파일(관례대로, `research/strategy-lab/` 안):
  `findings/minute-opening-fade-comparison/`, `findings/cand1-execution-microstructure/`,
  `cand1_execution_microstructure.py`(다른 세션 작성분, 이번 세션은 실행만).
  그 외 기존에 누적된 다수 미커밋 파일(V6/PBR 계열 등)은 `세션인수인계-2026-08-22.md`
  이후 관례대로 계속 로컬에만 있음 — 목록은 `git status`로 확인.
- `docs/control/VM-이전계획-2026-08-23.md`는 계획 문서일 뿐 사전 승인을
  대신하지 않는다 — 각 실행 단계는 그때그때 다시 확인.
