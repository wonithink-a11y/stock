# 세션 인수인계 — 2026-08-20

이 문서는 정본이 아니다 — 정본은 GitHub main의 실제 파일과 각 검증 문서 원본이다.
여기는 "오늘 뭘 했고 다음에 뭘 봐야 하는지"만 모은다.

## 새 세션은 여기부터

1. **오늘 세션은 두 갈래였다** — ① A5-3(peg 조정기준) 설계 조사, ② 그동안 여러
   병렬 세션이 쌓아둔 미커밋 파일 정리. 둘 다 진행 중이며 완전히 끝나지 않았다.
2. **바로 이어받을 것**: 저장소 루트 + `research/strategy-lab/`에 남은 미커밋
   스크립트/산출물 정리(§3). DEEPSEEK-3 감사가 절반만 끝냈다.
3. **결정 대기 중인 🔴 사항**: A5-3(§2)의 D2 vs D4, KR-2.3 설계안 재검토(§4).
   둘 다 사용자 GO 없이 진행하지 않는다.
4. **CLAUDE.md가 오늘 작업을 아직 반영 안 했다** — 아래 §5 참고. 다음 세션
   우선순위 1번으로 남겨둔다.

```bash
git log --oneline -15
git status --short
```
먼저 실행해서 이 문서가 쓰인 시점 이후 다른 창이 더 커밋했는지 확인한다.

---

## 1. 오늘 커밋된 것 (시간순, 최신이 위)

```
d8e0d87  DEEPSEEK-3 감사 반영 — 검증된 리포트 커밋, 반증된 분석 2건 삭제
dd2f1ef  SL2-sequential 요약 데이터 — 검증 문서가 가리키던 파일이 실제로 없었음
699ed18  Codex slot-marginal 발견 + DEEPSEEK-2 A4 marginal IC 독립검증
f6e8615  A5-3 peg 조정기준 결정 브리프 — D2/D4 두 안 정리, GO 대기
525699a  DART 일 한도 20,000→40,000 잔여 서술 정정
6ab0817  OpenCode 서브에이전트 위임 기준 추가
38a08e5  SL-2(3) stop_distance 비교 결과 독립 재현 검증
581e97c  독립 전략 후보 리포트(2026-08-18) 최초 커밋 + REV20 결합 견고성(§11)
c63340d  A4 수급 데이터 Strategy Lab 연구용 데이터셋 추가
```
(이 위 `21eca6d`부터는 어제/오늘 자동화 커밋 — daily analysis, disclosures, market flows)

**production 코드·정책 무변경** — `lib/`·`scripts/`(DART 한도 주석 정정 3곳 제외)·
`config/`는 오늘 손대지 않았다.

## 2. A5-3(peg 조정기준) — 설계 조사, 🔴 GO 대기

`docs/A5-3-peg-조정기준-결정브리프.md` 참고. 요약:

- A3c 우회식(`per = price ÷ (netIncome ÷ istcTotqy)`)도 원 문제(A2a 수정주가 ↔
  A3b/A3c 원본 조정 기준 불일치)를 그대로 재현한다 — 실측 확인
- **D2**(raw 가격 신규 수집, A2c) vs **D4**(DART 공시 라벨로 안전하게 만든
  주식수 소급 보정, 새 수집 불필요) 두 안이 남았다. D1(배수만 보고 역산)은 기각
- D4는 6/6 대조 일치 + 유상증자 `ic_mthn` 분류(주주배정 1건 포함 4개 실사례)까지
  확인했지만, 정밀 검증(권리락 기준일 특정 등)은 여기서 멈췄다 — 더 파도 판단이
  안 바뀐다는 게 그 시점 판단(ChatGPT·Claude 교차 검토)
- **다음 세션이 할 일**: D2 vs D4 GO 판단. 브리프 §9·§10 참고

## 3. ★ 미커밋 파일 정리 — 절반만 끝남, 이어받을 것

### 3.1 이미 끝난 것 (다시 안 봐도 됨)
```
research/strategy-lab/data/a4/                    A4 연구 데이터셋
research/strategy-lab/reports/2026-08-18-strategy-candidates/   REV20 등
research/strategy-lab/reports/2026-08-16-sl2-sequential/ 요약 7개 파일
research/strategy-lab/findings/slot-marginal-contribution/       Codex 발견
docs/verification/DEEPSEEK-2-A4-marginal-IC-결과.md
docs/control 문서 2개(ChatGPT·DeepSeek 세션인수인계 2026-08-17)
research/strategy-lab/reports/ 나머지 20개 중 검증된 15개 (아래 3.2 참고)
```
gitignore 예외 처리는 `research/strategy-lab/.gitignore`에 화이트리스트 형태로
남겨뒀다 — 새 리포트를 커밋하려면 그 파일의 패턴을 참고한다(디렉터리 전체가
아니라 필요한 파일만 예외 처리하는 방식, 249MB짜리 원시 캐시를 통째로 커밋하는
실수를 SL2-sequential에서 한 번 했었다).

### 3.2 보류로 남긴 것 (이유 있음, 재판단 필요시 참고)
```
1,400건 pre-fix 오염분   a25-smoke·b55-smoke·annual-analysis·exit-type-analysis
                        (CLAUDE.md 자체가 "재확인 전 인용 금지"라 명시)
불확실                  benchmark-analysis·pnl-equity-validation
                        (최종 정본 확정 절차 미완)
KR-2.3-supplyDemand-설계안.md   §4 참고
```

### 3.3 ★ 아직 하나도 안 본 것 — 다음 세션이 이어받을 부분

**(a) 저장소 루트 스크립트 25개** (DEEPSEEK-3 감사가 A·B·C까지만 끝내고 D 표를
못 채운 채 출력이 끊겼다):
```
analyze_candidates.py · audit_samebar_stop.py · audit_search.py ·
check_corp_action.py · check_data.py · check_excluded.py · check_foreign.py ·
check_foreign2.py · check_pkl.py · classify_impact.py · convert_a2a.py ·
corp_action_analysis.json · final_classification.py · read_files.py ·
same_bar_stops.json · temp_a4_keys.txt · temp_scratch.txt · temp_scratch2.txt ·
test_load.py · verify_5dc.py · verify_a1b_pnl.py · verify_commit.py ·
verify_survivorship.py · verify_trend.py
```
(`HANDOVER_2026-08-17.md`는 이미 보류 확정 — 다른 문서가 "구식, same-bar
231건은 틀렸고 130건이 맞다"고 명시. `scratch-a4-*.json` 2개는 이미 커밋됨)

**(b) `research/strategy-lab/` 안의 루즈 스크립트·산출물 약 50개** — 이건
DEEPSEEK-3 과제 범위에 아예 없었다(그 과제는 `research/strategy-lab/reports/`만
다뤘지 strategy-lab 최상위의 `.py`·`.pkl`·`.txt`는 안 봤다). 목록은
`git status --porcelain` 실행해서 `research/strategy-lab/`로 시작하고 하위
디렉터리가 아닌 것들(`analyze_2025_concentration.py`, `lowmom60_survivorship.py`,
`rev20_robustness.py`, `5dc_v1a_p_resolved.pkl`, `full_smoke_result.pkl` 등).
이 중 상당수는 **오늘 이미 커밋한 리포트를 만든 생성 스크립트**일 가능성이 높다
(예: `lowmom60_survivorship.py` → 커밋된 `lowmom60_survivorship.json`). 그런
경우는 재현성을 위해 스크립트도 같이 커밋하는 게 맞다 — 하나하나 대조 필요.

### 3.4 (a)(b) 처리 방법 — DEEPSEEK-3와 같은 틀 재사용

`docs/control/handoff/DEEPSEEK-3-미커밋파일-정리-감사.md`의 라벨 체계
(커밋 추천/보류(최신본 있음)/보류(불확실)/삭제 후보)와 확인 항목(최신 여부 근거·
production 영향·용량)을 그대로 재사용하면 된다. 이번엔 표가 안 끊기도록 (a)·(b)를
나눠서 두 번에 걸쳐 위임하는 걸 권한다 — 한 번에 75개는 많다.

opencode 직접 호출 예시(이번 세션에 실제로 썼던 방식):
```bash
opencode run "<과제 파일 경로> 읽고 지시대로 수행해줘. 읽기 전용이다." \
  -m opencode/deepseek-v4-flash-free --agent explore \
  --title "<제목>" --dir "C:\Users\User\projects\stock"
```
`--agent explore`가 쓰기 권한을 제한한 프로필이라 안전하다(`opencode agent list`로
권한 확인 가능). 백그라운드 실행 후 완료되면 출력 파일을 Read로 확인한다.

## 4. KR-2.3(supplyDemand 축 재정의) — 보류 중

`docs/control/KR-2.3-supplyDemand-설계안.md` 자체가 "Codex 설계 검토를 먼저
받을지 결정 필요"로 미결 남겨뒀는데, 같은 날 나온 Codex의 slot-marginal 발견
(`research/strategy-lab/findings/slot-marginal-contribution/`)이 정확히 그
우려(5일 분류 방식이 역방향일 수 있다)를 실증해버렸다. **재검토 없이 이 설계안을
그대로 구현하면 안 된다.** 다음 세션이 뭔가 결정할 필요는 없고, 재개하려면
`trendFromWindow()`를 20일 창으로 바꿨을 때의 classification IC를 먼저 재는
것부터다(`research/strategy-lab/analyze_a4_marginal.py`에 로직 추가 실행하면
됨, API 불필요).

## 5. ★ CLAUDE.md가 오늘 작업을 반영 안 했다 — 다음 세션 우선순위 1번

`docs/control/세션인수인계-2026-08-19.md`(어제 커밋 안 됐다가 오늘 §3.1에서
커밋된 문서)가 이미 "Codex의 slot-marginal 발견을 docs/verification/·CLAUDE.md로
옮겨 적기"를 최우선으로 지정해뒀는데 아직 안 했다. 여기에 오늘 것도 추가해야 한다:

```
CLAUDE.md에 반영 필요한 것 (아직 안 함)
  - A4 수급 데이터셋 완료 + 핵심 발견(기관 IC 양·개인축 항등식)
  - REV20 결합 견고성 검증 결과(채택 불가로 하향)
  - SL-2(3) stop_distance 독립 재현 검증 완료
  - Codex slot-marginal 발견(pbr이 최대 레버, 5일 수급 분류가 역방향)
  - A5-3 peg 조정기준 조사 착수 (D2/D4 GO 대기)
  - KR-2.3 설계안 보류 상태
```
CLAUDE.md는 `docs/*계약*` 급은 아니지만 매 세션 자동 로드되는 문서라, 낡은 채로
두면 다음 세션이 오늘 한 일을 모르고 다시 조사하게 된다(교훈43과 같은 함정).

## 안전 관련 메모

이 세션은 `lib/`·`config/`를 전혀 수정하지 않았다(DART 한도 주석 3곳은 서술
정정이지 값 변경이 아니다 — 정책 값 자체는 FN-1.7에서 이미 맞았음). 삭제한
파일 2건(§1 `d8e0d87` 커밋 메시지 참고)은 전부 git에 한 번도 안 올라간
untracked 파일이라 히스토리 손실 없다. push는 이 세션에서 한 번도 안 했다.
