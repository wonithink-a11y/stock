# 데이터 소스 가용성 (실측 확정, 재론 금지)

`CLAUDE.md`에서 분리했다(2026-08-10). 원문 그대로다.

**재론 금지의 뜻은 '다시 시도하지 말라'다.** 아래는 추정이 아니라 실측이며,
막힌 것을 다시 뚫으려는 시도로 시간을 쓰지 않기 위해 남긴다. 증상 메시지가
원인을 지목하지 않는다는 것(교훈41)과 대조군이 같은 경로인지 먼저 확인하라는
것(교훈52)이 이 표에서 나왔다.

---


| 소스 | 결과 |
|---|---|
| 개별종목 일봉 | 가용 — 단 **naver 경로다**(pykrx `adjusted:true`). KRX 아님 |
| KRX bld 전체 (`getJsonData.cmd`) | **차단** — 전종목 스냅샷·개별종목 일봉·종목별 수급 전부 `400 LOGOUT`. 세션 시드·UA 무관 (2026-08-05, `pykrx 1.0.51` 기준. 로그인 진입점이 없던 버전 — 아래 갱신 참고) |
| naver 종목별 수급 | HTML 2005-01까지(20행/페이지) · JSON 최근 60거래일. 기관 '합계'만 |
| KRX 지수 | 차단 |
| KIND 상장법인목록 | 가용 (`corpList.do?method=download&searchType=13`, 2,802행, euc-kr) |
| KIND 상장폐지목록 | **경로 없음** — 파라미터 미반영 셸 페이지 |
| DART corpCode.xml | 가용 (법인 118,583 / stock_code 3,981) |
| DART list.json | 가용 (폐지 사유 복원 경로) |

`pykrx 1.0.51`에는 로그인 진입점이 없다. `KRX_ID`/`KRX_PW`는 위 경로들과 무관하다.

---

## 갱신 (2026-08-18) — KRX bld·종목별 수급 한정 재론

**"재론 금지"를 깬 것이 아니라, 전제(pykrx 버전)가 바뀌어 다시 잰 것이다** —
교훈52(대조군이 같은 경로인지 먼저 확인)를 그대로 적용한 결과다.

pykrx가 2026-04(PR #282)에 `KRX_ID`/`KRX_PW`(data.krx.co.kr 일반 회원 로그인 —
KRX 공식 Open API 키가 아니다) 로그인 세션을 추가했다. `pykrx==1.2.8` +
실계정으로 재실측한 결과: **종목별 수급(`get_market_trading_value_by_date`/
`_volume_by_date`)은 로그인 후 정상 응답한다** — A4 백필이 이 경로로 2,578종목·
540만9,687행(2016-01-04~2026-08-14)을 finalize까지 통과시켰다(manifest
`data/backfill/manifest/A4.json`, 세션인수인계-2026-08-18.md). 로그인 없이는
여전히 대부분 차단(무자격 재실측 확인 — 전종목 스냅샷·시가총액·PER/PBR·
공매도·지수·업종분류 전부 실패, 개별종목 일봉만 간헐적 성공).

**바뀌지 않은 것**: 전종목 스냅샷·지수·업종분류·공매도·외국인보유는 로그인
계정으로 재확인한 적이 없다 — "차단"을 아직 뒤집지 않는다. 이 프로젝트는
비공식 스크레이핑(KRX 공식 Open API 아님)을 production PRIMARY로 승격하지
않는다는 방침도 그대로다. 이 갱신은 "A4(수급)는 로그인 경로로 된다"는 사실
하나만 확정한다.

---
