# 실현손익 누적 회계 잔존 스크립트 분류 (2026-09-05)

- 생산: OpenCode `opencode/big-pickle` (지시문 2번,
  `docs/control/opencode-지시문-2026-09-05.md`)
- 검증·정정: Claude (생산자·검증자 분리, CLAUDE.md "AI 협업 구조")
- 배경: 2026-09-02 `findings/pbr-value-v1-realized-pnl-accounting-fix-2026-09.md`
  가 "같은 결함이 17개 스크립트에 더 있다"고 남긴 항목

## 요약 — CLAUDE.md 상태블록이 낡아 있다

CLAUDE.md 는 2026-09-02 기준으로 **"eq += pnl 패턴이 아직 13개 스크립트에
남아 있고 `run_dd252_v1` · `run_foreign_flow5d_v1` 등이 인용 중"** 이라고
적어두었다. **그 뒤 6개가 고쳐졌고 CLAUDE.md 에 반영되지 않았다.**

| 분류 | 건수 | 뜻 |
|---|---:|---|
| **A. 이미 수정됨** | **6** | MTM 이 `resultTable` 이고 옛 방식은 `deprecatedRealizedPnL`("인용 금지")로만 남아 있다 |
| **C. 절대 성과지표를 뱉는데 MTM 아님** | **20** | 다시 돌리면 무효 MDD·Sharpe 가 나온다 |
| B. 성과지표 자체를 안 냄 | 0 | 해당 없음 |
| T. tests fixture | 3 | 회귀 테스트, 판정과 무관 |

★ **2026-09-05 1차 판정을 정정한다.** 처음엔 `resultTable` 키의 존재로
"절대수치를 뱉는가"를 판정해 C 를 6건으로 냈다. 그런데
`run_robustness_real.py` 가 그 키 없이 CAGR·MDD·Sharpe 를 뱉는다 —
**키 이름으로 재는 것이 틀린 기준이었다.** `cagr|sharpe|maxDrawdown|mdd|calmar`
출현으로 다시 재니 **C 가 6 → 20 건**이다. 아래 목록은 정정본이다.

## A. 이미 수정됨 (6) — CLAUDE.md 가 아직 "미수정"으로 적고 있는 것들

```
pbr_oos_pooled_2023_2026_vs_ew.py
run_dd252_v1.py
run_foreign_flow5d_v1.py
run_lowmom60_v1.py
run_pbr_value_v1.py
run_pbr_value_v1_oos.py
```

실측 근거 — 세 파일을 열어 확인:

```
run_dd252_v1.py:19   from pbr_vs_ew_monthly_mtm import schedule_with_monthly_mtm, curve_metrics
run_dd252_v1.py:46   """폐기된 회계 방식 - 참고·대조용으로만 남긴다(resultTable에 쓰지 않는다)."""
run_dd252_v1.py:95   "resultTable": {**mtm, **t_stats},
```

`run_foreign_flow5d_v1.py` 동일 구조. `pbr_oos_pooled_2023_2026_vs_ew.py:114`
는 `"폐기된 실현손익 누적 회계. 인용 금지 - 위 cagr/mdd/sharpe를 쓴다."`.

**즉 이 6개는 지금 다시 돌려도 MTM 수치가 나온다.** CLAUDE.md 의
"`run_dd252_v1`(이미 기각) · `run_foreign_flow5d_v1`" 인용 경고는 해소됐다.

## C. 남은 위험 (20) — 다시 돌리면 무효 절대수치가 나온다

```
analyze_event_independence.py          cause_decomposition_5dc.py
gen_robustness.py                      pbr_vs_ew_same_engine.py
perf_decomposition.py                  probe_stop_distance_candidates_5dc.py
replay_scheduler_5dc.py                run_5dc_v1a_p_merged.py
run_5dc_v1a_p_samebar.py               run_capacity_test.py
run_crypto_walkforward.py              run_factor_backtest.py
run_macd_kr_v1_smoke.py                run_robustness_real.py
run_sector_neutral_pbr_growth.py       yearly_comparison_5dc.py
reports/2026-08-22-baseline-independent-repro/convention_comparison.py
reports/2026-08-22-baseline-independent-repro/reproduce_baseline_metrics.py
reports/2026-08-22-convention-standardized-rerun/verify_convention_change.py
reports/2026-08-23-5dc-riskoff-filter-validation/riskoff_filter_validation.py
```

이 중 넷은 파일 안에 "폐기" 표시가 있고 MTM 대체본이 존재한다 —
`pbr_vs_ew_same_engine`(→`pbr_vs_ew_monthly_mtm.py`) ·
`run_5dc_v1a_p_merged`(→`mtm_baseline.py`, ★ CI 아티팩트를 필수 인자로 받아
**지금 실행 자체가 안 된다**) · `run_5dc_v1a_p_samebar` ·
`run_sector_neutral_pbr_growth`(→`run_sector_neutral_mtm.py`).

`reports/` 밑 넷은 그 시점의 재현 경로이므로 그대로 둔다.

**나머지 12개는 표시도 대체본도 없다.** CLAUDE.md 가 "A/B 비교 스크립트는
방향이 보존되므로 우선순위가 낮다"고 적어둔 것이 이 무리인데, **어느 것이
실제로 A/B 비교인지는 코드만 봐서는 기계적으로 못 가른다** — 파일별로 읽어야
하는 판단이다. 이번 위임은 그 판단까지 가지 못했다.

## ★★ 지시문 3번과 교차했을 때 나온 실제 위험

지시문 3번(재실행 가능성)이 **`factor-earnings-yield-robustness-2026-08`
(verdict = PASS)** 을 "CAGR/MDD/Sharpe 산출 가능"으로 분류했다. 그 문서의
제작 스크립트가 **`run_robustness_real.py` — 위 C 목록에 있다.**

```
수치가 빈 PASS 문서를 채우려고 재실행
        -> run_robustness_real.py 가 실현손익 회계로 MDD·Sharpe 를 낸다
        -> 무효 수치가 PASS 판정 문서의 frontmatter 로 들어간다
```

**수치 공백을 메우는 작업이 그 자체로 무효 수치를 주입하는 경로가 된다.**
지시문 2번과 3번을 각각 따로 읽었으면 안 보였을 것이다.

→ 재실행으로 수치를 채우기 전에 **그 스크립트가 C 목록에 있는지 먼저 본다.**
C 에 있으면 MTM 으로 고친 뒤에 돌린다.

## ★ OpenCode 원본 결과의 오류 — 정정 기록

CLAUDE.md 원칙("OpenCode 결과는 관점 하나다, Claude 의 판단과 동일시하지
않는다")대로 검증한 결과 **두 가지가 틀렸다.**

**① 요청한 분류 축을 안 썼다.** A(수정됨)/B(A·B비교)/C(절대수치 인용)로
나누라고 했는데 *적립 단위*(건별/월별/일별)로 나눴다. 그건 결함의 심각도와
무관한 축이다. 다만 말미에 파일 자기주석 기준 3묶음을 덧붙여 그건 쓸 수 있었다.

**② "마커 없음" 6건 판정이 6건 모두 틀렸다.**

```
OpenCode:  "마커 없음(참고·대조용, resultTable 미사용)"
           pbr_oos_pooled_2023_2026_vs_ew · run_dd252_v1 · run_foreign_flow5d_v1
           run_lowmom60_v1 · run_pbr_value_v1 · run_pbr_value_v1_oos

실측:      6개 전부 MTM import O · deprecated 마커 O · resultTable = MTM
           → "마커 없음"이 아니라 "이미 수정됨"이다
```

이 오판을 그대로 받았으면 **이미 고쳐진 6개를 다시 고치는 작업**을 했을 것이다.

## 프로세스 관측

**OpenCode 가 지정한 결과 파일을 쓰지 않았다.** stdout 으로만 냈고
`.cache/opencode-out/` 은 비어 있었다("이 결과는 관측치이며, 쓴 파일은
없습니다"라고 스스로 밝힘). 다음 위임부터는 **stdout 을 정본으로 받는다고
지시문에 적는 편이 낫다** — 파일 쓰기를 기대하면 산출물이 없다.

저장소 오염은 없었다(`git status` 에 OpenCode 가 만든 변경 0건).

## 권고

1. **CLAUDE.md 의 "17개 스크립트" 문단을 갱신한다** — 6개는 수정 완료,
   절대 성과지표를 뱉는 채로 남은 것이 20개다.
2. **수치 공백을 채우는 재실행 전에 C 목록 대조를 의무화한다.** 위 교차
   발견이 그 이유다.
3. **C 20개 중 표시·대체본이 없는 12개를 파일별로 읽어** A/B 비교인지
   절대수치 인용인지 가른다. 이건 기계적 위임 대상이 아니다.
4. **`reports/` 밑 4개는 그대로 둔다** — 그 시점 재현 경로다.

넷 다 이 문서의 권고이고, 실제 반영은 별도 판단이다.
