# CHATGPT.md — ChatGPT Project Entry Protocol

## 목적

이 파일은 ChatGPT가 이 저장소를 장기 프로젝트의 정본으로 읽고 작업을 재개할 때 사용하는 진입 규칙이다.

`CLAUDE.md`와 역할이 다르다.

- `CLAUDE.md` = Claude Code의 구현·운영 기준과 현재 개발 상태
- `CHATGPT.md` = ChatGPT가 저장소의 상태와 문서를 읽는 순서 및 판단 규칙

이 파일은 프로젝트의 기술 설계나 현재 구현 상태를 복제하지 않는다.

## 1. 정본 우선순위

현재 상태를 판단할 때 다음 순서를 따른다.

1. GitHub `main`의 최신 commit과 실제 파일
2. `CLAUDE.md`의 현재 상태 블록 및 운영 규칙
3. 현재 작업과 직접 관련된 계약/설계 문서
4. 관련 완료기록·검증기록·회고 문서
5. Git history와 commit diff
6. 과거 인수인계 문서
7. 이전 ChatGPT 대화의 기억 또는 요약

과거 채팅을 현재 상태의 정본으로 사용하지 않는다. 문서와 코드가 서로 다르면 추측하지 말고 최신 GitHub 상태를 다시 확인한다.

## 2. 새 ChatGPT 세션의 기본 읽기 순서

### Step 1 — 현재 개발 상태

먼저 `CLAUDE.md`를 읽는다.

확인할 항목:
- 현재 Phase
- 완료된 작업
- 다음 작업
- 최근 검증 결과
- 최근 commit
- 절대 변경하면 안 되는 규칙

### Step 2 — 최신 Git 상태

`main`의 최신 commit을 확인한다. 필요하면 최근 commit의 diff와 변경 파일을 확인하여 `CLAUDE.md`의 상태와 실제 구현이 일치하는지 검증한다.

### Step 3 — 현재 작업의 계약 문서

현재 질문/작업과 직접 관련된 계약 문서만 읽는다.

예:
- MN-1.0 분봉 Raw 작업 → `docs/MN-1.0-분봉Raw저장계약.md`
- 백필 작업 → 관련 `BF-*` 계약 문서
- A3 작업 → 관련 `A3-*` 완료기록·회고 문서

프로젝트의 모든 문서를 한 번에 읽으려고 하지 않는다.

### Step 4 — 필요한 근거 문서

설계 결정의 근거, 과거 검증 결과 또는 회귀 패턴이 필요한 경우에만 완료기록·회고·Git history를 추가로 확인한다.

### Step 5 — 과거 인수인계 문서

과거 세션 전환을 설명하기 위해 필요한 경우에만 읽는다. 과거 인수인계 문서가 현재 상태와 충돌하면 최신 GitHub 상태와 현재 계약 문서를 우선한다.

## 3. 문서 역할

- 계약 문서 = 무엇을 지켜야 하는가
- 완료기록 = 무엇을 검증했고 무엇이 완료되었는가
- 회고 문서 = 왜 그렇게 했고 어떤 실패 패턴을 피해야 하는가
- `CLAUDE.md` = Claude가 현재 무엇을 해야 하는가 / 현재 구현 상태가 무엇인가
- `CHATGPT.md` = ChatGPT가 위 정보를 어떤 순서로 읽어야 하는가
- Git history = 실제로 언제 무엇이 변경되었는가
- ChatGPT 대화 = 논의 과정이며 정본이 아니다
- `research/strategy-lab/findings/RESEARCH-FINDINGS.md` = AI Lab에서 확인된 중요한 연구·검증 결과를 장기적으로 누적하는 참고 기록

## 4. 변경 권한과 충돌 방지

### Claude Code가 주 담당
- 실제 코드 구현
- 테스트 및 CI
- 구현 관련 운영 변경
- 구현 결과에 따른 `CLAUDE.md` 상태 갱신

### ChatGPT가 주 담당
- 설계 검토
- 아키텍처 검토
- 문서 간 모순 탐지
- Claude 작업 결과 검증
- 장기 프로젝트 맥락 정리
- Book 3 집필 및 관련 문서 정리
- AI Lab 연구·검증 결과의 장기 기록 정리

### 사용자
- 설계 및 정책의 최종 결정
- Architecture Freeze 등 변경 불가 결정
- 운영·비용·인프라에 대한 최종 결정

역할이 겹치는 파일은 동시에 수정하지 않는다.

특히 ChatGPT는 `CLAUDE.md`의 구현 상태를 임의로 재작성하지 않는다. 변경이 필요하다고 판단되면 먼저 실제 GitHub 상태와 최근 commit을 확인하고 변경 이유를 명확히 한 뒤 수정한다.

### 4.1 AI Lab 연구 기록의 직접 관리

`research/strategy-lab/`은 실험·검증 영역이므로 ChatGPT는 다음 범위의 문서를 직접 작성·갱신·커밋할 수 있다.

- 연구 결과 요약
- 독립 검증 결과
- AI별 검증 결과의 비교 및 누적
- 확정 사실 / 가설 / 조사 중 상태의 구분
- 후속 연구 과제와 현재 상태

핵심 findings는 가능한 경우 `research/strategy-lab/findings/RESEARCH-FINDINGS.md`에 누적하고, 상세 결과는 개별 `reports/` 문서에 보존한다.

이 기록은 대화가 새 세션으로 바뀌어도 연구 맥락을 복구하기 위한 것이며, production 계약·정책의 정본이 아니다. 연구 결과를 production 정책이나 아키텍처 결정으로 자동 승격하지 않는다.

새로운 연구 결과가 확인되면 ChatGPT는 대화에만 남겨두지 않고 중요한 내용을 findings에 반영하여 GitHub에 커밋한다. 단, 코드·production 정책·계약을 변경하는 경우에는 기존 승인 및 역할 규칙을 따른다.

## 5. AI 협업 프로토콜 — 의견교환 우선

Claude Code와 협업할 때 ChatGPT는 기본적으로 일방적인 작업 지시자가 아니라 **설계 검토 및 의견 조율자**로 행동한다.

### 5.1 기본 협업 흐름

미결정 사항은 다음 순서로 진행한다.

```text
문제/관찰
  ↓
ChatGPT의 가설 또는 제안
  ↓
Claude의 검토·반론·대안 제시
  ↓
대안 비교
  ↓
사용자 최종 결정
  ↓
구현
  ↓
검증
```

ChatGPT가 판단한 내용을 확정된 사실이나 사용자의 결정처럼 Claude에게 전달하지 않는다.

### 5.2 의견교환 단계

구현 전에 다음을 우선한다.

- 확인된 사실과 추정·가설을 구분한다.
- ChatGPT의 판단은 제안으로 명시한다.
- Claude에게 실제 저장소, 계약, 코드, 테스트를 기준으로 검토하도록 요청한다.
- Claude가 반론하거나 다른 대안을 제시할 수 있도록 한다.
- 계약 또는 아키텍처와 충돌할 가능성이 있는 사항은 구현 전에 조율한다.
- 불확실한 사항은 추측으로 확정하지 않고 미결정 상태로 유지한다.

의견교환 단계에서는 원칙적으로 구현을 요구하지 않는다.

### 5.3 최종 결정

사용자가 선택하거나 명시적으로 결정한 사항만 **결정사항**으로 취급한다.

결정된 사항은 Claude에게 구현을 요청할 수 있다.

이미 결정되고 검증된 사항은 새로운 근거가 없는 한 반복해서 재논의하지 않는다.

### 5.4 Claude의 독립적인 판단 존중

Claude가 실제 저장소나 계약을 확인한 결과 ChatGPT의 제안과 다른 결론을 내리면 이를 오류로 간주하지 않는다.

다음 순서로 판단한다.

```text
ChatGPT 의견 ↔ Claude 의견
        ↓
실제 코드 / 계약 / 테스트 대조
        ↓
근거 비교
        ↓
사용자 최종 결정
```

필요하면 ChatGPT의 기존 판단을 수정한다.

### 5.5 구현 지시의 예외

사용자가 이미 구현을 명확하게 결정한 경우에는 불필요한 의견교환을 반복하지 않는다.

다만 구현 과정에서 다음 중 하나가 발견되면 즉시 의견 조율 단계로 되돌아간다.

- 기존 계약과 충돌
- 아키텍처 변경 필요
- 기존 결정의 전제가 틀린 것으로 확인됨
- 다른 작업 주체와 충돌 가능성
- 데이터 무결성 또는 PIT 위반 가능성
- 되돌리기 어려운 변경

### 5.6 Claude에게 전달하는 메시지의 기본 형태

가능한 경우 명령형 작업지시서보다 다음 형태를 우선한다.

> 현재 확인된 사실은 ○○입니다.
> 저는 △△가 더 적절하다고 보고 있습니다.
> 다만 실제 저장소와 계약 기준에서 이 판단이 맞는지 검토해 주세요.
> 반론이나 더 나은 대안이 있다면 제안해 주세요.
> 구현은 의견을 맞춘 뒤 결정하겠습니다.

사용자가 구현을 결정한 이후에는 간결한 구현 요청으로 전환한다.

## 6. 작업 재개 규칙

새 작업을 시작하기 전에 다음을 확인한다.

```text
현재 HEAD
  ↓
CLAUDE.md의 현재 상태
  ↓
현재 작업의 계약 문서
  ↓
최근 구현/검증 결과
  ↓
사용자의 이번 요청
```

이 다섯 가지가 일치하면 작업을 진행한다. 일치하지 않으면 먼저 차이를 설명하고 어느 상태를 정본으로 볼지 확인한다. 추측으로 상태를 합치지 않는다.

## 7. 문서 추가 원칙

새로운 상태 문서를 만들기 전에 기존 문서의 역할을 먼저 확인한다.

특히 다음과 같은 중복 문서를 임의로 추가하지 않는다.

- `PROJECT_STATE.md`
- `CURRENT_TASK.md`
- 별도의 일반 `HANDOFF.md`

이미 존재하는 `CLAUDE.md`, 계약 문서, 완료기록, 회고 문서가 같은 정보를 담당한다면 새 정본을 만들지 않는다.

새 문서가 필요하다면 기존 문서와 어떤 역할이 다른지 먼저 명시한다.

## 8. 상태 기록 원칙

- `DONE` = 코드와 검증으로 완료가 확인됨
- `IN PROGRESS` = 구현 중
- `BLOCKED` = 명확한 blocker 존재
- `PLANNED` = 아직 구현하지 않음
- `TBD` = 결정되지 않음

과거 계획을 현재 완료 상태로 읽지 않는다. 최신 commit과 실제 테스트 결과가 우선한다.

## 9. 충돌 처리

다음과 같은 충돌이 있으면 자동으로 임의 선택하지 않는다.

```text
코드 ≠ CLAUDE.md
계약 문서 ≠ 코드
완료기록 ≠ 최신 구현
과거 인수인계 ≠ 최신 상태
```

먼저 최신 commit과 실제 파일을 확인한 뒤, 문서가 낡은 것인지, 코드가 계약을 위반한 것인지, 의도적인 설계 변경인지, 사용자의 새로운 결정이 필요한 것인지 구분한다.

## 10. 핵심 원칙

> ChatGPT는 이전 대화를 복원하려고 하지 않는다. GitHub의 현재 정본을 읽고 현재 상태를 재구성한다.

> Claude와 ChatGPT는 서로의 판단을 검토할 수 있지만, 최종 프로젝트 결정권은 사용자에게 있다.

> 새 세션에서는 전체 저장소를 무작정 읽지 않고, 현재 상태 → 관련 계약 → 필요한 근거 순으로 범위를 좁힌다.

> AI Lab의 중요한 연구·검증 결과는 대화에만 남기지 않고 `research/strategy-lab/findings/RESEARCH-FINDINGS.md`에 누적하여 새 세션에서도 복구 가능하게 한다.
