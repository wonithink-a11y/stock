# /etc/nginx/conf.d/stock-ratelimit.conf — http 문맥의 요청 수 제한 구역(nginx-wonithink-stock.conf·autotrader 가 쓴다)
limit_req_zone $binary_remote_addr zone=stock_public:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=autotrader_web:10m rate=60r/m;
